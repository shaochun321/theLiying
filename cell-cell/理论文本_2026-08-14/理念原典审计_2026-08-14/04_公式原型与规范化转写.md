# 公式原型与规范化转写

## 1. 转写规则

共享网页把部分公式拆成单字符行。本文件按上下文恢复为标准 LaTeX，并标明来源强度。规范化转写只恢复结构，不增加新符号语义。

## 2. 生成深度主链

来源 E1-01；符号转写 F2，语义 F3。

$$
\boxed{
\chi_\alpha^{(d)}
\xrightarrow{\mathcal N}
\widehat y_\alpha^{(d)}
\xrightarrow{\mathcal C}
x_\rho^{(d+1)}
\xrightarrow{\mathcal Q}
\chi_\rho^{(d+1)}.}
$$

不可添加：$\mathcal N,\mathcal C,\mathcal Q$ 的唯一解析式、能量形式或线性性质。

## 3. 组织资格链

来源 E1-02；F3。

$$
\boxed{
\text{耦合}
\to
\text{组织候选}
\to
\text{闭合发生}
\to
\text{生成元资格}.}
$$

## 4. 表示限制候选

来源 E3-01；C。

$$
H_n=(\xi_0,\ldots,\xi_n),
\qquad
m_n=\mathcal R(H_n).
$$

$$
\mathcal R(H_n)=\mathcal R(H_n')
\Longrightarrow
X_{n+k}(H_n)=X_{n+k}(H_n')
\quad\forall k\ge0,
$$

前提是后续输入相同且内部动力确定。原创性未审计。

## 5. 稳定接口提升候选

$$
Q_d:\Gamma^{(d)}\to Y^{(d)},
$$

$$
\alpha_d:\Gamma^{(d)}\to U^{(d+1)},
\qquad
\rho_d:U^{(d+1)}\to\Gamma^{(d)},
$$

$$
\alpha_d\circ\rho_d
\approx
\operatorname{id}_{U^{(d+1)}}.
$$

符号 $\approx$ 的度量、容差和物理意义未定义。

## 6. 后验再作用候选

$$
B_{n+1}=\Psi(B_n,m_n,u_{n+1}),
$$

$$
F_{B_{n+1}}(m_n)
\ne
F_{B_n}(m_n).
$$

含义：过去未被重写，同一沉积在新结构中的未来作用改变。

## 7. 有限支持可延展候选

$$
C_n=(B_n,m_n,E_n),
$$

$$
\forall N\in\mathbb N,\quad
\exists C_0\to C_1\to\cdots\to C_N,
$$

$$
|\operatorname{supp}C_n|<\infty
\quad\forall n<\infty.
$$

## 8. 谱系—区间实例不增殖候选

来源 E3-02；C+。

$$
\operatorname{Id}\!\left(\chi_\alpha^{(d)}\right)
=
\left(
\Lambda_\alpha^{(d)},
I_\alpha^{(d)}
\right).
$$

$$
\boxed{
\Delta\Lambda_\alpha^{(d)}=0,
\quad
\Delta I_\alpha^{(d)}=0
\Longrightarrow
\Delta N_{\mathrm{occ},\alpha}^{(d)}=0.}
$$

$$
N_{\mathrm{formal}}^{(d)}
\le
N_{\mathrm{admissible\ parent\ lineage\ intervals}}^{(d)}.
$$

## 9. 正式闭合四分解

来源：共享页第 1125—1224 行；C。

$$
\boxed{
\mathcal Q_{\alpha,\mathrm{formal}}^{(d)}
=
\mathcal Q_{\alpha,\mathrm{dyn}}^{(d)}
\land
\mathcal Q_{\alpha,\mathrm{lineage}}^{(d)}
\land
\mathcal Q_{\alpha,\mathrm{support}}^{(d)}
\land
\mathcal Q_{\alpha,\mathrm{rearm}}^{(d)}.}
$$

$$
\boxed{
\text{动力学 rearm}
\ne
\text{本体实例 rearm}.}
$$

这不是早期 $\mathcal Q$ 原定义，而是依据 D1/R1 失败提出的修订候选。

## 10. 三个经验命题

$$
\tau_u=\inf\{t:\chi_u(t)=1\},
\quad
P_T(u)=\Pr(\tau_u\le T),
$$

$$
u_{\mathrm{on}}(T;p)
=
\inf\{u:P_T(u)\ge p\},
$$

$$
T_2>T_1
\Longrightarrow
u_{\mathrm{on}}(T_2;p)
\le
u_{\mathrm{on}}(T_1;p).
$$

$$
\left|\frac{dA}{du}\right|\approx0,
\qquad
\left\|\frac{dT}{du}\right\|>0.
$$

$$
H(x_A)\approx H(x_B),
\qquad
d_{\mathrm{ext}}(A,B)>0
\centernot\Longrightarrow
A\perp_{\mathrm{internal}}B.
$$

三者是局部工程命题，不是完整 TOPRXin 公理。

## 11. 观察与评价

$$
\rho^{\mathrm{obs}}
\ne
\Delta w^{\mathrm{DA}},
$$

$$
\frac{\partial\rho^{\mathrm{obs}}}
{\partial d^{\mathrm{DA}}}=0,
\qquad
\Delta w
=
\Phi(
\rho^{\mathrm{obs}},
d^{\mathrm{DA}},
e^{\mathrm{plastic}}
).
$$

偏导为零只针对一次给定关系发生的判定路径，不排除 DA 改变未来结构。
