# 运动势时空测度语言：量纲锚定、元构建与网络构建的统一框架

**文档性质**：正式架构文档  
**日期**：2026-06-30  
**关联组件**：TSI功率账本、Xin张力动力学、前庭v2.0、体感网络层、短连接串联  
**版本**：v1.0


## 一、概述：为何需要时空测度语言

当前系统面临三个尚未对齐的维度：

| 维度 | 当前状态 | 需要解决的问题 |
| :--- | :--- | :--- |
| **量纲** | 无量纲参数（oto_x=6.0）| 绑定物理单位（rad/s, m/s², V, A） |
| **元构建** | 前庭结构固定但参数任意 | 建立可配置的“神经链路单元”，与参数实例解耦 |
| **网络构建** | 短连接串联规则待实现 | 用地址系统取代硬编码Bundle |

运动势时空测度语言 $\nu$ 是连接这三者的桥梁。它提供了：

1. **量纲锚点**：$\nu$ 的量纲是功率（W），所有系统参数最终都可追溯到功率
2. **元构建边界**：每个元构建贡献一份 $\nu$ 的局部功率，可独立核算
3. **网络构建准则**：$\nu$ 的梯度驱动短连接串联结构——能量效率最大化原则


## 二、运动势 $\nu$ 的精确定义

### 2.1 Xin 张力

Xin 张力 $\xi_j(t)$ 是影子层预测误差的漏积分器：

$$
C_{\xi} \frac{d\xi_j(t)}{dt} = -\frac{\xi_j(t)}{R_{\xi}} + \left| O_j(t) - P_j(t) \right|
$$

其中：
- $O_j(t)$：实际观测（传感器输入）
- $P_j(t)$：影子层预测
- $C_{\xi}$：Xin 张力电容（单位：F，量纲=功率×时间²/电压²）
- $R_{\xi}$：Xin 张力漏电阻（单位：Ω）

**物理量纲**：
$$
[\xi] = \text{V} \quad \text{（电压，与膜电位同量纲）}
$$

### 2.2 运动势

运动势是 Xin 张力在系统中的“功率通量”：

$$
\nu(t) = \frac{d}{dt} \left( \sum_j \frac{1}{2} C_{\xi} \xi_j^2(t) \right) = \sum_j C_{\xi} \xi_j(t) \frac{d\xi_j(t)}{dt}
$$

**物理量纲**：
$$
[\nu] = \text{W} = \text{kg} \cdot \text{m}^2 \cdot \text{s}^{-3}
$$

**物理意义**：
- $\nu > 0$：系统处于**探索态**——张力增长，能量倾斜给传输功率 $P_T$，驱动运动
- $\nu < 0$：系统处于**固化态**——张力降低，能量倾斜给结构功率 $P_S$，固化权重
- $\nu = 0$：系统处于**稳态平衡**（内稳态点）

**这是系统“行为模式切换”的物理判据。** 它不依赖任何外部标签或阈值判断。


## 三、$\nu$ 作为量纲锚点

### 3.1 统一功率账本

TSI 不等式确保所有功率项的物理一致性：

$$
P_{\text{input}} \ge P_T + P_S + P_I
$$

将 $\nu$ 嵌入 TSI 账本：

$$
\nu(t) = \frac{d}{dt} \left( \frac{1}{2} C_{\xi} \xi^2 \right) = \frac{\xi^2}{R_{\xi}} - \frac{\xi}{C_{\xi}} \left| O - P \right|
$$

**量纲一致性验证**：

| 项 | 量纲 | 来源 |
| :--- | :--- | :--- |
| $\xi^2 / R_{\xi}$ | V²/Ω = W | 功率 |
| $\xi \cdot \|O-P\| / C_{\xi}$ | V·V/F = W | 功率 |
| $\nu$ | W | 功率 |

**关键结论**：$\nu$ 的量纲天然是功率，无需额外转换。所有系统参数（增益、时间常数、发放率）最终都可以追溯到功率量纲，这为参数校准提供了**物理锚点**。

### 3.2 从 $\nu$ 推导其他参数的物理单位

| 系统参数 | 当前状态 | 通过 $\nu$ 推导的物理单位 | 关系式 |
| :--- | :--- | :--- | :--- |
| oto_x 幅度 | 6.0（无量纲）| rad/s | $\nu$ 的输入分量 → 角速度 |
| MET 跨导 gm | 2.0（无量纲）| A/(rad/s) | $\nu_{\text{input}} = g_{\text{MET}} \cdot \omega \cdot V_{\text{driving}}$ |
| Aff 发放率 | 2 Hz（可测）| 1/s | 发放率 = $\nu / E_{\text{spike}}$ |
| synapse_gain | 16.0（无量纲）| 无单位（纯增益）| 增益 = $\nu_{\text{out}} / \nu_{\text{in}}$ |

**具体推导链**：

```
角速度 ω (rad/s)
    ↓ MET 跨导 g_MET (A·s/rad)
MET 电流 I_MET = g_MET × ω (A)
    ↓ HC 增益 G_HC (无单位)
HC 输出电流 I_HC = G_HC × I_MET (A)
    ↓ HC→Aff 增益 G_HC_Aff (无单位)
Aff 发放率 f_Aff = I_HC × R_Aff / E_spike (1/s)
    ↓ Aff→Enc 增益 G_Aff_Enc (无单位)
Enc 膜电位 V_Enc = f_Aff × Q_spike × R_Enc (V)
    ↓ Enc→Col 增益 G_Enc_Col (无单位)
Col 膜电位 V_Col = G_Enc_Col × V_Enc (V)
    ↓ Col→Motor 增益 G_Col_Motor (无单位)
Motor 膜电位 V_Motor = G_Col_Motor × V_Col (V)
    ↓ Motor→Body 转换
Body 速度 v = η_motor × V_Motor (mm/s)
```

**所有增益都是无量纲的纯数**，只有输入和输出绑定物理单位。这就是量纲锚定的本质：**增益是物理量之间的转换系数，增益本身不需要单位，只需要知道输入和输出的单位。** 类似地，对数转换的性质同样源自这种量纲一致性——它将输入的物理量映射到输出的物理量，而自身的单位只是比值。

### 3.3 与当前实验参数的对齐

将当前实验观测值嵌入这个链中：

| 观测 | 单位 | 在链中的位置 |
| :--- | :--- | :--- |
| oto_x=6.0 | rad/s（待锚定）| 输入 $\omega$ |
| Aff=2 Hz | 1/s | $f_{\text{Aff}}$ |
| V_Enc=0.40V | V | Enc 膜电位 |
| Motor EMA=0.007-0.014 | V | Motor 膜电位 |
| body 速度≈0.001 | mm/s | 输出 $v$ |

**临时锚定**：将 oto_x=1.0 锚定为 0.01 rad/s（≈0.57°/s）是基于 Aff=2 Hz 反推的。这是一个自洽的临时约定，但不影响 $\nu$ 的物理一致性。


## 四、$\nu$ 与元构建：局部功率核算

### 4.1 每个元构建的 $\nu$ 贡献

元构建 $k$（如前庭 axis）对总运动势的贡献：

$$
\nu_k(t) = C_{\xi,k} \xi_k(t) \frac{d\xi_k(t)}{dt}
$$

**物理意义**：每个元构建独立产生一份“探索/固化”驱动力，$\nu_k$ 的正负决定该元构建当前处于学习态还是冻结态。

### 4.2 元构建之间的功率耦合

元构建之间的功率流动：

$$
\nu_{\text{total}} = \sum_k \nu_k + \sum_{k < l} \nu_{kl}
$$

其中 $\nu_{kl}$ 是元构建 $k$ 与 $l$ 之间的交叉项，由短连接串联的拓扑距离决定：

$$
\nu_{kl} \propto \frac{1}{L_{kl}}
$$

**物理意义**：相距越近的元构建，功率耦合越强，形成密集的短连接网络。长距离连接通过多跳串联实现，每跳增加功率损耗——这为短连接串联提供了热力学依据。

### 4.3 元构建内部的功率平衡

在稳态，元构建内部的功率平衡满足：

$$
\nu_k = \nu_{k,\text{in}} - \nu_{k,\text{out}} - \nu_{k,\text{diss}}
$$

- $\nu_{k,\text{in}}$：从传感器输入的功率
- $\nu_{k,\text{out}}$：输出到下游的功率
- $\nu_{k,\text{diss}}$：在元构建内部耗散的功率（焦耳热）

**这个平衡方程是 TSI 账本在元构建层面的微观表达。**


## 五、$\nu$ 与网络构建：短连接串联的物理基础

### 5.1 短连接串联的能量最优性

假设两点 $a_i$ 和 $a_j$ 之间需要通信。有两种方式：
1. **直接长连接**：单跳，延迟 $d_{ij}/v_{\text{cond}}$，维持成本 $\kappa_1 \cdot d_{ij}$
2. **多跳短连接**：$n$ 跳，延迟 $\sum d_k/v_{\text{cond}}$，维持成本 $\kappa_1 \cdot \sum d_k$

在 $\nu$ 框架下，最优路径是使总功率损耗最小：

$$
\text{Path}_{\text{opt}} = \arg\min_{\gamma} \sum_{e \in \gamma} \left( \frac{L_e}{v_{\text{cond}}} \cdot P_{\text{spike}} + \kappa_1 \cdot L_e \right)
$$

其中 $L_e$ 是单跳的拓扑距离，$P_{\text{spike}}$ 是每跳的 spike 功率。

**短连接串联的最优性条件**：
- 当直接连接 $d_{ij} > \theta$ 时，多跳路径的功率损耗更低
- 阈值 $\theta$ 由 $\nu$ 的梯度决定：$\theta = \nu / \nabla\nu$

### 5.2 $\nu$ 驱动的拓扑生长

当 $\nu > 0$（探索态），系统倾向于 sprout 新连接：

$$
P(\text{sprout}_{ij}) \propto \nu \cdot \exp(-\lambda \cdot L_{ij})
$$

当 $\nu < 0$（固化态），系统倾向于 prune 低效连接：

$$
P(\text{prune}_{ij}) \propto |\nu| \cdot \frac{1}{1 + L_{ij}}
$$

**这意味着**：在探索态（$\nu>0$），新连接生长概率与 $\nu$ 成正比，且随距离指数衰减——短连接优先。在固化态（$\nu<0$），长距离低效连接被优先修剪。这是一个完整的热力学闭环。


## 六、$\nu$ 与带宽的关系

### 6.1 $\nu$ 的频率谱特性

Xin 张力的动力学方程是一个低通滤波器，其截止频率由 $R_{\xi} C_{\xi}$ 决定：

$$
f_c = \frac{1}{2\pi R_{\xi} C_{\xi}}
$$

当前 $R_{\xi} C_{\xi} \approx 1\text{s}$ → $f_c \approx 0.16$ Hz。

**这解释了为什么 DR2 在 200k 步（200 秒）内波动 30%**：系统的有效时间常数是秒级，200 秒的测量窗口对于长期行为趋势是足够的，但对于瞬时快照来说，随机轨迹的差异依然显著。$\nu$ 作为功率通量，通过平滑 $f_c$ 以下的频率分量来抑制这种随机波动。

### 6.2 带宽的量纲一致性

带宽的量纲是 1/s（Hz），可以通过 $\nu$ 推导：

$$
\text{Bandwidth} \sim \frac{\nu}{E_{\text{total}}}
$$

其中 $E_{\text{total}}$ 是总能量储备（fill）。带宽与能量消耗的比值是系统信息处理效率的量度：

$$
\text{效率} = \frac{\text{Bandwidth}}{P_{\text{consumed}}}
$$

**物理意义**：带宽不是独立设定的，而是由 $\nu$ 和能量储备共同决定。


## 七、$\nu$ 与量纲锚定的完整映射表

| 物理量 | 符号 | 量纲 | 在 $\nu$ 框架中的位置 | 推导方法 |
| :--- | :--- | :--- | :--- | :--- |
| 角速度 | $\omega$ | rad/s | $\nu$ 的输入驱动项 | 从 Aff 发放率反推 |
| MET 跨导 | $g_{\text{MET}}$ | A·s/rad | $\nu$ 的转换系数 | 从 I_MET = g_MET × ω 推导 |
| HC 增益 | $G_{\text{HC}}$ | 无单位 | $\nu$ 的放大系数 | 从 I_HC = G_HC × I_MET |
| Aff 发放率 | $f_{\text{Aff}}$ | 1/s | $\nu$ 的时间导数 | $f = \nu / E_{\text{spike}}$ |
| Enc 膜电位 | $V_{\text{Enc}}$ | V | $\nu$ 的势能项 | $V = I × R$ |
| synapse_gain | $G_{\text{syn}}$ | 无单位 | $\nu$ 的缩放系数 | $G = \nu_{\text{out}} / \nu_{\text{in}}$ |
| 拓扑距离 | $L$ | mm | $\nu$ 的空间梯度 | $d\nu/dL$ |
| 时间延迟 | $\tau$ | s | $\nu$ 的时间积分 | $\tau = \int dt$ |


## 八、后续实施路线图

| 阶段 | 任务 | 依赖 | 产出 |
| :--- | :--- | :--- | :--- |
| **P0** | 确立量纲协议（本文档）| 无 | 量纲锚定规范 |
| **P1** | 将 $\nu$ 嵌入 TSI 账本 | P0 | `EnergyStore` 的功率审计 |
| **P2** | 元构建定义（含 $\nu_k$）| P1 | `VestibularPrimitive` 结构模板 |
| **P3** | 短连接串联规则 | P2 | `nearby()` + 多跳路由 |
| **P4** | 带宽校准 | P0-P3 | 频率响应验证 |


## 九、总结

| 问题 | 运动势 $\nu$ 的贡献 |
| :--- | :--- |
| 量纲缺失 | $\nu$ 的量纲是功率（W），为所有参数提供物理锚点 |
| 元构建边界 | 每个元构建的 $\nu_k$ 可独立核算，形成“功率账户” |
| 短连接串联 | $\nu$ 的梯度驱动拓扑生长/修剪，能量最优性决定连接结构 |
| 带宽 | 带宽由 $\nu$ 和能量储备共同决定，不需独立设定 |
| 实验参数对齐 | 所有观测值可追溯到 $\nu$ 链中的某个节点 |


**核心论断**：

运动势时空测度语言 $\nu$ 不是“另一个概念”，而是当前系统所有组件的**物理一致性校验器**。

- 如果某个参数无法通过 $\nu$ 的量纲链推导出来，它就没有物理锚点。
- 如果某个连接结构不符合 $\nu$ 的能量最优性条件，它就不稳定。
- 如果某个元构建的 $\nu_k$ 无法独立核算，它就没有被正确定义。

**这是“最小物理一致性”的标准。** 在此意义上，量纲锚定、元构建定义和网络构建不再是三个独立的任务，而是同一个问题——物理一致性——的三个不同视角。