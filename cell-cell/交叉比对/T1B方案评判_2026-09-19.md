# T1-B 方案评判分析（MAINLINE V2 — 跨 World 标定与 G0 回接前置）

日期：2026-09-19
评判对象：《MAINLINE V2 — T1-B：Transduction v2 跨 World 标定与 G0 回接
前置方案》（61 节，`cell-cell/交叉比对/`）
评判依据：W1 收口（894762f）+ T1-A 收口（563ac0b）。

---

## 一、总评：ADOPT with amendments C1-C5——五轮以来继承最精确的一份

61 节全核对，继承零失真：T1-A 四结论逐字准确（§2）；候选冻结 A/B/legacy
且禁新增（§29 "不得临时发明 Candidate D 救场"）；C 保持
REJECTED_AT_D_LAYER→FUTURE_L1_INTERNAL_CANDIDATE（§4）；held-out 必含
r_leak≈20 强耗散角（§7）=W1 负结果 4；§5 层级裁定"只做合同不改
production"=评判 B3 折中落实；§56 文档已自压缩为 4+负结果。

方法学亮点：
- **§18-19 认识论正确**：D 不得"推断回"隐藏态——Y_A(t0)=Y_B(t0) 时合法
  无未来访问的 D 必须输出相同，否则 HIDDEN_ACCESS_OR_STATE_LEAK=FAIL；
  B 的 prev-sample 差异必须可由边界 replay 完整解释。
- **§36 STRUCTURALLY_EXPECTED_ZERO** + **§37 World-loss vs D-loss 强制
  归因**——W0-E 以来"塌缩在哪层"方法论的制度化。
- **§40 blind held-out 冻结流程**（freeze→canonical+fingerprint→lock→
  一次；失败先登记 FIRST_HELDOUT_RESULT 再归因）——F1 fingerprint 纪律同源。
- **§12/§23 合法域替代 argmax**；CANONICAL_FOR_IMPLEMENTATION≠OPTIMAL。

## 二、修正建议（C1-C5，无阻塞裁定项）

| # | 修正 |
|---|---|
| C1 | 脚本 7→5（dataset / calibration+cross_world+LORO / heldout+replay+twins / timebase_audit / final_qualification） |
| C2 | timebase 锚定给具体数：Δt_ext=1 s（锚=SkinPatch τ=5s 既有约定；τ_env∈[20,2000]s 物理合理）；generator dt=0.001=内部积分粒度（非物理秒）；耦合合同=每 t_ext 消费 1 边界样本（现行 1:1 步配对显式化）——D3 双时钟合同层了结 |
| C3 | canonical 值 provenance **在 held-out 锁定前**预承诺：S/g=legacy κ 派生，标 CANONICAL_REFERENCE（延续现 G0 输入尺度直至端口改造） |
| C4 | 数据集具体化：cal=30/hold=20 新 seed 族；LORO 两组（cal 排 τ_env<50；cal 排 3 源）；strata 只按物理量 |
| C5 | Candidate B 状态初始化合同写死（prev=首样本，u_B(0)=0）保证 replay 逐位 |

## 三、可行性

全部基建在仓：数据集=world_v2_core Sampler/Episode（W1 冻结只读）；
dt 三档=W1 收敛机制复用；双 twin=W1 冻结协议复用；replay=WT0 模式复用；
smoke=exp_P2A1b_3 接线（G0 只运行）。计算量秒级。

## 四、结论

ADOPT with amendments C1-C5，执行 T1-B 全轮；终态四选一如实给。
