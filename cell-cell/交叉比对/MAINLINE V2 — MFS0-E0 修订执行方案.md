# MAINLINE V2 — Memory / Feedback Substrate-0

## MFS0-E0：G1 历史资格、后续调制与持久结构沉积资格实验

**版本：2026-09-23 修订版**  
**状态：ADOPT WITH AMENDMENTS / READY FOR STEP 0**  
**执行范围：research-only；production READ_ONLY**

---

# 0. 本轮定位

当前已经冻结：

```text
G0_OCCURRENCE_V2_QUALIFIED
D2_RELATION_PROCESS_V0_QUALIFIED
D2_1_RECURSIVE_GENERATION_QUALIFIED
G1_QUALIFIED
```

其中 G1 当前资格范围仍为：

```text
REPLAY / REFERENCE QUALIFICATION
```

不解释为 live end-to-end G1。

D2-1 已证明：

\[
\chi_\rho^{(1)}
\]

能够通过与基础 occurrence 同原则的 typed interface 再次进入新的关系结构，而且其动态状态可以改变未来，并能够通过状态移植关闭未来差异。

Posterior-0 当前按 2026-09-22 后续修补终态冻结为：

```text
POSTERIOR-0 = TERMINAL C
POSTERIOR_EFFECT_ADDITIVE_ONLY = TRUE
POSTERIOR_STATE_REORGANIZATION_V0_QUALIFIED = FALSE
READY_FOR_POSTERIOR_1 = FALSE
```

因此：

```text
DO NOT reopen Posterior-0
DO NOT run Posterior-1
```

MFS0 是新的结构设计轮，不反向修改 Posterior-0 结果。

---

# 1. 本轮唯一研究问题

G1 已回答：

\[
\boxed{
\text{一个生成出来的关系状态能否重新成为未来的因果参与者？}
}
\]

当前答案为 YES。

MFS0-E0 只继续问：

\[
\boxed{
\text{这个因果参与者发生之后，
能否留下一个有限的物理修改资格？}
}
\]

以及：

\[
\boxed{
\text{后来发生的物理事件，
能否通过另一个独立物理状态，
选择性地把这段历史沉积为持久结构变化？}
}
\]

要求最终形成：

\[
\boxed{
Past\rightarrow e
}
\]

\[
\boxed{
Later\rightarrow M
}
\]

\[
\boxed{
(e,M)\rightarrow\Delta z
}
\]

并在：

\[
e\rightarrow0,\qquad M\rightarrow0
\]

以后仍满足：

\[
\boxed{
z^+\neq z^-.
}
\]

进一步必须：

\[
\boxed{
Response(Q|z^+)
\neq
Response(Q|z^-).
}
\]

---

# 2. 本轮正式物理结构

本轮冻结：

\[
\boxed{
z=w
}
\]

即：

```text
persistent state candidate = Memristor.w
```

完整候选链：

\[
\chi_\rho^{(1)}
\rightarrow
C_e
\rightarrow
e_W(t)
\]

以及独立的 later occurrence：

\[
\Delta
\rightarrow
H_\tau^\Delta
\rightarrow
M_\Delta(t).
\]

然后：

\[
e_W
\rightarrow
FET_e
\]

\[
M_\Delta
\rightarrow
FET_M
\]

形成 gate-dependent write current：

\[
I_{\rm write}.
\]

再：

\[
I_{\rm write}
\rightarrow
Memristor.update(...)
\rightarrow
w.
\]

最终：

\[
w
\rightarrow
G(w)
\rightarrow
I
\rightarrow
Future.
\]

---

# 3. 四项开工裁定

## R-1 — Modulation source

正式采用：

\[
\boxed{
H_\tau
}
\]

作为 MFS0-E0 的 later-event modulation 基底。

不得复用旧：

```text
reward DA
RPE
hunger_to_da
satiety_to_da
thermo_delta_to_da
preset da_concentration
```

Hτ 必须建立为 **later occurrence 专属实例**：

\[
H_\tau^\Delta.
\]

必须保证：

```text
Past W → e path
```

和：

```text
Later Δ → HτΔ → M path
```

拥有：

- 独立 state；
- 独立 lineage；
- 独立 physical input；
- 独立地址。

禁止：

\[
W\rightarrow H_\tau^\Delta.
\]

否则 \(M\) 可能只是 \(e\) 的重复编码。

评判已经确认 Hτ 是现有语义较清白的候选之一，而且具有已有真实历史电容状态；同时也明确指出必须解决它与 \(e\) 的独立性问题。

---

## R-2 — Physical multiplication tier

采用：

```text
MFS0_MULTIPLICATION_TIER = 2
```

即：

\[
e\rightarrow FET_e
\]

和：

\[
M\rightarrow FET_M
\]

两个物理量分别经过真实器件转换，再组成 gate-dependent write quantity。

采用已有双 FET / DEG-019 同族结构先例。

必须登记限制：

```text
LIMITATION:
two factors are generated by physical devices,
but final factor composition is not a fully
self-consistent series-node analog solution.
```

因此本轮不得宣称：

```text
fully emergent analog multiplication
```

档 3 的自洽串联负载线乘法留待未来强化轮，不阻塞 MFS0-E0。

评判确认档 2 已有既存项目先例和用户裁定基础，而档 3 会显著增加本轮结构复杂度。

---

## R-3 — Posterior status

正式冻结：

```text
POSTERIOR0_TERMINAL_C_FROZEN = TRUE
```

MFS0 的结果不得反向修改 Posterior-0。

若 MFS0 成功，只允许解释为：

> 新增持久结构载体以后，项目第一次获得载体级历史沉积机制。

不能解释为：

> Posterior-0 原来其实 PASS。

---

## R-4 — Initial memristor working point

正式冻结：

\[
\boxed{
w_0=0.90
}
\]

登记：

```text
MFS0_INITIAL_W = 0.90
NO_RUNTIME_W0_SEARCH = TRUE
```

原因是旧 \(w\approx0.1\) 工作区已知存在严重 conductance readout compression；评判指出这一限制在正式实验前即可解析估计，而不应该运行以后才发现 M5 无法测量。

禁止：

```text
0.90 → 0.92 → 0.95 → 0.98
```

一类结果驱动寻优。

如果 \(w_0=0.90\) 仍无法产生可测未来作用，则接受对应负终态。

---

# 4. Eligibility 的正式定义

历史输入只能来自：

\[
\chi_\rho^{(1)}
\]

及：

```text
RelationOccurrencePortV1
```

禁止读取：

```text
RelationCell membrane state
RelationInputNeuron internal state
bundle internal trace
hidden state
semantic relation label
```

现有 relation port 已存在，而且能够被当前 typed replay infrastructure 消费。

---

## 4.1 Physical carrier

定义：

\[
\boxed{
e(t)=V_{C_e}(t)
}
\]

真实状态载体必须使用：

```text
semiconductor.Capacitor
```

而不是：

```text
bundle._eligibility_traces
Python float
list
dictionary
```

动力学：

\[
C_e\frac{dV_e}{dt}
=
I_e
-
\frac{V_e}{R_e}.
\]

所以：

\[
\tau_e=R_eC_e.
\]

---

## 4.2 τe 冻结

正式预注册：

\[
\boxed{
\tau_e=0.300\,s
}
\]

依据：

旧 eligibility 原始意图为约：

\[
300\ generator\ steps.
\]

在冻结时间合同：

\[
dt_G=0.001s
\]

下对应：

\[
300\times0.001=0.300s.
\]

这里不复用旧 step-based 数值实现，只继承其物理尺度意图，然后重新通过：

\[
R_eC_e
\]

实现真实秒制 RC。

登记：

```text
TAU_E = 0.300 s
TAU_E_RUNTIME_RETUNING = FORBIDDEN
```

不得因为 later timing 无法落入窗口而修改 \(\tau_e\)。

评判特别指出，\(\tau_e\) 若不在 timing 结果之前冻结，会成为整个方案最大的自由度漏洞。

---

## 4.3 Eligibility 不编码 amplitude

本轮选择：

```text
ELIGIBILITY_AMPLITUDE_ENCODING = NO
```

因此：

\[
e
\]

只表达：

> 一个 relation occurrence 最近发生过，当前仍处于有限修改资格窗口。

不表达：

> occurrence 有多强。

输入采用现有 occurrence window / phase typed contract。

不得为 MFS0 打开新的：

```text
U_T amplitude port
```

也不得为了获得强度信息去读取 RelationCell 内态。

评判已经确认当前 `build_phase_drive` 的合法输入本身只有 occurrence 窗口边界，没有 amplitude 字段。

---

## 4.4 Canonical readout

Eligibility 的正式读出：

\[
\boxed{
e(t)=V_{C_e}(t)
}
\]

即直接读取 capacitor voltage。

不得把 MOSFET 后的：

```text
activation
```

作为 canonical eligibility state。

activation 只允许：

```text
DIAGNOSTIC_ONLY
```

因为硬阈值可能把真实存在的 RC residual 截成零。Posterior-0 已经实际暴露过这一测量面问题。

---

# 5. Modulation 的正式定义

定义：

\[
\boxed{
M(t)=M_\Delta(t)
}
\]

其中：

\[
\Delta
\]

必须是合法 later physical occurrence。

本轮不允许：

```python
M = constant
da_concentration = 0.5
```

作为正式路径。

旧 R-Prec plastic test 中常数 DA 输入必须明确登记为：

```text
LEGACY_SEMANTIC / TEST CONVENIENCE PATH
NOT MFS0 INPUT
```

评判已确认这一点正是现有旧可塑性实现的主要污染点之一。

---

# 6. Semantic Erasure Audit

Step B 开始以前必须输出：

```text
SEMANTIC_ERASURE_AUDIT.json
```

至少检查并明确拒绝：

```text
reward
punishment
RPE
hunger
satiety
warming-good
intake reward
fill-rate reward
positive-valence clamp
preset DA
```

HτΔ 只能代表：

> later occurrence 留下的局部历史动力学。

不得代表：

```text
good outcome
bad outcome
success
failure
reward
error
```

---

# 7. Memristor 写入

不使用：

```text
bundle.learn()
```

原因：

旧 bundle eligibility / DA EMA 仍依赖 step-era canonical 常量，而且 eligibility 并非真正 Capacitor state。评判明确认定这些机制不应直接进入 MFS0。

本轮使用已有：

```text
Memristor.update(current, pre_trace, post_trace)
```

和 A8 candidate 中已存在的 gated write-current 原型作为结构参考。

评判确认这已经是仓库中真实存在的“write current → w”物理入口，不需要重新发明一套 learning algorithm。

---

# 8. Memristor 持久性合同

正式冻结：

```text
MFS0_W_METABOLIC_LEAK = OFF
```

本轮问题是：

> 写入能否沉积？

不是：

> 长期记忆应该以什么时间常数遗忘？

所以不引入旧：

```text
lambda_metabolic
```

本轮 \(w\) 是无恢复力的持久连续状态，而不是 RailLatch 型双稳态。

必须在报告中明确：

```text
Memristor.w persistence
= marginal continuous persistence / integrator-like
≠ bistable latch
```

评判已经指出 memristor 与 RailLatch 的动力学身份完全不同。

washout 同时报告：

\[
Retention_w=
\frac{|w(t_{washout})-w_0|}
{|w(t_{write-end})-w_0|}.
\]

---

# 9. RailLatch 的角色

RailLatch 保留为：

```text
MEASUREMENT_CHAIN_POSITIVE_CONTROL
```

它只回答：

> 当前 measurement/query chain 能否检测一个已知存在的持久差异。

不允许：

```text
RailLatch = alternative MFS0 z
```

RailLatch 的状态是：

\[
V_C
\]

而不是：

\[
w.
\]

同时它目前只存在于 research/A8 路径，并非主干 production primitive。

---

# 10. Step 0 — Pre-registration + Integrity Seal

建立：

```text
research/memory_feedback_substrate_v0/
```

第一步不得运行主实验。

先生成：

```text
preregistration.json
frozen_upstream_seal.json
budget_ledger.json
```

冻结：

```text
MULTIPLICATION_TIER = 2
M_SOURCE = H_tau_delta
TAU_E = 0.300 s
INITIAL_W = 0.90
W_METABOLIC_LEAK = OFF
ELIGIBILITY_AMPLITUDE_ENCODING = NO
NEW_G0_TRAJECTORIES = 0
```

同时冻结：

```text
epsilon_e
epsilon_M
epsilon_z
epsilon_selectivity
epsilon_Q
S_min
```

以及：

```text
initial topology
query definition
washout rule
timing-control rule
heldout protocol
```

---

## 10.1 SHA seal

对：

```text
G0 frozen data
D2-0 frozen data
D2-1 frozen data
OccurrencePortV2
RelationOccurrencePortV1
reused occurrence traces
lineage manifests
```

做 SHA256。

实验结束重新计算。

要求：

```text
BEFORE == AFTER
```

否则：

```text
MFS0-INTEGRITY = FAIL
```

---

# 11. Step A — Physical Eligibility Qualification

组装：

\[
RelationOccurrencePortV1
\rightarrow
build\_phase\_drive
\rightarrow
RelationInputNeuron
\rightarrow
frozen\ SynapticBundle
\rightarrow
C_e.
\]

production 不新增模块。

输出：

```text
e_peak
t_peak
tau_e_measured
last_step_above_epsilon
query_relevant_window
capacitor_voltage_trace
charge trace
resource trace where measurable
```

必须运行：

\[
W=1
\]

和：

\[
W=0.
\]

要求：

\[
e_{W=1}>floor
\]

而：

\[
e_{W=0}\approx baseline.
\]

同时实测：

\[
\tau_e^{measured}
\]

必须与预注册：

\[
0.300s
\]

处于预先定义的工程容差内。

禁止运行后修改 \(R_e,C_e\) 追求目标。

如果失败：

```text
MFS0_PHYSICAL_ELIGIBILITY_NOT_ESTABLISHED
STOP
```

---

# 12. Step B — Physical Modulation Qualification

先完成：

```text
SEMANTIC_ERASURE_AUDIT
```

再组装：

\[
\Delta
\rightarrow
H_\tau^\Delta
\rightarrow
M(t).
\]

必须运行：

\[
\Delta=1
\]

和：

\[
\Delta=0.
\]

输出：

```text
M_peak
tau_M
support_window
baseline
state trace
resource trace
lineage
```

要求：

\[
M_{\Delta=1}>floor
\]

\[
M_{\Delta=0}\approx baseline.
\]

并确认：

\[
M(t)\rightarrow0.
\]

如果只能靠直接赋值产生 modulation：

```text
MFS0_PHYSICAL_MODULATION_NOT_ESTABLISHED
STOP
```

---

# 13. Step C — Persistent-State Baseline

在正式写入以前先独立验证：

\[
z=w.
\]

必须回答：

1. \(w\) 是否为真实内部器件状态；
2. \(w\) 是否控制 \(R(w)\)；
3. \(R(w)\) 是否控制 \(G(w)\)；
4. \(G(w)\) 是否改变 downstream current；
5. 相同 future input 下不同 \(w\) 是否理论上可测。

---

## 13.1 M5 feasibility 必须先解析计算

给定：

\[
w_0=0.90
\]

和预期最小：

\[
\Delta w_{min},
\]

先解析计算：

\[
G(w)
=
\frac{1}
{r_{min}+(r_{max}-r_{min})(1-w)}
\]

以及：

\[
\frac{\Delta G}{G}.
\]

进一步估计：

\[
\Delta I
\]

和下游最小理论 response difference。

要求在实验前回答：

\[
ExpectedSignal_Q
>
\epsilon_Q?
\]

如果解析上不可测：

```text
READOUT_FEASIBILITY_FAIL
```

在主 factorial 之前停止。

不得运行以后再调整 \(w_0\)。

---

# 14. Step D — Timing Freeze

只有 Step A/B/C 全部通过以后才允许选择 timing。

\(\tau_e\) 此时已经冻结，不能修改。

优先复用已有 G0 occurrence 库。

正式：

```text
new_g0_trajectories = 0
```

现有库存已经存在多个可用 later window，因此本轮时点可达性原则上不构成 Posterior-0 式硬阻塞。

定义两个 timing：

## NEAR

满足：

\[
e_W(t_\Delta)>floor.
\]

## TIMING CONTROL

保持：

```text
same W
same Δ
same event identity
same physical path
same event dose
```

唯一差异：

\[
e_W(t_\Delta)\approx0.
\]

禁止：

```text
shift W
retune tau_e
increase Δ amplitude
change G1
change relation parameters
```

---

# 15. Step E — 2×2 Factorial

运行：

\[
W\in\{0,1\}
\]

\[
\Delta\in\{0,1\}.
\]

四臂：

```text
00 = no Past / no Later
10 = Past only
01 = Later only
11 = Past + Later
```

记录：

\[
w_{00},w_{10},w_{01},w_{11}.
\]

---

# 16. Iz 降级

原：

\[
I_z=
(w_{11}-w_{10})
-
(w_{01}-w_{00})
\]

保留，但正式降级为：

```text
IMPLEMENTATION_CONSISTENCY_CHECK
```

因为在主动实现：

\[
\dot w\propto eM
\]

以后，\(I_z\neq0\) 很大程度上是实现结构的解析结果，而不是独立可证伪发现。

开工前应根据具体 gate 方程给出：

```text
predicted_Iz
```

实测主要检查：

```text
observed ≈ predicted
```

而不是把：

```text
Iz != 0
```

当成 MFS0 的主要资格门。

---

# 17. 主选择性指标

MFS0-M4 主判据改为 timing selectivity。

定义：

\[
\Delta w_{near}
=
w_{near}^{post}-w_{near}^{pre}
\]

\[
\Delta w_{tc}
=
w_{tc}^{post}-w_{tc}^{pre}.
\]

计算：

\[
\boxed{
D_{sel}
=
|\Delta w_{near}-\Delta w_{tc}|
}
\]

和：

\[
\boxed{
S_{sel}
=
\frac{|\Delta w_{near}|}
{\max(|\Delta w_{tc}|,\epsilon_z)}
}
\]

要求全部成立：

\[
|\Delta w_{near}|>\epsilon_z
\]

\[
D_{sel}>\epsilon_{sel}
\]

\[
S_{sel}>S_{min}.
\]

三个阈值必须在 Step 0 预注册。

ratio 不允许单独作为 PASS 条件，避免 denominator 接近零制造虚高倍率。

---

# 18. Negative Controls

必须执行：

```text
NC1 no Past
NC2 no Later
NC3 timing control
NC4 eligibility path block
NC5 modulation path block
NC6 query-write freeze
```

其中 NC3 为最重要选择性门。

### NC4

物理阻断：

\[
W\rightarrow e.
\]

保留：

\[
W,\Delta
\]

其他条件不变。

要求：

\[
\Delta w
\]

明显下降或消失。

### NC5

物理阻断：

\[
\Delta\rightarrow M.
\]

其他条件不变。

要求：

\[
\Delta w
\]

明显下降或消失。

---

# 19. Washout

写入结束后不得立即 query。

等待：

\[
e(t)<\epsilon_e
\]

且：

\[
M(t)<\epsilon_M.
\]

washout 时间由实测：

\[
\tau_e,\tau_M
\]

决定，不使用任意固定步数。

此时要求：

\[
\boxed{
e\approx0,\quad
M\approx0,\quad
w_{near}\neq w_{TC}.
}
\]

并报告：

\[
Retention_w.
\]

如果差异随 \(e/M\) 消失：

```text
MFS0_PERSISTENT_DEPOSITION_NOT_ESTABLISHED
```

---

# 20. Step F — Future Query

washout 后给所有实验臂完全相同：

\[
Q.
\]

Q 只能使用已有 typed occurrence/reference path。

禁止：

```text
memory-read instruction
special query amplitude
semantic probe
custom read command
```

记录 continuous trajectory 为主：

\[
D_Q
=
D(
Response(Q|w_{near}),
Response(Q|w_{TC})
).
\]

要求：

\[
D_Q>\epsilon_Q.
\]

如出现 occurrence difference：

```text
existence
t_up
t_down
t_rearm
margin
```

可作为强化证据，但不代替 continuous criterion。

---

# 21. Step G — State Transplant

这是 MFS0 的最强因果门。

在：

```text
washout complete
query not yet started
```

的时刻执行：

\[
do(w_{TC}\leftarrow w_{near}).
\]

只允许移动：

\[
w.
\]

不得移动：

```text
e
M
membrane voltage
history state
event trace
query state
```

然后给完全相同：

\[
Q.
\]

要求：

\[
Response_{TC}'
\approx
Response_{near}.
\]

如预算允许再做：

\[
do(w_{near}\leftarrow w_{TC}).
\]

如果 response 随 \(w\) 双向转移：

```text
MFS0_MINIMAL_SUFFICIENT_STATE_CANDIDATE = w
STOP_DEEPER_DECOMPOSITION
```

不继续拆解 memristor 内部数值细节。

---

# 22. Step H — Future Path Block

保留写好的：

\[
w
\]

但物理阻断：

\[
w\rightarrow future\ conductance/readout.
\]

如果原本：

\[
Response_{near}\neq Response_{TC}
\]

阻断后：

\[
Response_{near}^{block}
\approx
Response_{TC}^{block},
\]

则进一步支持：

\[
w
\]

是未来差异的 causal carrier。

如 transplant 强 PASS，但 block 因电路结构无法独立解释：

```text
BLOCK_LIMITATION
```

不得自动判 FAIL，但必须完整披露。

---

# 23. Held-out

全部 calibration 完成后冻结：

```text
tau_e
tau_M
w0
write-current parameters
FET parameters
timing
washout
floors
query
all thresholds
```

再建立：

```text
hold6
```

建议：

```text
3 × main-history condition
3 × matched timing/control condition
```

第一次运行前：

```text
SHA256 lock
```

运行以后：

```text
NO RETUNING
```

---

# 24. 六门资格

## MFS0-M1 — Typed Historical Entry

\[
\chi_\rho^{(1)}
\]

只能通过合法 typed interface 进入 \(e\) 路径。

禁止 hidden-state readback。

---

## MFS0-M2 — Physical Eligibility

必须存在真实：

\[
e=V_{C_e}
\]

具有：

```text
formation
finite decay
physical tau
physical carrier
```

---

## MFS0-M3 — Physical Later Modulation

later occurrence 必须形成独立：

\[
M_\Delta(t)
\]

且：

```text
no preset modulation
no reward semantic path
no shared e carrier
```

---

## MFS0-M4 — Selective Persistent Deposition

washout 后要求：

\[
|\Delta w_{near}|>\epsilon_z
\]

\[
D_{sel}>\epsilon_{sel}
\]

\[
S_{sel}>S_{min}
\]

以及：

\[
Retention_w
\]

超过预注册最低值。

\(I_z\) 不作为主 PASS 指标。

---

## MFS0-M5 — Future Causal Action

要求：

\[
D_Q>\epsilon_Q.
\]

即：

\[
Response(Q|w^+)
\neq
Response(Q|w^-).
\]

---

## MFS0-M6 — Causal Carrier + Integrity

至少要求：

```text
state transplant PASS
```

同时：

```text
frozen SHA unchanged
production unchanged
budget legal
heldout locked
semantic audit PASS
lineage valid
single physical time respected
```

全部满足。

---

# 25. 预算

```text
new_g0_trajectories        = 0

eligibility calibration    <= 6
modulation calibration     <= 6
persistent baseline        <= 6
timing points              <= 4
factorial sets             <= 4
timing controls            <= 3
state interventions        <= 4
path blocks                <= 3
heldout                    = 6
```

任何：

```text
error
aborted
duplicate
unreachable
support failure
```

只要真正运行过，都记为：

```text
attempted_run
```

不得通过更改分类逃避预算。

---

# 26. 时间合同

整个 MFS0 只有：

\[
t_{phys}.
\]

canonical 参数只能使用：

```text
seconds
```

禁止：

```text
eligibility_tau_steps
modulation_tau_steps
memory_window_steps
washout_steps
```

作为正式参数。

如果：

\[
dt=0.001s,
\]

步数只能由：

\[
N=\frac{T}{dt}
\]

派生。

旧 `bundle.learn()` 中 step-era eligibility/DA 参数不得进入本轮。

评判确认当前旧 bundle eligibility 的时间语义与现主线单物理时间合同存在直接冲突。

---

# 27. 建议目录

```text
research/memory_feedback_substrate_v0/

    mfs0_common.py
    preregistration.py
    primitive_audit.py

    eligibility_physical.py
    modulation_physical.py
    semantic_erasure_audit.py

    persistent_state_audit.py
    readout_feasibility.py

    timing_freeze.py
    factorial_trials.py
    timing_controls.py

    state_intervention.py
    path_block.py

    heldout_eval.py
    final_qualification.py

    data/
        preregistration.json
        frozen_upstream_seal.json
        primitive_audit.json
        semantic_erasure_audit.json

        eligibility_characterization.json
        modulation_characterization.json
        persistent_state_characterization.json
        readout_feasibility.json

        timing_manifest.json
        factorial_results.json
        timing_controls.json

        causal_interventions.json
        path_blocks.json

        heldout_manifest.json
        qualification_summary.json
        budget_ledger.json
```

---

# 28. Production 禁令

本轮：

```text
production = READ_ONLY
```

禁止新增：

```text
new production memory module
new DA system
new STDP system
new relation cell
new G1 layer
new modality
new Shadow path
Xin
TOPRXin
```

需要的新组装只存在于：

```text
research/memory_feedback_substrate_v0/
```

资格化以后才讨论是否迁移。

---

# 29. 终态

## A — Qualified candidate

```text
MFS0_PHYSICAL_MEMORY_SUBSTRATE_CANDIDATE_QUALIFIED
```

要求：

```text
MFS0-M1..M6 = PASS
```

只允许解释：

> 系统第一次建立了一个由历史资格、后续独立物理调制和持久结构状态组成的最小因果记忆基底候选。

禁止：

```text
memory solved
learning solved
consolidation solved
Posterior qualified
Xin qualified
cognition
semantic learning
```

---

## B — Physical eligibility absent

```text
MFS0_PHYSICAL_ELIGIBILITY_NOT_ESTABLISHED
```

\[
Past\not\rightarrow e_{physical}.
\]

---

## C — Physical modulation absent

```text
MFS0_PHYSICAL_MODULATION_NOT_ESTABLISHED
```

\[
Later\not\rightarrow M_{physical}.
\]

---

## D — No persistent deposition

```text
MFS0_PERSISTENT_DEPOSITION_NOT_ESTABLISHED
```

存在：

\[
e,M
\]

但 washout 后：

\[
w^+=w^-.
\]

---

## E — Persistent state has no measurable future causality

```text
MFS0_PERSISTENT_STATE_CAUSALITY_NOT_ESTABLISHED
```

\[
w^+\neq w^-
\]

但：

\[
Response(Q|w^+)
=
Response(Q|w^-).
\]

---

## F — Nonselective write

```text
MFS0_NONSELECTIVE_WRITE_ONLY
```

later event 在没有有效 historical eligibility 时仍产生同等级写入，或者：

\[
D_{sel}\approx0.
\]

这不能叫 history-dependent deposition。

---

# 30. 硬停止规则

得到任何：

```text
A / B / C / D / E / F
```

以后：

```text
STOP
```

本轮禁止继续：

```text
add more relation cells
increase depth
add more modalities
retune tau_e
search w0
restore DA reward
restore legacy STDP
reopen Posterior-0
run Posterior-1
implement Xin
reconnect Shadow
```

---

# 31. 实际执行顺序

严格按以下顺序：

```text
Step 0
Preregistration
→ SHA seal
→ budget ledger

Step A
Physical Eligibility

Step B
Semantic Erasure Audit
→ Physical Modulation

Step C
Persistent State Baseline
→ Analytical Readout Feasibility
→ RailLatch positive control

Step D
Freeze NEAR / TIMING CONTROL

Step E
2×2 factorial
→ NC1~NC6

Washout

Step F
Future Query

Step G
w-only State Transplant

Step H
Future Path Block

Hold6
→ Final six gates
→ Final terminal state
→ Regression closure
```

禁止跳步。

特别是：

```text
Step A/B/C 未 PASS
→ 不允许进入 factorial
```

---

# 32. 本轮最终逻辑

真正需要证明的不是：

\[
W+\Delta
\rightarrow
different\ output.
\]

也不是：

\[
eM\neq0.
\]

而是完整链条：

\[
\boxed{
\chi_\rho^{(1)}
\rightarrow
e_W
}
\]

\[
\boxed{
\Delta
\rightarrow
M_\Delta
}
\]

并且两个物理状态拥有独立载体和独立 lineage。

随后：

\[
\boxed{
(e_W,M_\Delta)
\rightarrow
I_{write}
\rightarrow
\Delta w
}
\]

再等待：

\[
e_W,M_\Delta\rightarrow0.
\]

仍得到：

\[
\boxed{
w_{near}\neq w_{control}.
}
\]

随后同一未来：

\[
Q
\]

产生：

\[
\boxed{
Response(Q|w_{near})
\neq
Response(Q|w_{control}).
}
\]

最终通过：

\[
\boxed{
do(w_{control}\leftarrow w_{near})
}
\]

让 future response 一同转移。

只有完整通过这一链，才能登记：

```text
MFS0_PHYSICAL_MEMORY_SUBSTRATE_CANDIDATE_QUALIFIED
```

---

# 33. 本轮最重要的研究边界

MFS0-E0 只研究：

\[
\boxed{
\text{history-dependent persistent physical deposition}
}
\]

不研究：

\[
\text{memory consolidation}
\]

不研究：

\[
\text{semantic learning}
\]

不研究：

\[
\text{meaning update}
\]

不研究：

\[
Xin.
\]

如果本轮 A 成立，下一轮才有资格问：

\[
z_0
\xrightarrow{W_1,\Delta_1}
z_1
\xrightarrow{W_2,\Delta_2}
z_2
\]

是否可以成立。

即：

> 已沉积的结构状态能否再次被新的历史—反馈组合修改。

到了那里，项目才第一次真正进入“practice / feedback / repeated memory modification”的研究阶段。

---

# 34. 开工状态

正式登记：

```text
MFS0-E0_PLAN_STATUS
= ADOPTED_WITH_AMENDMENTS

E-1..E-12
= INCORPORATED

R-1
= H_TAU_LATER_MODULATION

R-2
= DUAL_FET_TIER2

R-3
= POSTERIOR0_TERMINAL_C_FROZEN

R-4
= INITIAL_W_0_90

TAU_E
= 0.300 s

ELIGIBILITY_AMPLITUDE_ENCODING
= NO

ELIGIBILITY_CANONICAL_STATE
= CAPACITOR_VOLTAGE

MFS0_W_METABOLIC_LEAK
= OFF

NEW_G0_TRAJECTORIES
= 0

PRODUCTION
= READ_ONLY

READY_TO_EXECUTE_STEP_0
= YES

READY_FOR_MAIN_FACTORIAL
= NO
```

只有 Step 0 / A / B / C 依次通过后，才允许：

```text
READY_FOR_MAIN_FACTORIAL = YES
```