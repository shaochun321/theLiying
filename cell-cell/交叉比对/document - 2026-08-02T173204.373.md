这版可以通过：

[
\boxed{\text{P2-C1F-a/b/c完成，共同源型R2已经真实产生。}}
]

最有价值的新发现是存在两个不同时间尺度：

[
t_{\mathrm{R2检测}}=564,
\qquad
t_{\mathrm{父R1闭合}}=1289,\ 1559.
]

也就是说：

> R2物理关系可以先发生，父R1的正式身份记录稍后才完成。

现在采用“上升沿时保留候选时刻，等待父R1闭合后再核验谱系”的机制是正确的，没有把记录延迟误认为物理发生延迟。

## 下一步唯一阻塞

不能立即照搬X2c做 `K_R2`，因为当前文件只描述了：

```text
T1活动 → R2 trace ┐
                  ├→ R2 collector
T2活动 → R2 trace ┘
```

尚未描述：

[
R2\ collector
\xrightarrow{\ell_{\mathrm{out}}^{R2}}
q_{R2}.
]

如果直接把 `r2_fork_collector.pre_trace` 当作 (Y_{R2})，会形成循环证明：

> AND门被两路输入触发，所以证明AND门需要两路输入。

因此P2-C1F-d之前必须先确定一个最小R2下游输出：

[
\boxed{
Y_{R2}(s)
=========

I_{R2\rightarrow q}^{\mathrm{local}}(s)
}
]

然后再测试：

[
K_{R2}
======

\left|
Y_{T1\land T2}
--------------

## Y_{T1}

Y_{T2}
+
Y_{\varnothing}
\right|_2,
]

并分别切断两条父R1到R2的输入链路和R2输出链路。

当前冻结状态应写成：

[
\boxed{
\begin{aligned}
P2\text{-C1F-a/b/c}&:\text{通过};\
P2\text{-C1F-d0}&:\text{定义R2输出链路与局部读出Y};\
P2\text{-C1F-d}&:\text{执行双父依赖和输出切断测试}.
\end{aligned}
}
]

其余不需要重开。
