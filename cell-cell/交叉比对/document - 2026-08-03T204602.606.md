这份 **TSS-2b可以通过并冻结**。

它已经真正满足了尺度原型的关键条件：

[
\boxed{
\text{同一次外部发生}
\rightarrow
\text{同一内部生成活动云}
\rightarrow
\text{两个不同物理支撑窗口}
\rightarrow
\text{两个独立下游输出}.
}
]

而且切断广域独有的七条支撑链路后：

[
Y_{\mathrm{local}}=2.3280
]

完全不变，而：

[
Y_{\mathrm{broad}}:3.4876\rightarrow2.4918.
]

因此两个输出的差异确实依赖不同的物理支撑集合，而不是测试代码中的求和、插值或标签判断。

## 需要固定的资格名称

建议正式冻结为：

[
\boxed{\Lambda_{\mathrm{support}}^{(1)}：
\text{物理支撑范围尺度生成算子原型}}
]

它已经证明：

> 同一生成活动云在两个嵌套的物理支撑结构中，可以产生不同、可切断验证的下游作用。

但暂时不要宣称：

* 系统自行发现了局部与广域尺度；
* 已经实现任意尺度转换；
* 已经实现粗粒化或重整化；
* 不同尺度之间已经能够互相转换；
* 当前两个尺度窗口适用于其他模态和环境。

---

# 一个很小的命名修正

运行时不应继续把：

```text
radius=2.0
radius=3.0
```

当成尺度算子的身份。

这两个半径只用于TSS-2a的资产发现和标定。TSS-2b真正的算子结构是：

[
N_{\mathrm{local}}\subset N_{\mathrm{broad}}.
]

所以建议在代码和文档中明确：

```text
radius_2 / radius_3
    = calibration provenance

N_LOCAL / N_BROAD
    = frozen physical support sets
```

换句话说：

[
\boxed{\text{外部半径发现尺度资产，内部支撑集合实现尺度算子。}}
]

只要目前代码运行时确实不再读取HeatSource半径和坐标来决定成员资格，就不需要重跑实验。

另外，全局 `random.seed()` 能消除对照实验噪声差异，但以后最好保存并恢复随机状态，或使用局部RNG，避免测试之间互相污染。这不是当前阻塞。

---

# 当前主线已经完整到哪里？

现在已有：

[
\omega[W]
\xrightarrow{\mathcal M}
\mathfrak C_\omega[W]
]

以及三个基础结构算子原型：

[
\Theta_{\prec}^{(1)}
]

时间先后；

[
\Sigma_{\mathrm{support}}^{(1)}
]

物理支撑局部性；

[
\Lambda_{\mathrm{support}}^{(1)}
]

嵌套支撑范围尺度。

因此主线已经到达：

[
\boxed{
\text{基础生成元}
\rightarrow
\text{外部过程—内部活动云映射}
\rightarrow
\Theta/\Sigma/\Lambda
\rightarrow
\text{耦合生成元入口}.
}
]

现在不应再补尺度实验，也不需要返回竞争支线。

# 下一步：CG-0，第一条方向耦合生成元

方向不应重新造一个“方向collector”后直接贴名字，而应由现有算子真实耦合：

[
\boxed{
\mathcal C_{\mathrm{dir}}^{(1)}
===============================

\mathcal K_{\mathrm{dir}}
\left(
\Theta_{\prec}^{(1)},
\Sigma_{\mathrm{support}}^{(1)};
\Lambda_{\mathrm{local}}^{(1)}
\right).
}
]

以当前资产为例：

[
28\prec31
]

提供时间有序性；

[
31\in
\mathfrak C_{\omega_{28}}^{\mathrm{local}}
]

提供源发生过程下的局部物理支撑关系；

[
\Lambda_{\mathrm{local}}^{(1)}
]

明确该关系在哪一个支撑尺度中成立。

于是第一条方向候选可以写成：

[
\boxed{
\mathcal C_{\mathrm{dir}}^{(1)}
:
\mathcal G_{28}
\longrightarrow
\mathcal G_{31}
\quad
\text{at }\lambda_{\mathrm{local}}.
}
]

这里的箭头不是外部欧氏向量，而是由：

[
\text{同一发生谱系}
+
\text{时间顺序}
+
\text{空间支撑关系}
+
\text{明确尺度}
]

共同构造出的项目内部有向算子。

## CG-0最低资格

下一轮只要求：

1. 输入必须来自真实的 (\Theta,\Sigma,\Lambda) 活动，不读取投影名称；
2. 三者必须回指同一次外部发生过程或明确可接续的父谱系；
3. STDP、DA关闭后仍能产生；
4. 有独立生成链和独立下游输出；
5. 切断时间输入或空间输入，方向输出消失；
6. 改变尺度窗口时，方向输出允许改变；
7. 交换两端时不能仍产生完全相同的有向结果；
8. 不读取欧氏坐标、`direction`字符串或站点语义名称。

暂时不做梯度、散度，也不做学习。

因此下一任务应直接定为：

[
\boxed{\text{CG-0a：方向耦合生成元的类型契约与现有资产映射。}}
]

先确认当前 (\Theta_{\prec})、(\Sigma_{\mathrm{support}})、(\Lambda_{\mathrm{support}}) 能否在谱系和时间区间上真正耦合，再一次性实现和验证方向算子。
