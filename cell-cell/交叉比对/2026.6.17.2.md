Now I understand the full picture. This document is the canonical reference for "母本分化" (Mother Differentiation) and "新构建提议机制" (New Component Construction Proposal Mechanism).

Key things to note for CLAUDE.md:

1. **母本分化** is the principle of deriving specialized behavior from a unified mother class (Neuron/Bundle) through configuration differences rather than code branches. It has 3 levels:
    
    - Level 0: 母本 (Neuron/Bundle/HebbianCircuit)
    - Level 1: 工厂分化 (Factory Pattern — config level)
    - Level 2: 元件分化 (Compensation — component level: CRI/DN/D2R/etc.)
    - Level 3: 运行时分化 (Homeostatic — physics-driven)
2. **新元件构建原则**: When a new differentiated component needs to be created, it should go in `components/compensation.py` at Level 2, map to a real biological or physical object, use existing primitives like Capacitor/MOSFET/Memristor/PowerRail, and be activated through config flags rather than code branches.
    
3. The document also reveals an important lesson from the 浅拷贝 bug fix history — I need to use deepcopy for split/sprout operations to avoid unintended mutations.
    
4. A key constraint that keeps coming up: don't modify the mother class code to add functionality, instead use inheritance and adapters to override behavior.
    
5. The real insight here is that component allocation shouldn't be based on crude "shadow vs main layer" distinctions, but rather on the functional requirements of each connection in the signal chain — this requires a more granular signal path analysis to guide where components should be placed.
    

Now I'm thinking through how to structure these concepts in CLAUDE.md, particularly explaining the 3-level hierarchy and the core principle that the same mother class can exhibit different behaviors through differentiation. I'm seeing that the user wants clearer guidance on two interconnected concepts: how the three-level hierarchy works for differentiating the base model, and the specific workflow for proposing and implementing new components—which involves identifying signal-path needs, mapping them to biological objects, creating classes in compensation.py, adding config flags, and wiring everything through the appropriate factory functions.

I also notice there's a parameter discrepancy between what I implemented and what this document specifies: the document shows XIN_GAIN should be 0.1 and vr_base_rate should be 0.1, but I used 1.0 and 0.05 based on the earlier unified plan document. document might have been created after the work reports, which would explain the discrepancy. I need to figure out which version is actually correct—whether these are intentional revisions or if there's a conflict between the two documents that I need to resolve.

For now, I should focus on what the user asked: adding the 母本分化 framework and new compensation mechanism to CLAUDE.md. I'll need to document the three-tier architecture and the construction process for the new compensation components. I think the 母本分化 document was likely a planning draft written before the actual implementation, so those parameter values were estimates rather than the final ones used. The 大一统方案 is the authoritative reference, which explains why XIN_GAIN ended up at 1.0 instead of 0.1. I should focus on updating CLAUDE.md with the missing structural principles and note this discrepancy for the user.

文档读完了。在写入 CLAUDE.md 前，我发现文档 §五 记录的参数值与我的实际实现有差异，需要先确认：

|参数|本文档（§五）|大一统方案（§5.1，我执行的依据）|我的实际实现|
|---|---|---|---|
|`XIN_GAIN`|3.0 → **0.1**|3.0 → **1.0**|**1.0**|
|`vr_base_rate`|0.01 → **0.1**|0.01 → **0.05**|**0.05**|

本文档与大一统方案数值不同。我按大一统方案执行的，请确认哪个版本是权威值。

---

现在关于 CLAUDE.md 的写入内容，我已理解两个概念：

**母本分化（Mother Differentiation）**：从统一母本类通过配置差异（不是代码分支）产生特化行为，分三层——Level 1（工厂函数，_column_config 等）、Level 2（补偿组件，CRI/DN/D2R）、Level 3（运行时自适应）。

**新构建提议机制**：在 Level 2 创建新补偿组件时，不能只因为"某层需要"就加，必须基于**信号路径功能需求**分析（战役二的教训：CRI 分给了影子层但遗漏了主层 Col，因为当时按"影子 vs 主层"分组而非按"Col→Motor 这条路径需要什么"分析）。

关于写入 CLAUDE.md 内容，我的建议：在现有"⛔ 写代码前的强制三问"之后，添加一个**母本分化架构说明**节。但这里有一个设计判断需要你参与：

**现有 CLAUDE.md 已有**的内容：

- "Don't modify mother code to add features. New components go in separate files, integrated via inheritance/adapter"
- "⛔ 强制三问"（Q1 BIO，Q2 物理结构，Q3 参数依据）

**可能需要补充的**：

1. 三层分化层次的显式说明（让未来操作新组件时有明确的放置位置参考）
2. "信号路径分析"原则（组件分配依据功能需求，不依据层组归属）

确认参数差异后我再写入。你想要我现在先写 CLAUDE.md，还是等参数问题确认后一起处理？