# W1 方案评判分析（MAINLINE V2 — World v2 构建）

日期：2026-09-18
评判对象：《MAINLINE V2 — W1：条件物理 World v2 与有限观察环境池构建方案》
（65 节，`cell-cell/交叉比对/`）
评判依据：T1-A 收口（563ac0b）+ 本轮读码核查。

---

## 一、总评：ADOPT with amendments B1-B6，实际成本远低于表面体量

方向与冻结序完全一致（WT0/T1-A CLOSED → W1 只建 𝒲+ℬ → T1-B → G0 回接
→ SCALE-0 → TSS 回接）；G0 两债务缓办（§53）与 T1-A 登记吻合。防共适应
纪律完整：§61 四禁 / §34 无最佳点 / §35 极端边界 / §58 负结果同存 /
§36 "World validity ≠ internal detectability" / §14 "删除即改变未来，
否则只是 audit metadata"（对软件秘密变量的精确防线）。

## 二、代码事实核查——复用面好于方案自身预期

- §7 六组件**五个存在**：`DynamicHeatSource`/`ThermalFieldLocator`
  （`thermal_source_coupling.py`）、`ThermalFieldGraph`
  （`dynamic_thermal_field.py:227`）、`ThermalContact`
  （`skin_thermal_contact.py`）、`JointThermalStepPlan`。
  **`ThermalSourceCoupler` 类名不存在**——耦合=模块级
  Source.release + JointThermalStepPlan 编排（名称更正，非功能缺失）。
- **M1（物理闭合）大半在产**：`ThermalFieldGraph.total_energy()` /
  `closure_residual(node_id)` / `_total_injected` / `_total_leaked_ambient`。
- **M3（独立时间尺度）零代码可达**：`r_leak_ambient` 是自由构造参数，
  10/κ 绑定只是调用点惯例——解绑=声明合法参数域。
- **§27/§28（Source-hidden twin/因果阻断）物理基础在产**：
  `DynamicHeatSource` 有 `energy_remaining/power/有限预算/耗尽`，
  `preview_release/release` 分离。
- `ThermalFieldGraph` 通用 cells+links 容器（任意 N/拓扑，8-step 快照
  调度防边序偏差）——M2 的 N 扫描只需 research 层建链工厂。

⇒ 真实工作量 = episode 采样层包装（Spec/Sampler/Episode/BoundaryView/
Ledger）+ 四组资格实验 + 文档。

## 三、修正建议（B1-B6，无阻塞性裁定项）

| # | 修正 |
|---|---|
| B1 | 交付合并：9 脚本→4（structure_scan / hidden_twins / physics_audit / final_qualification）；6 文档→4（沿 WT0 六并三、T1-A 八并四先例） |
| B2 | §26 阈值预登记落地（方案要求冻结但未给数）：ε_B=‖ΔY(t0)‖/RMS(Y)<1e-2；ε_X=‖ΔX(t0)‖/RMS(X)>0.2；ε_F=max future div/RMS(Y)>5e-2（v1 正对照实测 ~1e-5/~1.5/~0.5，余量充足）；写死 docstring 后不得改 |
| B3 | LAYER_CATEGORY_CONFLICT 时点折中：文字归类裁定在 T1-B 开工时，代码修复在 G0 回接（调和 T1-A"T1-B 前"与外部 §53"G0 回接"） |
| B4 | §32 显式欧拉扩散稳定条件在脚本显式实现，区分物理/数值失稳 |
| B5 | 采样器用 `random.Random(seed)` 独立实例，不碰全局 RNG（保护 WT0/T1A 逐位冻结） |
| B6 | manifest 全存；轨迹只存资格实验代表 episode（其余 seed+spec 可再生，符合 §57 self-contained） |

## 四、结论

ADOPT with amendments B1-B6；执行 W1 全轮（六门 + NC1-5 + E1-5 +
极端案例 + 终裁十二问），production 零改动，终态三选一如实给。
