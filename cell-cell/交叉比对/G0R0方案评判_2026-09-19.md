# G0-R0 方案评判分析（MAINLINE V2 — 统一物理时间与多率回接）

日期：2026-09-19
评判对象：《MAINLINE V2 — G0-R0：统一物理时间与多率回接方案》（45 节）
评判依据：T1-B 收口（c484be0）+ 本轮读码预核查。

---

## 一、总评：ADOPT with amendments D1-D6——含一项对我方的勘误（D2）

**D2 勘误（本评判最重要）**：§44"不许用 generator dt is internal 一句话
绕过"直指我方 T1-B 合同 C2 断言（"dt=0.001=内部积分粒度非物理秒"）。
按 §14 做代码预核查，**该断言错误，状态 A（dt=物理积分步长[s]）成立**：
- `Capacitor.inject`：ΔQ=I·dt（semiconductor.py:64-72，剂量物理缩放）
- `Capacitor.leak`：exp(-dt/RC)（:74-86）
- `Neuron` 基类 traces：exp(-dt/τ) 且 trace_tau_pre×0.001 **显式 ms→s
  换算**（neuron.py:587-588）；适应 exp(-dt/tau_w)（:527）
核心物理层全按物理秒消费 dt。MAINLINE_TIMEBASE_CONTRACT 须按 §15 修订
为"单一物理时间轴+多率积分"；T1-B 合同该句登记勘误。外部"合同文字不能
替代代码证据"（§14）的坚持完全正确。

**推论**：1 World 秒=1000 G0 毫秒步；历史 1:1 步配对（exp_P2A1b_3）=
生成元以 1/1000 物理速率运行——§40 预期负结果。冻结结果均为 step 语义
内部自洽，登记不重审。

## 二、预核查例外清单（D3，审计脚本实测确认）

- `ThermalDeltaNeuron._TRACE_DECAY=0.99` **每步衰减不含 dt** → L1 trace
  τ 随 dt_G 漂移 → §17 G0_DT_COUPLED_PHYSICS_FAIL 定点候选；
- OccurrenceClosure rearm=500 与 t_up/t_down/t_rearm 均 **step 计数**
  （DISCRETE_COUNTER by design）→ 需 §22 n→t_phys 映射非改语义
  （§20 本轮禁调阈值 ✓）；
- DelayedBundle 延迟单位（step vs ms）待审计。

## 三、方法学评判

正确设计：§8 禁 occurrence 选插值法；§12 比 G0 **内部状态**非只看输出；
§13 tolerance 预登记（bit-exact 不强求）；§30 ZOH 剂量攻击（预核查
inject ΔQ=I·dt ⇒ 预判无 1000× 复制，实测确认）；§24 无 GlobalClock；
§32 严禁同时改 L1（误差源隔离）；§41 压缩包纪律针对 07-20
transducer_neurons 断链旧伤。

## 四、修正建议（D1-D6）

| # | 修正 |
|---|---|
| D1 | 脚本 7→5（audit / scheduler+参照+收敛 / invariance+delay/RC+剂量 / replay+twin+bridge / final） |
| D2 | 如上勘误：终裁按代码证据（预判状态 A+定点例外）；T1-B 合同措辞修订 |
| D3 | 预核查例外清单先登记，脚本实测确认 |
| D4 | tolerance 预登记：E_state 细化一档误差比<0.7 且最细档相对误差<5% |
| D5 | 压缩包=git 提交完整性核查（ls-files 对照报告引用），仓库即包 |
| D6 | G0 参与实验 T_phys=60 s 恒定窗（1ms 档 6 万子步）；World-only 长窗 |

## 五、结论

ADOPT with amendments D1-D6，执行 G0-R0 全轮；终态四选一如实给
（预判"状态 A + 定点例外登记"，若例外构成全局冲突则如实给 B/D）。
