我看完了。这个文件不是新的工程报告，而是我上一轮对 X2d 提出的扩展意见。重新审视后，我认为它**过度扩张了当前阶段**，不应该全部执行。

考虑到你的 token 和算力成本，现阶段应立即收缩范围：

[
\boxed{\text{不要再次修改X2d，直接准备执行X2c。}}
]

## 为什么可以直接继续

当前 X2d 已经具备 X2c 真正需要的最低条件：

* 有明确的 (I_{R1})；
* 有独立的统一测量窗口 (W_Y^\star)；
* 父 epoch 改变会终止旧区间；
* trace 和 collector 回归基线后可以关闭；
* 所有反事实条件可以使用同一个 (W_Y^\star)。

这些已经足够计算：

[
K_{R1},\qquad K_{\mathrm{gen}},\qquad K_{\mathrm{out}}.
]

此前建议的：

* `RELAXING → SUPPORTED`恢复；
* 多组迟滞计数器；
* `s_candidate`；
* 新增 `transmission_enabled`；
* 更复杂的统计基线窗口；

都属于**增强项**，不是当前 X2c 的阻塞项。

尤其是 `RELAXING → SUPPORTED`：目前没有实际测试证明现有物理轨迹真的出现“退出后重新取得同一关系支撑”。在没有失败证据前增加这一机制，反而会继续扩大状态机，并增加新的语义争论。

## 现在只保留一个执行前检查

切断实验必须确认：

[
\boxed{
\max_s |I_{\mathrm{cut}}(s)|
\leq \varepsilon_{\mathrm{num}}.
}
]

也就是说，用 `weight_max=0` 是否足够，不需要先争论。直接写一个小测试：

1. 激活相同输入；
2. 正常 bundle 产生非零电流；
3. 设置切断；
4. 确认该 bundle 输出电流为零；
5. 确认其他结构和初始状态不变。

如果通过，就继续使用现有切断方法，不新增物理门。

---

# 最省成本的下一步

只执行一个阶段：

[
\boxed{\text{P2-B1X2c：局部电流不可约与双链路切断测试。}}
]

本轮不要再做：

* R1理论扩展；
* 空间／尺度投影；
* 状态机增强；
* ledger验证；
* 新神经结构；
* 参数搜索。

测试只输出：

```text
K_R1
K_gen
K_out
ε_int / ε_gen / ε_out
PASS 或 FAIL
```

如果失败，再根据失败位置修一处：

* (K_{R1}) 失败：组合没有可测局部作用；
* (K_{\mathrm{gen}}) 失败：作用不依赖关系生成链；
* (K_{\mathrm{out}}) 失败：切断方法或输出归因有问题。

不要提前修还没有出现的问题。

## 后续交流也应改成低消耗模式

之后每份报告我只给：

1. **通过／不通过；**
2. **唯一阻塞项；**
3. **下一步一项任务。**

不再为非阻塞可能性连续追加架构改动。

当前裁定就是：

[
\boxed{
X2a'/b/d冻结，不再重开；
下一步直接执行X2c。
}
]
