# T1-A 执行方案评判分析

日期：2026-09-18
评判对象：《T1-A：Transduction–L1 职责分离与 v2 结构合同 — Agent 执行方案》
（`cell-cell/交叉比对/`）
评判依据：WT0 实测（f8a14da，三文档）+ 本轮读码预核查。

---

## 一、总评：高质量，ADOPT with amendments A1-A5

WT0 冻结事实继承零失真（终裁引用准确；L1~L4=T0 四负结果、L5~L8=WT0 新增
逐条核对无误）。三处设计尤其正确：
- **§32 红线**："旧转导不好"≠"需要更复杂的转导"，证据优先支持"v2 更薄"
  ——与 T0/WT0 实测方向一致（legacy 病根是偷偷做太多，非做太少）。
- **§27C + §22**：允许"问题在 G0/L1 接口"的如实结论出口
  （G0_PORT_CONTRACT_CHANGE_REQUIRED，本轮不改代码），避免 §23 反模式
  （把接口错误硬修到转导层）。
- **§25/§14/§17/§24**：禁综合评分/禁 occurrence 排序/不预设最佳保留率/
  双对照（legacy 负对照+明确标注的数学对照）——防 Goodhart 全套到位。

## 二、代码预核查（评判增量：三个决策门已可预判）

1. **Gate C 预判 PARTIAL**：`ThermalDeltaNeuron.step()`
   （`nexus_v1/somatosensory/transducer_neurons.py:305-323`）＝方案 §8.5
   所怀疑的瞬时半波——`activation = clip(max(0, dT_raw×200), 0, 10)`，
   **无内部速率提取状态**（pre_trace/_activation_ema 是输出侧痕迹非微分）。
   不实现 dT/dt，只对外部喂入的 dT 做整流+增益。BIO 声称（Type II AMH
   dT/dt 检测器）与 runtime 行为的落差即 §8 三层剥离的预期结论。
2. **职责矩阵"时间微分"格的生产答案：既不在 D_i 也不在 L1，在
   Boundary/contact 层**：生产路径 `_step_quantum_thermal_pathways`
   （`nexus_v1/circuit/variant_adapter.py:1910` 起）`_T, dT =
   patch.sample(world, body, dt)`——速率提取由 SkinPatch 采样（带内部
   状态的物理差分载体）完成，warm 支路 +dT / cool 支路 −dT。
   ⇒ Candidate B 的"𝒫_Ṫ 由谁承担"在项目结构里**已有物理载体先例**。
3. **PORT_SEMANTIC_MISMATCH 必须带支路范围**：生产量子元支路
   （SkinPatch→dT→L1）语义自洽；错位只在 **TSS WORLD_COUPLED 支路**
   （`feed_from_skin` 把幅值映射 u 塞进 dT_raw 口）。全局判定会冤枉
   生产路径；Gate E 更可能落在"端口改名/双语义声明"而非全链路重构。

## 三、可行性核查

- R1-R8 全部可构造：R4（同峰值不同上升速率）用注入整形；R5 复用已冻结
  正对照；R6/R7 与剂量攻击同型。§29 冻结回归全部在仓可复跑。
- Q1 文献卡有项目内锚点（ThermalDeltaNeuron REF: LaMotte & Campbell
  1978；Norris et al. 2021）。

## 四、修正建议（执行时采纳，无阻塞性裁定项）

| # | 修正 |
|---|---|
| A1 | 交付物 8 文档 → 并为 4（审计三合一 / Q1 卡 / 候选族+度量合同 / 诊断+终裁），沿 WT0 六并三先例 |
| A2 | R1-R8 每场景显式声明 Y_B 配置（BOUNDARY_FULL_P2A / BOUNDARY_REDUCED_N0）——R-3 纪律延伸 |
| A3 | §1 允许修改 `research/transduction_requalification/` 有污染 T0 冻结证据风险 → 改只读；新内容全进 `research/transduction_v2/` |
| A4 | Q1 卡以已有 REF+教科书共识为锚，新增引用标注可信度 |
| A5 | 量纲审计允许诚实登记"约定单位"+S 缩放映射，不硬造单位（§4 已预留出口） |

## 五、结论

ADOPT with amendments A1-A5；按修正后方案执行 T1-A 全轮。
