先提交当前修正，**不要与第3步混在同一个commit**。

[
\boxed{\text{这次不是普通参数修补，而是撤销了一项过强资格。}}
]

建议提交名：

```text
TSS-2b修正：Λ资格降格与physical_seed解耦
```

修正后的关键结论很清楚：

[
Y_{\mathrm{broad}}^{\mathrm{cut}}
=================================

# Y_{\mathrm{local}}

2.328029.
]

这说明原来的广域输出只是：

[
\text{局部支撑贡献}
+
\text{广域独有站点贡献},
]

切掉 (N_\Delta) 后严格退化为局部输出。因此目前只有：

[
\boxed{\Lambda_{\mathrm{nested}}^{(1)}
\text{：嵌套支撑集合的结构读出}}
]

还没有“尺度专属转换”，撤下 (\Lambda) 基础生成算子资格是正确的。

## 第3步可以继续，但先做一次极小审计

不要直接写 `28≺12` 电路。下一任务建议定为：

[
\boxed{\text{CG-0a：广域独有目标 }12\text{ 的时间关系资产审计}}
]

只回答三问。

### Q1：生物／物理对应物

不是“站点12代表某种语义方向”，而是：

> 同一外部发生过程在较广物理支撑范围内，使源生成元28先取得活动，随后广域独有生成元12取得活动；trace—collector结构检测这两个基础生成元活动之间的有序耦合。

因此候选算子是：

[
\Theta_{28,12}^{(1)}:
\left(
\mathcal G_{28},
\mathcal G_{12}
\right)
\rightharpoonup
Y_{28\prec12}.
]

它对应的是**异位置基础发生之间的延迟汇聚／先后检测结构**，不是学习出来的关系，也不依赖STDP或DA。

### Q2：为什么必须是12

因为：

[
12\in N_\Delta
==============

N_{\mathrm{broad}}\setminus N_{\mathrm{local}}.
]

它能提供当前缺少的实验区分：

* 局部支撑窗口不包含目标12；
* 广域支撑窗口包含目标12；
* 同一时间关系是否能在不同支撑范围下获得或失去资格。

这比随便选一个已有近邻更有判别力。

### Q3：参数能否直接复用T1

结论是：

[
\boxed{\text{可以作为首轮结构先验复用，但不能直接宣称已经标定。}}
]

允许复用的前提：

* 站点12使用相同的 (\kappa^{10}) 架构；
* 相同模态和相同神经元／collector类型；
* 输入、trace和阈值量纲一致；
* 不新增学习机制。

可先复用：

[
\tau_{\mathrm{fast}},
\quad
\tau_{\mathrm{slow}},
\quad
w_{\mathrm{trace}},
\quad
\theta_{\mathrm{collector}}
]

作为**来源明确的冻结初值**。

但必须先实测：

[
t_{28},
\qquad
t_{12},
\qquad
\Delta t_{28,12}=t_{12}-t_{28}.
]

然后判断T1现有的trace窗口是否覆盖：

[
0<\Delta t_{28,12}<W_{\Theta,\mathrm{T1}}.
]

* 若覆盖：直接复用，不重新标定；
* 若不覆盖：先说明物理时间差超出T1适用域，再按推理调整窗口；
* 不做网格搜索。

## 下一任务的停止条件

审计只需得出三种结果之一：

1. **可直接复用**：进入 `28≺12` 最小电路；
2. **需要一次有根据的时间窗调整**：给出参数来源后再实现；
3. **站点12没有稳定的后发活动**：停止，不强造关系。

所以现在的顺序是：

[
\boxed{
\text{先提交资格降格修正}
\rightarrow
\text{审计 }28\prec12
\rightarrow
\text{再决定是否建新collector和bundle}.
}
]

不要现在恢复 (\Lambda) 名称，也不要开始完整CG-0。
