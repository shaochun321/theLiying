这份可以通过。

[
\boxed{\text{P2-C1F-a/b/c/d0/d 全线完成，可以冻结。}}
]

它证明了：

[
(28\prec31)\land(28\prec23)
]

这两条真实R1活动能够经过：

[
R1_{T1},R1_{T2}
\rightarrow R2\text{ trace}
\rightarrow R2\text{ collector}
\rightarrow \ell_{\mathrm{out}}^{R2}
\rightarrow Y_{R2}
]

产生非零、双父输入依赖、输出链路依赖的局部电流。

## 这次实验设计是干净的

四组实验使用同一次真实物理驱动，只切换R1活动能否传入R2：

* T1、T2自身的物理活动保持一致；
* 外部环境和父R1过程保持一致；
* 唯一变量是R2输入链路；
* 输出切断使用已验证有效的 `synapse_gain=0`。

因此它比重新构造四套不同的外部输入更适合验证R2层的因果必要性。

## 唯一需要修正的命名

当前所谓：

[
\varnothing
]

并不是真正的“无外部输入”或“系统空条件”。

这个条件下，T1和T2仍然在真实活动，只是两条R2输入链路都被切断。因此更准确的名称应是：

[
\boxed{\varnothing_{R2\text{-input}}}
]

或代码中写成：

```text
R2_INPUT_NULL
```

否则以后容易误读为“整个系统没有发生任何活动”。

四条件准确表达应为：

[
\varnothing_{R2},
\quad
T1\rightarrow R2,
\quad
T2\rightarrow R2,
\quad
(T1,T2)\rightarrow R2.
]

这只是文档和测试命名修正，不需要重跑实验。

## 资格边界仍然正确

由于R2 collector也是AND结构：

[
Y_{T1}=Y_{T2}=Y_{\varnothing_{R2}}=0,
]

所以：

[
K_{R2}
======

# K_{\mathrm{out},R2}

# |Y_{T1\land T2}|_2

0.1456.
]

因此目前证明的是：

[
\boxed{\text{共同源双R1合取产生了真实、链路依赖的R2局部输出。}}
]

没有证明：

* R2超越AND合取；
* 已经形成散度；
* 已形成通用共同源代数；
* 高阶关系由系统自行发现；
* R2具有超过人工选定拓扑的涌现能力。

报告已经主动限制了这些声明，所以没有过度解释。

## 当前项目位置

现在完整链路已经走到：

```text
真实外部作用
   ↓
κ¹⁰基础发生
   ↓
D1 Occurrence
   ↓
R1时间关系
   ↓
RelationOccurrence
   ↓
两条共同源R1真实活动
   ↓
共同源分叉型R2
   ↓
R2Occurrence
   ↓
R2局部输出电流
```

对应资格为：

[
\boxed{
D1\text{发生资格}
\rightarrow
R1\text{关系及输出资格}
\rightarrow
R2^{\mathrm{fork}}\text{关系及输出资格}.
}
]

这意味着项目已经第一次完整实现：

> 物理发生能够生成关系，关系活动又能够生成更深一层关系，并对下游产生实际作用。

## 下一步不建议马上进入R2–R2

考虑到成本和底层审查压力，现在最合理的是暂停扩层，先冻结一份很短的阶段总表：

| 层  | 已证明          | 尚未证明         |
| -- | ------------ | ------------ |
| D1 | 真实输入产生唯一发生实例 | 通用输入类型       |
| R1 | 时间合取关系及局部输出  | 空间、尺度、强不可约性  |
| R2 | 共同源分叉及局部输出   | 传递型、非AND高阶结构 |
| D3 | 未开始          | 事件核全部留白      |

当前没有需要返工的阻塞。只需把“空条件”改名为“R2输入空条件”，然后冻结P2-C1F。
