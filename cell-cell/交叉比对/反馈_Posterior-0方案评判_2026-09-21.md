# 对《Posterior Construction-0 方案评判分析》的反馈与执行裁定

日期：2026-09-21

评判对象：

《Posterior Construction-0 方案评判分析》

当前结论：

```text
ADOPT WITH AMENDMENTS
E-1 ~ E-7 = ACCEPTED
R-1 ~ R-3 = RULED
READY_TO_EXECUTE = YES
```

---

# 一、总体反馈

接受评判的总体判断。

Posterior-0 原方案与 D2-1 冻结状态衔接无实质失真，尤其以下基础前提已经被正确核实：

```text
D2_1_RECURSIVE_GENERATION_QUALIFIED
G1_QUALIFIED
READY_FOR_POSTERIOR_0
```

以及：

```text
G0 / D2-0 / D2-1 = READ_ONLY
g_rel2 / theta_2 / rearm_2 = READ_ONLY
REPLAY_REFERENCE qualification remains in force
```

评判对这些前置状态的逐项核实接受。

原方案主体不重写，不启动 Posterior-0 v2。

本反馈只用于修订执行边界、预注册值和裁定项。

---

# 二、E-1：posterior 时点可达性

## 裁定

**接受 E-1。**

原方案：

\[
\delta t
=
\{1,2,4\}\tau_{\rm residual}
\]

不得作为固定三点要求直接执行。

评判已经证明现有 site23 occurrence 最晚窗口早于 rc_main 的：

\[
t_{\rm rearm}=5387,
\]

而 NF-1 又使晚时刻 G0 occurrence 出现物理支撑失效，因此 far 点不是一个当前已证明可达的实验条件。

因此修订为：

\[
\boxed{
\text{先测物理可达性，再决定 posterior timing}
}
\]

---

# 三、R-1 裁定：采用 (a)，(c) 条件后备，拒绝 (b)

正式裁定：

```text
R-1(a) = PRIMARY
R-1(c) = CONDITIONAL FALLBACK
R-1(b) = REJECTED
```

## 主路径：R-1(a)

先用最多：

\[
2
\]

条新 site23 G0 trajectory 探测晚时点可达边界。

只要求先建立：

```text
NEAR = reachable
```

若数据允许，再增加：

```text
MIDDLE = conditional
```

不强求 FAR。

FAR 若不可达，登记：

```text
LIM-POSTERIOR-TIMING
```

它不是实验失败。

---

## 条件后备：R-1(c)

只有在：

```text
NEAR unreachable
```

且原因被确认是：

```text
physical-support window too short
```

时，才允许一次预注册的：

```text
support-window extension
```

即延长 pulse duration。

但必须满足：

1. 只改变 physical support duration；
2. 不改 G0 参数；
3. 不改 relation 参数；
4. 不改变 posterior 判据；
5. 新 duration 在第一次运行前冻结；
6. 与原 duration 保留 paired control；
7. 数据中显式标：

```text
R1C_FALLBACK
```

不允许继续迭代 duration 寻优。

---

## 拒绝 R-1(b)

不允许人为平移整个历史窗口。

理由不是数学上不能平移，而是当前项目已经把 occurrence 的**真实绝对物理时间位置**作为实验条件的一部分。

如果为了 Posterior 资格而把：

\[
W
\]

整体搬到更早时间，

会直接绕过 NF-1 暴露的真实物理限制。

因此：

```text
DECLARATIVE_TIME_SHIFT = NOT_ALLOWED
```

评判列出的三条路径及其代价接受上述裁定。

---

# 四、不新增第五终态

评判提出：

```text
POSTERIOR_TIMING_UNREACHABLE
```

作为可能的 B 变体。

本反馈裁定：

**不新增第五终态。**

保留原四终态：

```text
A POSTERIOR_STATE_REORGANIZATION_V0_QUALIFIED

B POSTERIOR_SUBSTRATE_ABSENT

C POSTERIOR_EFFECT_ADDITIVE_ONLY

D POSTERIOR_CAUSAL_REORGANIZATION_NOT_ESTABLISHED
```

如果：

1. R-1(a) near 不可达；
2. R-1(c) 一次预注册后备仍不可达；

则：

```text
terminal_state = D
reason_code = POSTERIOR_TIMING_UNREACHABLE
```

这样区分：

```text
没有历史 substrate
```

和：

```text
当前物理链无法把 posterior event 放进合法时窗
```

但不扩张终态体系。

---

# 五、E-2 / R-2：Δ 与 Q 通道

## 裁定

完全接受。

```text
DELTA_PRIMARY_CHANNEL = C
QUERY_CHANNEL = C
R_CHANNEL_POSTERIOR = SECONDARY_ONLY
```

原因：

R-only 已经可以单独越过：

\[
\theta_2,
\]

会直接形成新的 relation occurrence，严重混淆：

\[
\text{posterior reorganization}
\]

与：

\[
\text{parallel new state}.
\]

而 C-only 当前略低于阈值，是更干净的 posterior physical entry。

---

# 六、Δ 主实验合同

Posterior 主实验：

\[
W+\Delta_C.
\]

其中：

\[
\Delta_C
\]

必须：

1. 经过正常 C transduction；
2. 进入 `tin_c`；
3. 再经 `bundle_c`；
4. 最终影响 RelationCell；
5. 自身不直接触发新的 relation occurrence。

若某个 Δ 条件意外越阈产生新 occurrence：

```text
PARALLEL_NEW_STATE = TRUE
```

该条件不能直接用于 Posterior A 终态资格。

可以作为负结果保留。

---

# 七、Q probe 修订

接受评判关于阈值放大的警告。

不能只报告：

```text
occ_Q = 0 → 1
```

作为“最强证据”。

必须同时报告：

\[
M_Q
=
peak_Q-\theta_2
\]

即：

```text
probe_margin
```

以及连续量轨迹指标：

\[
RMSE_Q
\]

或等价预注册 trajectory metric。

因此所有 Q 结果至少同时包含：

```text
occurrence existence
probe margin
trajectory metric
```

防止：

\[
10^{-4}
\]

级模拟差经过阈值变成：

```text
0 vs 1
```

后被误写成巨大 posterior effect。

---

# 八、E-3：posterior path block 修订

完全接受。

禁止使用：

```text
remove Delta input completely
```

作为 posterior path block。

正式阻断改成：

```text
Delta → tin_c = ACTIVE
tin_c → bundle_c = BLOCKED
bundle_c → RelationCell = ZERO
```

也就是说：

\[
\Delta
\]

仍然真实存在并消耗输入端资源；

只切断：

\[
\Delta
\rightarrow
Z_W
\]

的传播。

---

# 九、Path-block 最低审计项

阻断臂必须同时确认：

```text
tin_c received Delta       = YES
tin_c state changed        = YES
tin_c energy/resource used = YES

bundle_c transmission      = ZERO
RelationCell Delta dose    = ZERO
```

否则不能叫 causal path block。

该修订接受评判 E-3。

---

# 十、E-4 / R-3：washout 与状态分量

完全接受。

正式预注册：

\[
\boxed{
T_{\rm washout}=1\times\tau_2
}
\]

当前：

\[
\tau_2\approx456\text{ steps}.
\]

因此：

```text
POSTERIOR_WASHOUT_STEPS = 456
```

第一轮固定。

不得看到数据后改 washout。

---

# 十一、Posterior state 分量预注册

状态分成三类。

## Z-primary

```text
RelationCell membrane state
```

这是 D2-1 已资格化的 G1 causal state carrier。

---

## Z-secondary

```text
RelationInputNeuron / tin state
```

允许记录；

不能单独支撑 Posterior qualification。

---

## Resource trace

```text
PowerRail / energy / dissipation history
```

单独报告为：

```text
RESOURCE_TRACE_ONLY
```

如果：

\[
Z_{\rm membrane}
\]

已经没有区别，

只有累计 energy 不同，

则：

```text
PERSISTENT_POSTERIOR_STATE = NO
```

不能靠 energy ledger 让 M3 PASS。

评判关于这一风险的分析接受。

---

# 十二、Step A 不再输出“有/无”二值

接受 E-5。

因为 rearm 并不会重置 RelationCell 的物理膜状态，所以：

\[
t_{\rm rearm}
\]

不等于：

\[
Z=0.
\]

Step A 必须输出：

\[
\boxed{
(T_{\rm residual},\tau_{\rm residual},T_{\rm boundary})
}
\]

具体要求：

```text
residual_start
tau_residual
last_step_above_epsilon
last_step_query_relevant
epsilon_num
```

---

# 十三、Posterior substrate 的正确问题

不再问：

> rearm 后状态是不是精确为零？

而问：

> rearm 后还剩下多大的 causal residual window，并且这个窗口是否足以容纳合法的 Δ？

因此：

```text
POSTERIOR_SUBSTRATE_ABSENT
```

只在：

\[
T_{\rm residual}
<
T_{\rm minimum\ reachable\ posterior}
\]

时成立。

不是要求：

\[
Z=0.
\]

---

# 十四、E-6：Q-scale 修订

接受。

删除“query scale calibration”措辞。

本轮不存在新的连续 Q amplitude 参数。

Q 只能通过：

```text
which frozen occurrence window
+
where that window is placed
```

进行选择。

也就是说：

\[
Q
\]

继续遵守当前 typed drive。

正式改成：

```text
QUERY_WINDOW_SELECTION
```

而不是：

```text
QUERY_AMPLITUDE_CALIBRATION
```

不得新引入：

\[
\alpha_Q\vartheta
\]

一类 amplitude multiplier。

该修订与现有 `build_phase_drive` 合同一致。

---

# 十五、Posterior Levels 改名

接受 E-7。

原：

```text
P0
P1
P2
```

全部改为：

```text
PL0 = Immediate Response
PL1 = Persistent State Change
PL2 = Prior-dependent Reorganization
```

避免与项目历史：

```text
P0/P1/P2 priorities
Phase 0...
```

冲突。

---

# 十六、Posterior interaction 主判据保持

核心：

\[
I_W
=
(Y_{11}-Y_{10})
-
(Y_{01}-Y_{00})
\]

保持不变。

但：

\[
Y
\]

不得使用单一 occurrence boolean。

必须至少同时计算两种表面：

### Continuous

例如：

\[
I_W^{traj}
\]

基于：

```text
trajectory RMSE / integral / local state difference
```

### Event

\[
I_W^{occ}
\]

基于：

```text
occurrence existence / boundaries
```

终态 A 至少需要：

```text
continuous interaction > numerical floor
```

occurrence 0/1 只能作为更强佐证。

---

# 十七、PL2 真正要求什么

PL2 不是：

\[
W+\Delta
\neq W.
\]

而是：

\[
\boxed{
\Delta\text{ 在有 }W\text{ 与没有 }W
\text{ 时产生不同作用。}
}
\]

即：

\[
I_W\neq0.
\]

这仍然是 Posterior-0 的核心资格式。

---

# 十八、状态移植仍为最强因果门

同意评判：

M5 的主要因果负担应该由 transplant 承担。

构造：

\[
Z_{10}\leftarrow Z_{11}
\]

后给完全相同 Q。

要求：

\[
Response_{10}'
\approx
Response_{11}.
\]

反向 transplant 可作为一次加固：

\[
Z_{11}\leftarrow Z_{10}.
\]

如果预算允许建议做一次。

若单细胞 membrane state 足以关闭差异：

```text
POSTERIOR_MINIMAL_SUFFICIENT_STATE_CANDIDATE
STOP_DEEPER_DECOMPOSITION
```

不继续拆。

---

# 十九、PARALLEL_NEW_STATE 判别保留并收紧

若 posterior event 后观察到新 occurrence：

必须分别问：

1. 原 G1 carrier：

\[
Z_W
\]

是否被改变；

2. 是否只是新增了：

\[
Z_\Delta.
\]

如果：

\[
Z_W^{posterior}
=
Z_W^{sham}
\]

而：

\[
Z_\Delta\neq0,
\]

则：

```text
PARALLEL_NEW_STATE
POSTERIOR_REORGANIZATION = NO
```

不能因为最终 query 不同而判 PASS。

---

# 二十、历史不可修改继续作为硬门

本轮开跑前计算：

```text
SHA256(prior raw tracks)
SHA256(prior occurrence manifests)
SHA256(prior lineage records)
```

收口后重新计算。

要求：

```text
BEFORE == AFTER
```

否则：

```text
PC0-M6 = FAIL
```

不得解释为 posterior。

---

# 二十一、预算口径修订

接受 E-7。

创建：

```text
research/posterior_v0/data/budget_ledger.json
```

开工即建立。

明确：

```text
new_g0_trajectories       <= 4
prior_state_replays       <= 12
posterior_timing_points   <= 4
factorial_sets            <= 4
state_interventions       <= 4
path_blocks               <= 3
heldout                   = 6
```

### replay 计数规则

每次完整运行一个：

```text
W=1
```

历史 arm，计：

```text
1 prior-state replay
```

不按 2×2 set 整体只算一次。

预算不能靠定义压缩。

---

# 二十二、首跑中止也必须记账

类似 D2-1 标定首跑经验：

只要真正执行过一次不同参数/条件，

即使因为：

- error；
- duplicate；
- unreachable；
- support failure；

中止，

仍登记：

```text
attempted_run
```

和：

```text
physical trajectory consumed
```

如适用。

---

# 二十三、UTF-8 与回归

接受 E-7。

所有新文件显式：

```python
encoding="utf-8"
```

Windows 执行入口继续：

```text
PYTHONIOENCODING=utf-8
```

收口：

```text
python -m nexus_v1.tests.test_regression
python -m pytest tss/tests -m fast -q
version_pairing
```

如果长回归超时：

```text
TIMEOUT / NOT VERIFIED
```

不得写 FAIL，也不得冒写 PASS。

---

# 二十四、修订后的实际执行序

## Step A — Substrate + Reachability

先运行：

```text
substrate_audit.py
```

完成两件事：

### A1

测：

\[
\tau_{\rm residual}
\]

以及 residual availability window。

### A2

最多 2 条新 G0 trajectory 探测：

```text
late site23 occurrence reachable?
```

输出：

```text
posterior_near_reachable
latest_reachable_t_up
support_failure_boundary
```

这是整个 Posterior-0 的首个硬 Gate。

---

## Step B — Freeze Timing

如果 near reachable：

冻结：

```text
NEAR
```

若证据允许：

```text
MIDDLE
```

否则 middle/far 登记 LIM。

如果 near 不可达：

触发一次 R-1(c)。

如果仍不可达：

```text
terminal = D
reason = POSTERIOR_TIMING_UNREACHABLE
STOP
```

---

## Step C — Build Dataset

完成：

```text
2×2 calibration factorial
negative controls
query window
hold6
```

随后立即：

```text
SHA256 hold lock
```

---

## Step D — Main Interaction

执行：

\[
(0,0),(1,0),(0,1),(1,1)
\]

计算：

```text
I_W_trajectory
I_W_occurrence
probe_margin
resource_trace
```

---

## Step E — Negative Controls

依次：

```text
later-event-only
history-only
timing-control
parallel-new-state check
```

---

## Step F — Causal Tests

先：

```text
bundle_c path block
```

再：

```text
RelationCell state transplant
```

后者为主要因果门。

---

## Step G — Held-out + Final

hold6 一次运行。

然后：

```text
PC0-M1..M6
```

终裁。

---

# 二十五、六门修订后的精确定义

## PC0-M1 — Closed-history usable substrate

不是“state 非零”即可。

要求：

\[
T_{\rm residual}
\]

足以容纳至少一个合法 posterior event。

---

## PC0-M2 — Physical posterior entry

Δ：

```text
C input
→ tin_c
→ bundle_c
→ RelationCell
```

正常物理传播。

---

## PC0-M3 — Persistent organizational state

washout=456 后：

\[
Z_{11}^{membrane}
\neq
Z_{10}^{membrane}
\]

超过 numerical floor。

`RESOURCE_TRACE_ONLY` 不算。

---

## PC0-M4 — Prior-dependent interaction

\[
I_W^{traj}
>
\epsilon_{num}.
\]

event difference 可加固，不能替代 continuous criterion。

---

## PC0-M5 — Causal carrier

要求至少：

```text
state transplant PASS
```

并优先要求：

```text
bundle_c block reduces/removes interaction
```

若 block 因结构限制不可解释，但 transplant 强成立：

不得自动 FAIL；

登记 block limitation，由 final ruling 单独披露。

---

## PC0-M6 — Immutable / Blind / Ledger

要求：

```text
past SHA unchanged
hold6 locked before run
lineage valid
budget legal
replay deterministic
```

---

# 二十六、Posterior operator 的命名纪律保持

如果 A 成立，只允许：

\[
\boxed{
\mathfrak B_{\Delta\Gamma}^{W}
\neq id
}
\]

和：

```text
POSTERIOR_OPERATOR_CANDIDATE
```

禁止：

```text
Xin qualified
Xin implemented
TOPRXin confirmed
posterior cognition
reinterpretation
meaning update
```

Posterior-0 仍只是第一次物理后验状态重组织实验。

---

# 二十七、终态保持四种

```text
A
POSTERIOR_STATE_REORGANIZATION_V0_QUALIFIED
POSTERIOR_OPERATOR_CANDIDATE = YES
READY_FOR_POSTERIOR_1 = YES
```

```text
B
POSTERIOR_SUBSTRATE_ABSENT
```

```text
C
POSTERIOR_EFFECT_ADDITIVE_ONLY
```

```text
D
POSTERIOR_CAUSAL_REORGANIZATION_NOT_ESTABLISHED
```

D 允许 machine-readable reason：

```text
TIMING_UNREACHABLE
INTERACTION_WITHOUT_CAUSAL_CLOSURE
HELDOUT_FAIL
PHYSICAL_PATH_FAIL
OTHER_PREDECLARED
```

不增加第五终态。

---

# 二十八、对评判最终意见

接受其：

```text
ADOPT with amendments
```

结论。

特别接受三个关键纠正：

1. posterior timing 必须先服从当前 site23/G0 的物理可达域，而不是为了理论设计强行制造 far 条件；
2. Δ 应优先走不会自己产生新 occurrence 的 C 通道；
3. 当前 G1 state 是耗散 RC 状态，所以 Posterior-0 研究的是**有限历史残余窗内的后验重组织**，而不是永久记忆。

这三个修订不会削弱 Posterior-0。

反而使其问题变得更准确：

\[
\boxed{
\text{在当前 G1 历史状态仍物理存在的有限窗口内，
后来的合法事件能否对它产生依赖过去的、非加法的、可因果干预确认的重组织？}
}
\]

这才是当前架构真正有资格回答的问题。

---

# 二十九、正式开工状态

```text
POSTERIOR-0 PLAN = ADOPTED

E-1 = ACCEPTED
E-2 = ACCEPTED
E-3 = ACCEPTED
E-4 = ACCEPTED
E-5 = ACCEPTED
E-6 = ACCEPTED
E-7 = ACCEPTED

R-1 = (a) PRIMARY + (c) CONDITIONAL FALLBACK
R-1(b) = REJECTED

R-2 = ACCEPTED
DELTA_PRIMARY = C
QUERY = C

R-3 = ACCEPTED
WASHOUT = 456 steps
PRIMARY_STATE = RelationCell membrane
RESOURCE_TRACE_ONLY = excluded from posterior-state qualification

READY_TO_EXECUTE = YES
```

---

# 三十、一句话执行要求

\[
\boxed{
\text{先测“历史还活多久”和“后来事件能不能物理到达”，
只有两者确实重叠，才允许开始测试 Posterior。}
}
\]

不要为了让后验构建成立而制造新的记忆机制、平移历史或增加自由参数。