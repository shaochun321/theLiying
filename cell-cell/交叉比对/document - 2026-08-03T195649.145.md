确认：

[
\boxed{\text{TSS-A1通过，可以冻结，不需要重开。}}
]

现在最准确的正式结论应采用你刚才修正后的过程表述：

[
\boxed{
\text{外部物理发生过程}
\xrightarrow{\mathcal M}
\text{地址化内部生成活动云过程}.
}
]

而不是“外部对象被表示成内部静态结构”。

当前已经实际满足：

* 父谱系来自真实 `OccurrenceInstanceId`；
* 云成员由真实collector响应决定；
* 近邻进入、远方不进入；
* 地址重命名不改变成员资格；
* 映射结果保留外部发生过程与内部生成活动之间的可追踪关系。

这使当前主线第一次真正连成：

[
\boxed{
\omega[W]
\rightarrow
\mathfrak C_\omega[W]
\rightarrow
{\Theta,\Sigma,\Lambda}
\rightarrow
\mathcal C_{\mathrm{couple}}.
}
]

## 下一步：不要直接匆忙写TSS-2电路

尺度生成算子比当前空间局部性更容易混入人工语义。首先需要冻结它的最低定义：

[
\boxed{
\Lambda^{(1)}
\text{不是给同一数据贴上“粗／细”标签，}
\text{而是实际改变生成活动云可被组织和作用的结构窗口。}
}
]

同一个外部发生过程产生：

[
\mathfrak C_\omega[W],
]

在两个由项目结构实际承载的尺度窗口下：

[
Q_{\lambda_a},
\qquad
Q_{\lambda_b},
]

形成：

[
\mathfrak C_\omega^{(\lambda_a)}
================================

Q_{\lambda_a}
\bigl(\mathfrak C_\omega\bigr),
]

[
\mathfrak C_\omega^{(\lambda_b)}
================================

Q_{\lambda_b}
\bigl(\mathfrak C_\omega\bigr).
]

尺度算子要回答的是：

> 当结构窗口改变时，哪些生成活动被保留、合并、消失或重新取得下游作用资格？

而不是简单计算平均值或修改数组分辨率。

## 当前唯一现实阻塞

现有TSS-A1活动云只有：

[
\text{active_sites}={31}.
]

单个活动生成元无法充分验证结构尺度转换，因为：

[
\text{一个元的局部读出}
\quad\text{和}\quad
\text{一个元的聚合读出}
]

很容易退化成同一个结果。

因此下一任务建议定为：

[
\boxed{\text{TSS-2a：尺度生成算子的最低资格与现有物理载体审计。}}
]

本轮只确认：

1. 当前哪些已存在的物理结构可以构成两个不同尺度窗口；
2. 是否已有一次外部发生能够产生至少两个活动云条目；
3. 尺度窗口改变是否会真实改变下游可见结构；
4. 哪些变化只是软件聚合，不能算尺度生成；
5. 是否需要一个新的多生成元外部驱动场景。

不要立即加入学习、竞争、STDP或DA。

如果现有资产无法提供至少两个共同活动的基础生成元，应先补一个最小多成员活动云场景，再构造：

[
\Lambda^{(1)}.
]

所以当前冻结状态可以写为：

```text
TSS-0：通过
TSS-1：Σ_support^(1)通过
TSS-A0：过程映射契约通过
TSS-A1：过程映射运行时闭合通过
TSS-2a：尺度载体与最低资格审计，下一步
```

这样继续沿着生成元主线前进，同时避免把人工数据聚合误称为尺度生成算子。
