# 基础生成元执行总纲 V3（精简版，2026-07-17 创建，2026-07-17 批判十六评判后更新）

> **本文件只保留当前有效结构**，不追加执行流水记录。完整历史（十五轮交叉比对批判的
> 逐条评判、每个设计决策的推导过程、已废弃路径的原因）保留在
> `基础生成元双轨落地方案_v2_2026-07-16.md`（历史审计记录，不删除、不再追加新内容）。
> 后续批判评判优先对照本文件的"当前状态"，仅在需要追溯某项历史决策的完整论证时查阅
> V2。**多轮小规模评判-裁定循环直接更新本文件对应章节，不逢轮新建文件/报告**（用户
> 2026-07-17 反馈："两到三个版本做一份方案，只要标明衔接就行"）。

**当前状态（2026-07-17，差分评判协议——只记当前状态，不复述历史推导；完整逐轮
论证见V2存档）**：P1-A/P1-B（B0~B3）/P1-C0 已完成冻结；P1-C1已完成（夹具A/B
真实运行+三级账本+完整联合图同构+退化/组合失稳，详见下方P1-C1小节冻结契约）；
下一步 P1-C2。

## 项目共同规范（不随阶段变化，详见 CLAUDE.md / `nexus_v1/RULES.md`）

- 结构优先，禁语义硬编码：任何新信号路径/新参数动手前必须先答"强制三问"（Q1生物对应物
  /Q2物理结构Sources→Bundle→Targets/Q3参数依据）。
- 不改母本代码：新功能走子类叠加/独立新模块，不修改 `HebbianCircuit`/`VariantCircuit`/
  `ThermalFieldGraph`等已测试基础类的既有行为契约。
- Model before tune：参数从测量/物理锚定推导，不为了"让测试过"拍脑袋改数字。
- 每个新组件先在诊断脚本验证，再转为正式回归测试；每轮改动后跑全量回归
  `python -m nexus_v1.tests.test_regression`（21/21 通过）。

## 一、已冻结底座（不再修改机制，只在新物理输入下重新标定工作区间）

### 核心关系原语

| 组件 | 文件 | 状态 |
|---|---|---|
| `r≺^τ`（二元时间关系） | `nexus_v1/relations/temporal_r_prec.py` | B1通过；B2（真实ξ发生顺序验证）已完成 |
| `r_ρ^τ`（窗口化射影关系） | `nexus_v1/relations/ratio_r_part.py` + `ratio_r_part_ternary.py` | 二元(T3-C1)+三元(T3-C2)均已冻结 |
| `GradedPotentialRelay`（分级电位线性中继） | `nexus_v1/components/graded_potential_relay.py` | Bundle-source契约+独立账本已完成，通用连续关系载体 |

### 世界物理底座

| 组件 | 文件 | 状态 |
|---|---|---|
| `ThermalCell`/`ThermalLink`（局部导热网络） | `nexus_v1/components/dynamic_thermal_field.py` | W1完成+L2关系微分只读接口（`temperature_difference`/`heat_flux`/`node_divergence`/`incoming_flux`/`outgoing_flux`/`closure_residual`） |
| `ThermalFieldLocator`/`DynamicHeatSource` | `nexus_v1/components/thermal_source_coupling.py` | W2A完成，`couple_and_step()`原子化 |
| `SkinThermalState`/`ThermalContact` | `nexus_v1/components/skin_thermal_contact.py` | P0+P1-0完成：世界-皮肤联合守恒闭合+接触边稳定性检查（`contact_stability_numbers`/`is_contact_stable`） |

### 已知未决的独立议题（不阻塞主线，需要时单独立项）

- `stable_hash()`：替换 `bundle.py` 的 `hash()` 对称性打破扰动，全项目级可复现性修复，
  影响面覆盖所有已用该机制的既有测试，需独立评估。
- 两阶段 `prepare_physical_step()`/`apply_physical_step()` 事务接口：**已裁定推迟**
  （批判十六评判，2026-07-17）——P1 先延续 P0/P1-0 已验证的"检查全部在唯一副作用调用
  之前完成"纪律（`couple_world_skin_step()` 的顺序：稳定性检查→只读快照→唯一副作用
  调用→统一提交），不预先引入 `PhysicalStepPlan` 抽象。**客观触发条件**（批判十七⑧，
  2026-07-17，接受——"不够用时再引入"过于模糊，改为具体判据，命中任意一项就必须改为
  两阶段提交）：①一步内存在2个以上独立可变状态容器；②首处状态修改后仍可能发生可预见
  异常；③多条搬运边争夺同一源节点剩余能量；④验证结果依赖应用顺序；⑤需先扣source
  再确认world/skin能否接受；⑥失败后无法由账本唯一恢复到前状态。**P1-B 很可能触发
  ①③④项**——写 P1-B 代码时必须主动核查，不能等出bug再决定。

## 二、当前执行主线（2026-07-17 批判十六细化，P1/P2 各拆三层）

```
P1-A（身份/拓扑契约） → P1-B（有序过剩热能转移） → P1-C（联合验证）
  → P2-A（动态皮肤→ξ^occ） → P2-B（回接关系原语） → P2-C（关系-关系生成）
  → L3 决策
```

### P1-A（已完成）：身份/拓扑契约

**实施结果**：`nexus_v1/components/structural_address.py`（`StructuralAddress`/
`GeneratedAddress`/`SymmetricEdgeIdentity`/`OrderedEdgeIdentity`/`AddressRegistry`/
`TopologyValidationReport`）+ `nexus_v1/tests/test_structural_address.py`
（T-SA-1~9，9/9 PASS）。**实现过程中发现并修复一个真实漏洞**：`rebuild_physical()`
后若用旧 `local_key` 重新调用 `register_physical()`，因 `uid` 由 `local_key`
确定性推导，会悄悄覆盖已重建的新版本记录（旧地址实际未失效）——已用
`_retired_local_keys` 集合堵住，`register_physical()`/边注册的 `_require_registered()`
均拒绝引用已退休的 local_key 或过期版本的地址对象。全量回归 21/21 PASS。

**核查结论（批判十六①，2026-07-17）**：现有 `ThermalFieldGraph.cells` 已是
`Dict[int, ThermalCell]`（按 `node_id` 键控），`ThermalLink`/`ThermalContact` 全部用
显式 `node_id` 字段查表，全仓库无一处按数组位置寻址——"排列等变性"bug 类在现有架构下
已结构性不存在。**用户已裁定采用完整版身份地址系统**（覆盖我方"轻量版已够用"的建议，
作为长期基础设施投入）。

**类型化订正（批判十七①②③④，2026-07-17，接受）**：

- **地址拆两类**（原统一四元组混淆了物理层级和生成递归深度）：
  - `α_phys(x)=(d_x, u_x, v_x)`：用于世界节点/皮肤支撑/热源/物理边界——这些是关系
    生成依赖的物理支撑，不是"零阶关系生成元"；
  - `α_gen(x)=(d_x, u_x, P_x, δ_x, v_x)`：用于 `ξ^occ`/`r≺^τ`/`r_ρ^τ`/未来关系-关系
    生成物——`P_x`=父支撑或父关系地址集合，`δ_x`=生成深度。P1-A 本轮只实现两个地址
    类型的数据结构，`α_gen` 暂不接实体（留给 P2 使用）。
- **边身份拆两类**（原统一机制集合会给互易边偷偷塞进永久方向）：
  - `ε_e^sym=(u_e, {α_i,α_j}, m_e, v_e)`：扩散边/接触边，端点是**无序集合**——
    `J=κ(T_i-T_j)`/`J=H(T_w-T_s)` 本质是两端点间互易耦合，source/target只是某次
    计算的符号约定，不是边永久属性；
  - `ε_e^ord=(u_e, α_tail, α_head, m_e, v_e)`：只有 P1-B 的有序过剩热能转移边
    （`m_e="ordered-excess-thermal-transfer"`，2026-07-17批判二十已定型，取代本节
    最初讨论时的占位机制名`medium-transport`）——`J_e^oet=a_e·u_tail^+`
    （`u_tail^+=max(q_tail,0)`，P1-B1.5钳位后的当前有效公式，取代未钳位的
    `J_i→j^tr=a_ij·E_i`占位公式）的公式本身依赖哪端是tail，是真正有序的。
  - `m_e ∈ {diffusion, contact, ordered-excess-thermal-transfer}`。**订正说明
    （批判二十二，2026-07-17）**：本小节写于P1-A实现之初（早于P1-B0/B1.5的机制
    命名与钳位定型），此前长期保留占位机制名/公式未随下方P1-B章节同步更新，导致
    同一份当前生效文档内并存两套定义——已订正为当前唯一有效定义，`medium-transport`/
    `J_i→j^tr=a_ij·E_i`不再出现在任何当前章节，仅见于"已否决假设档案"（五、资格
    与升级登记表）。
- **排列测试升级为图同构**：不用矩阵置换 `M'=PMP^{-1}`（降级为线性子系统的具体实现
  测试），改用地址重命名双射 `φ:V→V'`，验证 `φ(F_G(x))=F_{φ(G)}(φ(x))`——同时覆盖
  节点状态/边端点/热源定位/皮肤接触/账本地址/生成元映射。**引入两层身份**：
  `local_key`（容器内部当前查表键，如 dict 的整数 key）vs `stable_uid`（跨重建保持的
  物理身份，即 `α.u_x`）——两者必须是不同字段，否则"索引变但地址不变"这条测试本身
  无法构造。
- **版本号递增条件**：`v_x` **仅**在身份连续性被破坏时递增（对象销毁后用同一UID重建/
  皮肤支撑被拆分或合并/物理节点角色发生结构性替换/父生成关系集合改变），**不**因
  普通状态变化（温度/能量/接触面积/边权）递增——否则地址每步失效。边有独立的
  `v_e`，不因节点动态状态变化而改变。

**8项结构测试**：边两端点存在/端点域与机制兼容/对象重排序结果不变/`local_key`变化但
`stable_uid`不变时物理结果不变/结构重建后版本递增旧地址失效/`e_ij`与`e_ji`是两个
不同有序传输实例/禁止同一物理边重复注册重复扣账/所有状态变化可追溯到明确源项或边
身份。加图同构验证测试（上述`φ`性质）。

**不实现**（避免"完整版地址系统"被误解为要建通信网络，批判十七九.第二步明确列出）：
路由/坐标/最近邻/网络消息/路径搜索/地址自动推断——"完整"指身份字段完整，不是建设
通信基础设施。

### P1-B0（已冻结定义，批判十八/十九，2026-07-17）：有序过剩热能转移机制契约

**核心订正①（批判十八）**：不叫`AdvectiveThermalLink`（真正介质平流需要质量转移
`J^adv=ṁ_e·c_p·T_i`+节点热容`C_i=m_i·c_p`随之变化；当前`THERMAL_FIELD_MODE=
"normalized"`无介质质量状态，只搬热能不是真平流）。

**核心订正②（批判十九，已用代码实测验证）**：`ThermalCell.capacitor.charge`
**不是绝对热能，是相对该节点自身`ambient_temperature`的偏差量**——`ambient_
temperature`是逐节点字段，`temperature=ambient_temperature+capacitor.voltage`。
两个`ambient_temperature`不同的节点，`charge=0`代表的实际温度可以完全不同（实测：
`ambient=100`/`ambient=0`两节点`charge=0`时温度分别是100.0/0.0）。且`charge`可以
为负（节点低于自身基线时），此时若不处理会让转移方向翻转但边身份不变，破坏"非负
有序通量"这个P2-C已依赖的前提。

**正式命名 `OrderedExcessThermalEnergyLink`**（比批判十八的`OrderedThermalEnergyLink`
再收紧一层——语义从"搬运全部能量"进一步收紧为"只搬运统一环境基线以上的过剩部分"，
命名须反映真实语义；未来进入physical-anchor模式、加入质量/比热/流量/体积才升格真正
advection）。

**冻结定义**（在批判十八7项基础上，批判十九订正②③④⑤，新增⑧⑨）：
1. **机制命名**：`OrderedExcessThermalEnergyLink`，语义为"有序过剩热能转移"，不是
   "方向"、不是"介质平流"、不是"总热能转移"。
2. **状态变量与过剩钳位（批判十九①，订正）**：`u_i^+=max(q_i,0)`，`q_i=
   ThermalCell.capacitor.charge`——节点低于自身环境基线时不提供过剩热量，
   `p_e=a_e·u_i^+`恒`≥0`，tail/head身份不会因符号翻转而颠倒。**不再声称**
   `charge`是"绝对能量零点"（原表述有误，已订正）。
3. **两端基线一致性前提（批判十九①，新增）**：有序边两端必须
   `ambient_temperature_tail=ambient_temperature_head`，否则转移物理上不对等——
   `step_ordered_transport()`在执行前校验，不满足则拒绝，状态不变。
4. **`a_e`参数来源（批判十九③加强措辞）**：`a_e=1/τ_e`，**`τ_e=2.0`是EXP机制
   测试起点，尚未物理标定**（不再只说"首轮起点"，需明确"尚未标定"）——未来标定
   路径：已知参考时间`t_r`内应保留比例`r`则`a_e=-ln(r)/t_r`，或半衰时间
   `a_e=ln(2)/t_half`。
5. **单边非负性稳定门（批判十九④，新增，已用代码验证是真实bug）**：
   `rate_per_time·dt≤1.0`时递推`u^{n+1}=(1-a_eΔt)u^n≥0`恒成立；超出时
   `step_ordered_transport()`在任何状态修改前`raise ValueError`（fail-fast，
   **不用`min()`静默钳制**——那会把线性模型悄悄变成分段模型并掩盖不稳定配置）。
   实测：`a_e=2.0,dt=1.0`（`a_eΔt=2.0>1`）此前会让tail从10变成-10，现已拒绝。
6. **`ThermalTransportPlan`**：范围受限（不是全项目通用`PhysicalStepPlan`，那个仍
   按批判十七裁定推迟）的不可变计划对象，**批判十九⑤建议与多边竞争合并**：
   P1-B2改为"计划生成与多边竞争一起实现"（不再是"先B2后B3"两个独立阶段）——
   多边竞争正是计划对象存在的原因，没有计划阶段无法保证多条边都从同一份快照
   计算。
7. **联合稳定性公式（批判十九②，订正量纲）**：
   `η_i^joint = Δt·[(Σ_j κ_ij + Σ_s H_is + G_i^env)/C_i + Σ_{e:tail(e)=i} a_e] ≤ 1`
   `G_i^env=1/r_leak_ambient`（**导纳形式**——`Capacitor.leak(resistance,dt)`
   的参数字面是电阻，`r_leak_ambient`是电阻量，必须先取倒数变成导纳才能与
   `κ`/`H`同处理进同一个分子括号；原表述"λ_i来自r_leak_ambient"只改了变量名
   没给换算关系，是真实疏漏，已订正）。
8. **三级账本字段**：边级`R_e=Δu_tail^e+Δu_head^e=0`；节点级
   `R_i=Δu_i-(q_i^source+q_i^diff+q_i^contact+q_i^transport-q_i^loss)`；全局
   `R_G=Σ_iΔu_i-(E_source,drawn-E_loss)`。
9. **驱动机制欠账声明（批判十九⑦，新增）**：`p_e=a_e·u_i^+`不依赖温差，可以把
   过剩热量从较冷节点搬到较热节点——不是被动扩散，是主动/未建模介质驱动的过程；
   能量两端守恒不代表完整热力学账本闭合（驱动该转移的机械功/压力差未建模）。
   新增`transport_drive_mode="exogenous_normalized"`标志字段，声明当前不对
   驱动机制的外部机械功/熵增/热力学第二定律做资格宣称。
10. **P1-C固定测试夹具**（记录供P1-C使用）：夹具A（5世界节点+1热源+2扩散边+2条
    共享tail的有序出边+1接触边+1皮肤状态）；夹具B（1世界节点+2皮肤点+2条接触边，
    专测`Σ_sH_is`求和情形，验证联合预算不能分别通过后拼接）。

**统一快照纪律**（批判十八③，接受）：同一tail节点多条有序出边时必须"先全部快照→
统一提交"，不能逐边即时扣除——直接延续`dynamic_thermal_field.py.step()`/
`skin_thermal_contact.py.couple_world_skin_step()`已验证的纪律，不是新要求。

**`step_ordered_transport()`定位（批判十九⑥，2026-07-17已落实）**：SINGLE-EDGE
DIAGNOSTIC ONLY——P1-B2已引入`ThermalTransportPlan`，多边场景的生产联合调度走
`prepare_thermal_transport_plan()`（P1-B3的`apply()`完成后走plan-based接口提交），
`step_ordered_transport()`保留作为单边诊断/测试入口，不用于多边生产场景。

### P1-B1（已完成）→ P1-B1.5（已完成，批判十九修复）：单边机制 + 过剩钳位 + 稳定性门

**实施结果**：`nexus_v1/components/ordered_excess_thermal_energy_link.py`
（`OrderedExcessThermalEnergyLink`+`step_ordered_transport()`+
`is_single_edge_stable()`，原`ordered_thermal_energy_link.py`已删除并改名）+
`nexus_v1/tests/test_ordered_excess_thermal_energy_link.py`（T-OETL-1~13，
13/13 PASS）。除 P1-B1 原有验收（单步精确匹配解析递推/多步收敛/参数边界拒绝/
边级守恒/纯只读）外，新增：负tail charge钳位为0转移/`a_eΔt=1`边界恰好全部转移/
`a_eΔt>1`执行前拒绝状态不变（原真实bug的回归测试）/两端`ambient_temperature`
不等拒绝/`tail==head`自环拒绝/`transport_drive_mode`标志存在。全量回归 21/21
PASS。

`i→j`只说明"本次物理转移从哪个能量仓扣除、向哪个能量仓注入"，**不意味着**前后
左右/环境方向/自我方向/`r→`/坐标轴正负。

### P1-B2（已完成）：不可变计划生成 + 同源多边竞争

**实施结果**：`nexus_v1/components/thermal_transport_plan.py`
（`ThermalTransportPlan`+`EdgeTransfer`+`prepare_thermal_transport_plan()`）+
`nexus_v1/tests/test_thermal_transport_plan.py`（T-TTP-1~8，11个测试函数全部
PASS）。同时给`AddressRegistry`新增`revision`只读计数器（每次成功注册/重建操作
递增一次，供计划快照+未来staleness检测）+ `is_current_address()`/
`is_current_ordered_edge()`两个只读校验方法。

`prepare_thermal_transport_plan(links, cells_by_uid, registry, dt, eta=1.0)` 严格
只读11步序列：验证地址/边身份仍是当前版本→验证机制类型正确→验证无重复边→验证
所有参与节点共享同一`ambient_temperature`→一次性拍摄所有tail的charge快照→计算
每条边`u_tail^+`→算功率+单步能量→按tail汇总出流率→验证传输专属稳定性门
`η_i^transport=Δt·Σ_{e:tail=i}a_e≤1`（不满足则`raise`，不生成计划）→生成节点净
变化+账本预览→返回不可变`ThermalTransportPlan`。

验收覆盖批判二十给出的8类范围：计划纯只读/两边共享tail用同一快照/边遍历顺序
不影响结果（正序/逆序）/组合失稳拒绝且状态不变（单条边各自稳定但总和超限）/
零或负过剩状态时该tail全部出边`p_e=0`/多个不同tail的预算独立不互相污染/地址
拓扑错误（stale地址/重复边/不同ambient基线/缺失ThermalCell）全部在计划生成阶段
拒绝/计划账本闭合（节点净变化总和为0）。全量回归 21/21 PASS。

### P1-B3（已完成，批判二十一）：提交与故障语义 —— P1-B 阶段整体收口

**实施结果**：`nexus_v1/components/thermal_transport_plan.py` 扩展——
`ThermalTransportPlan`新增`participating_node_snapshots`字段（全部参与节点tail+head
的charge快照，`prepare()`第5步一次性拍摄，供`apply()`提交前状态过期检测）+
`AppliedPlanRegistry`（`is_applied()`/`mark_applied()`，防止同一计划提交两次）+
`ThermalTransportReceipt`（成功提交回执）+ `apply_thermal_transport_plan(plan,
cells_by_uid, registry, applied_plans)`。`nexus_v1/tests/
test_thermal_transport_plan_apply.py`（T-TTPA-1~9，9项全部PASS）。

状态过期检测用**方案A**（计划自带全部参与节点快照，提交前逐一比对`==`，不一致拒绝）
而非全局状态修订号方案B——A零侵入（不需要给`Capacitor`/`ThermalCell`等已测试组件加
全局计数器字段，符合"不改母本代码"）。`apply()`**不重新计算任何物理量**（`u_i^+`/
`a_e`/功率/单步能量/节点净变化全部已冻结在`plan.edge_transfers`/`plan.
node_net_deltas`里），只做四层一致性检查（计划未提交过→registry版本未变→边/地址
仍是当前版本→参与节点charge与快照一致）后写入已冻结的delta。

故障语义**快照回滚**（不是"证明提交区不抛异常"——那需要完整源码级副作用审计，
本轮未做，不应假设）：写入前备份全部参与节点的完整`Capacitor`状态（`charge`+KCL
记账字段`_q_in`/`_q_out`，不只是`charge`，否则回滚后账本自身会不一致），写入过程
任何异常都触发全部已修改节点的完整回滚+重新抛出原始异常，不吞异常、不部分提交。
**已用代码实测验证**：多边计划中前一条边已成功写入、后一条边中途抛异常，回滚正确
恢复了包括前一条边已修改节点在内的全部参与节点状态（不只是抛异常那条边）。

T-TTPA-9项覆盖：成功提交与计划完全一致/计划生成后节点状态改变提交拒绝/registry
revision改变提交拒绝/边版本改变提交拒绝/同一计划二次提交拒绝/计划应用到另一个图
拒绝（拆两种具体表现：缺失ThermalCell/节点存在但状态不匹配）/中途异常触发完整
回滚/所有拒绝场景状态不变/成功提交后账本闭合（`total_transport_residual<1e-9`）。
全量回归 21/21 PASS。

**P1-B3 通过后，P1-B 阶段（B0定义→B1单边→B1.5钳位修复→B2计划生成→B3提交语义）
整体收口**——批判二十一明确要求"直接进入P1-C，不再拆新的单边修补阶段"，本方案
采纳。

### P1-C：联合验证（批判二十二，2026-07-17，拆三门：C0/C1/C2）

**批判二十二裁定**：P1-C最重要的交付不是"多跑一些组合测试"，而是先建立一个范围
受限的联合一步调度契约——现有三条局部更新路径（世界扩散+泄漏/世界-皮肤接触/有序
过剩热能转移）加动态热源`source.release()`，P1-B的`ThermalTransportPlan`只覆盖
有序传输，不保证它与扩散/接触/热源基于同一时刻状态。**联合一步的正确语义**：
```
X^n=(q_world^n, q_skin^n, E_source^n)  ← 统一快照
  → 计算 {J_diff, J_contact, J_oet, P_source, J_loss}  ← 从同一快照算全部流量
  → 联合稳定性校验 → 聚合 → 一次性提交 X^{n+1}
```
不能是`graph.step()→transport.apply()→skin.step()→source.release()`这种顺序
调用——后面的机制会读到前面已修改的状态，物理结果重新依赖调用顺序（P0/P1-0已
验证过的同一类问题）。**不再无限细分，但三个证明面必须分开**，对应下方C0/C1/C2。

#### P1-C0（已完成，批判二十二）：联合计划与统一提交

**实施结果**：新文件`nexus_v1/components/joint_thermal_step_plan.py`——
`JointThermalStepPlan`（不可变计划，含世界+皮肤+热源**完整状态快照**：
charge+`_q_in`+`_q_out`三元组，不是只有charge）+ `JointThermalRuntime`
（持久化运行时对象，长期持有`world_graph`/`registry`/`applied_plan_registry`/
`runtime_uid`，`runtime_uid`取自`id(world_graph)`——绑定实际存活的图对象本身，
不是其内容）+ `prepare_joint_thermal_step()`（统一快照→计算扩散通过复用
`ThermalFieldGraph.step()`内部逻辑（不重新实现扩散数学，只组合注入字典）+
接触+传输+热源释放→联合稳定性`η_i^joint`校验→返回不可变计划，`stability_report`
是η诊断值，真正的三级账本只能在`apply()`后由实际运行结果算出，不在`prepare()`
阶段预先假算）+ `apply_joint_thermal_step()`（四层校验：未提交过→runtime_uid
匹配→registry版本未变→全部参与容器完整状态快照匹配→唯一写入区：一次
`world_graph.step()`调用原子完成扩散+泄漏+热源+接触+传输的世界侧写入→皮肤侧
inject/leak→热源扣减→异常回滚）。**直接复用**`ThermalFieldGraph.step()`（未
修改，扩散+泄漏逻辑完全在其内部完成，本模块只负责组合`external_injections`
字典）/`ThermalContact.flux()`/`OrderedExcessThermalEnergyLink.power()`/
`DynamicHeatSource`（`release()`公式在`prepare()`阶段只读复制，不调用，
`apply()`直接扣减冻结好的`source_actual_energy`）/`ThermalFieldLocator`，
不修改这些已测试模块本体，只新增组合调度层。地址关联：传输边端点uid通过
`registry.resolve(uid)`解析回`ThermalFieldGraph`的整数`node_id`（约定
`register_physical(DOMAIN_WORLD_CELL, node_id)`，与既有P1-A/B测试一致），
无传输边参与的世界节点不需要注册地址。

**同时闭合"资格与升级登记表"新增的三条B类条目**（P1-B3状态过期检测范围/提交
登记表生命周期/运行实例绑定）——这三处结构性边界本来就是联合调度契约的原生组成
部分，由`JointThermalStepPlan`的完整状态快照+`JointThermalRuntime`持久化持有
`applied_plan_registry`+`runtime_uid`绑定一并解决，不需要单独回头给
`ThermalTransportPlan`打补丁。

联合稳定性公式（P1-0修复②方法论的直接推广，见`skin_thermal_contact.py`
`contact_stability_numbers()`先例；批判十九②订正环境泄漏项量纲，见P1-B0定义7；
批判二十二核实与此公式一致，采纳）：
```
η_i^joint = Δt·[(Σ_j κ_ij + Σ_s H_is + G_i^env)/C_i + Σ_{e:tail(e)=i} a_e] ≤ 1
（G_i^env = 1/r_leak_ambient，导纳形式；Σ_s H_is 覆盖该节点关联的全部接触边，
不是分别检查单条接触边后拼接）
```

**C0自己的证明面**（调度契约本身，不是C1的不变量套件）：`nexus_v1/tests/
test_joint_thermal_step_plan.py`（T-JTS-1~9，9/9 PASS）——退化一致性（无传输边时
联合结果与现有`couple_world_skin_step()`真实运行数值精确一致）/组合失稳拒绝
（扩散/接触/传输各自看似安全但联合`η_i^joint>1`时，在任何状态写入前拒绝，三方
状态不变）/三处P1-B3边界的回归测试（完整状态指纹外部干扰后拒绝/同一runtime下
双重提交结构性拒绝/镜像图因runtime_uid不同被拒绝）/中途异常触发世界+皮肤+热源
三方完整回滚/成功提交账本闭合/多接触边共享同一皮肤时通量求和+多传输边共享同一
tail时Σa_e正确进入联合稳定性门/未注册传输边端点在prepare阶段即拒绝。全量回归
21/21 PASS，与既有P0/P1-0/P1-B2/P1-B3测试（43项）零冲突。

**暂不整合**：`SkinThermalState`/`ThermalContact`/`DynamicHeatSource`暂不
retrofit进`StructuralAddress`系统——C0范围内用直接对象状态指纹即可闭合三处边界。
是否升级为完整地址身份留作后续审查点判断（C类工程范围限制，不阻塞）。

**补丁（批判二十三，2026-07-17，差分格式）**：`runtime_uid`改为`id(self)`
（原`id(world_graph)`会让两个独立runtime包装同一图时共享uid，可绕过双重提交
检测——已实测验证并修复）；异常回滚范围扩大到`ThermalFieldGraph`图级诊断字段
（`_last_*`/`_total_injected`/`_total_leaked_ambient`，此前只回滚Capacitor
三元组会产生幽灵`closure_residual()`读数）；`DynamicHeatSource`新增
`preview_release()`只读方法作单一事实源，`release()`与联合计划的`prepare()`
共用同一份公式（原为两处独立实现）；补测试覆盖"两个不同世界节点接同一皮肤"的
η_s聚合（公式本已正确，仅补测试）。T-JTS-10~12新增，全量T-JTS-1~12+既有53项
测试+回归21/21全部PASS。报告：`P1-C0补丁_三处结构性边界修复_2026-07-17.md`。

#### P1-C1（已完成，批判二十四）：一步联合不变量

**实施结果**：`joint_thermal_step_plan.py`扩展——`JointThermalStepPlan`新增
`node_source_injection`/`node_contact_injection`/`node_transport_net`/
`edge_contact_transfers`/`edge_transport_transfers`（折叠进`world_injections`
前的分项，节点/边级账本的唯一数据源，不重新计算物理量）；`JointThermalStepReceipt`
新增`node_ledger_residuals`/`edge_ledger_residuals`。新增
`test_joint_thermal_step_plan_c1.py`（T-C1-1~6，6/6 PASS）：
1. **夹具A/B真实运行**：夹具A（5世界节点+1热源+2扩散边+2条共享tail传输边+
   1接触边+1皮肤+环境泄漏）+夹具B（1世界节点+2皮肤+2接触边，测Σ_sH_is另一半
   ——一节点连两皮肤，与T-JTS-8"两节点连一皮肤"互补）均真实运行prepare+apply；
2. **组合失稳**：多节点场景下扩散/接触/传输各自安全但联合η超限，在写入前拒绝，
   5世界节点+1皮肤+1热源全部状态不变；
3. **三级账本**：边级`R_e`/节点级`R_i`（真实观测ΔE_i vs 冻结分项之和，扩散/
   泄漏项来自`ThermalFieldGraph`自己刚更新的图级诊断`_last_divergence_dt`/
   `_last_leak`，非重算）/全局`R_G`，覆盖夹具A全部5节点+1皮肤+3条边，全部
   `<1e-9`闭合；
4. **完整联合图同构**：地址重命名双射φ覆盖world node_id+skin_patch_id，
   `world_injections`/`skin_injections`/`stability_report`/三级账本残差/
   回执全局总量在φ下逐一对应，不止复用P1-A的世界图纯扩散同构测试。

全量回归21/21 PASS，与既有68项相关测试零冲突。报告：`P1-C1_一步联合不变量_
2026-07-17.md`。**下一步**：P1-C2（算子与长程资格）。

#### P1-C2（待启动）：算子与长程资格

- **线性正域**：所有有序tail全程`q_i>0`，构造真实运行算子`E^{n+1}=M_E·E^n+b`，
  验证`M_E,ij≥0`、封闭时`1^⊤M_E=1^⊤`，且必须验证真实代码
  `E_runtime^{n+1}≈M_E·E_runtime^n+b`（不能只证明一个独立构造的矩阵守恒）；
- **过剩边界**：`q_i→0`附近验证不出现负传输/tail与head不隐式翻转/`q_i≤0`后
  出边功率为零/账本仍闭合/没有数值抖动造成频繁正负穿越；
- **矩阵守恒需明确状态变量**（批判十七⑤，接受）：能量态`1^⊤M_E=1^⊤`与温度态
  `C^⊤M_T=C^⊤`（`C^⊤=(C_1,...,C_n)`）两者守恒条件不同（`E_i=C_i·T_i`，节点
  热容不同时不等价），不能用温度矩阵却检查列和为1；
- **步长收敛**：`Δt`/`Δt/2`/`Δt/4`跑到同一物理时刻，误差应随步长减小而收敛
  （不只是"都没发散"）；
- **浸泡测试拆两段**（批判十七⑥，接受——特征值方案只在参数冻结/线性/拓扑固定/
  `a_e(t)`不变时唯一存在）：**冻结段**（热源关闭，拓扑固定，运行`5~10τ_slow`，
  验证能量漂移/非负性/NaN·inf/三级账本/source耗尽后无虚假注入/正确稳态）+
  **驱动段**（热源开启或`a_e(t)`变化，不使用单一`λ_2`，验证有界性与逐步账本，
  驱动停止后必须能进入冻结段并收敛）。

**表述订正（批判十六②，2026-07-17，接受，跨阶段持续有效）**：P1最小实验验收项
不称"L2接口能区分'相同温度不同通量历史'"（`J_e=a_e(t)·E_i(t)`是无记忆瞬时
函数，与历史无关），准确表述是"相同温度状态+不同介质传输系数a→不同通量关系"。

### P2-A（P1-C通过后）：动态皮肤 → 真实发生元

链路：`world → contact → skin state → ξ_i^occ`。重新测量启动时间/峰值/burst/衰减/
静息/支撑范围/可消费幅度。用稳定地址建立明确映射
`α(skin patch) ↔ α(ξ_i^occ)`，不是通过数组位置碰巧对齐。

### P2-B（P2-A通过后）：真实发生回接关系原语

让真实发生进入`r≺^τ`/`r_ρ^τ`——验证原语机制不改，只重新标定输入工作区间。

### P2-C（P2-B通过后）：首次关系-关系生成

**信号域限制（批判十七⑦，2026-07-17，接受；批判二十③订正公式，2026-07-17）**：
`GradedPotentialRelay`/共享分流池吃非负活动量，但扩散/接触通量`J∈ℝ`可正可负——
直接输入会被钳制、失去含义。只有有序过剩热能转移通量
`J_e^oet=a_e·u_tail^+`（`u_tail^+=max(q_tail,0)`，**不是**未钳位的原始
`a_e·E_i`——若照旧公式实现会绕过 P1-B1.5 已修复的过剩钳位，重新读取原始
`charge`，重新引入负值翻转问题）天然非负。**首轮 P2-C 只能用同一节点的三条真实
有序传输出边**`J_{i→j1}^oet,J_{i→j2}^oet,J_{i→j3}^oet`构造：
`r_{ρ,J}^τ=[z_{i→j1}^τ:z_{i→j2}^τ:z_{i→j3}^τ]`（复用已冻结的三元共享池，不再是人工
给定的三条感温脉冲），**不接**带符号的扩散/接触通量。未来若要纳入互易边，应先定义
"实际单步转移事务" `Q_e^n=(donor, receiver, |ΔE_e|)`（幅度非负，donor/receiver由
当前物理状态决定）——仍是物理账本，不是方向生成元。**仍不称**方向/事件/趋向/空间
判断——只是实际物理关系之间形成的窗口化参与结构。

### L3 决策（P2-C通过后再评估，门槛已细化）

不仅要求"P0+P1+P2都通过"，还需满足：①连续物理过程没有外部事件分段标签；②关系状态
来自真实发生和真实输运；③不同物理过程能够形成不同关系轨迹；④区分不依赖坐标/方向
标签/预设类别；⑤候选事件边界来自关系延拓破缺，而非固定时间窗；⑥地址和拓扑审计可以
追溯事件涉及的真实物理支撑。达到后才决定是否建设结构诱导事件原型，不预先承诺。

## 三、暂停项（不在当前主线，需要单独重新确认才能恢复）

- 旧 T2（`r_edge-lag`方向生成元）/ M2（运动方向关系）路线；
- T4 旧双轨（温感轨+运动轨）并行集成；
- 感温点从最小支撑向全 64 点扩展；
- `MotionState.polarization` 硬编码替换；
- 口器坐标 / 自我坐标类组件；
- P/R、Xin、Motor 与本项目关系原语的正式回接；
- 运动轨 M0（任务#3，温感轨核心交付物已冻结，尚未启动，与世界模型P1~P2无直接依赖，
  可并行考虑但需用户重新确认优先级）。

## 四、报告存档索引（按时间倒序，仅列关键节点）

- P1-0（接触边稳定性修复）：`cell-cell/工作报告/P1-0_接触边稳定性修复与前置收口_2026-07-17.md`
- P0（世界-皮肤边界闭合）：`cell-cell/工作报告/P0_世界-皮肤热交换与联合守恒调度_2026-07-17.md`
- L1+L2（三元扩展+关系微分接口）：`cell-cell/工作报告/T3-C2三元扩展与世界关系微分只读接口_2026-07-17.md`
- T3-C1（动态比例+Bundle接入）：`cell-cell/工作报告/T3-C1_动态比例与真实Bundle接入验收_2026-07-17.md`
- T3-C1R（GradedPotentialRelay）：`cell-cell/工作报告/T3-C1R_分级电位线性中继与读出机制判别_2026-07-17.md`
- 更早历史（T0~T3-C0、W0~W2A）：见 V2 存档对应章节。

## 五、资格与升级登记表（2026-07-17 批判二十一新增，方法论沉淀）

**用途**：本会话近期发生多次收紧/改名（`AdvectiveThermalLink`→
`OrderedThermalEnergyLink`→`OrderedExcessThermalEnergyLink`/"L2能区分历史"被
收紧/"方向"生成元暂停等）。批判二十一给出方法论区分：**资格收紧 ≠ 路线降级 ≠
方案变形**——大部分变化是"把尚未获得的资格从名称和结论中撤掉"，不是取消最终目标。
本表把每次收紧的"原目标/现状态/为什么收紧/未来怎么恢复"结构化记录，防止"以后处理"
项目无限累积失控，也防止当前近似被误当成最终资格。

**三类降级分类**（判断新一轮批判提出的"收紧"该归入哪一类）：
- **A类·纠错性撤回**：原概念本身不成立（如把无质量热转移叫平流/把边端点顺序叫
  方向/把无记忆函数叫历史依赖/把相对环境charge叫绝对热能）——**不进入"升级清单"**，
  归档为"已否决假设"防止未来重复犯错。
- **B类·能力暂缓**：有明确物理前置条件，应保留升级入口（如normalized→
  physical-anchor/过剩热量转移→质量-焓平流/外部驱动标志→驱动功账本/单点皮肤→
  多点皮肤/无记忆传输→介质内部状态/最小支撑→更大网络）。
- **C类·工程范围限制**：按需处理，不一定全部完成（地址路由/全64感温点/通用事务
  系统/全网络扩展/性能优化）。

**四个集中审查点**（不是最后一次性处理，也不是每轮都重新讨论）：
- 审查点1：P1-C通过后——世界物理资格审查（normalized模式是否继续/有序过剩热能
  转移是否足够/是否需要质量比热流率/外部驱动功是否要进账本/多点皮肤是否已阻塞/
  当前世界是否足够支撑真实发生元）；
- 审查点2：P2-A通过后——感受边界资格审查（单点皮肤是否导致发生元失真/皮肤热容
  模型是否过薄/世界到皮肤映射是否足够/发生元工作区间能否真实标定）；
- 审查点3：P2-C通过后——关系资格审查（`r≺^τ`/`r_ρ^τ`是否在真实过程下仍成立/
  关系-关系生成是否有足够结构差异/是否需要内部状态化传输引入历史依赖/是否具备
  L3条件）；
- 审查点4：首次长程验证前——债务收口（`stable_hash()`/物理量纲统一/参数标定/
  事务边界/多点扩展/性能与可复现性/长程能量漂移/结构版本生命周期）。

**登记表（8字段：原始目标/当前资格/收紧原因/保留的不变量/被删除的宣称/升级前置/
触发审查点/禁止捷径）**：

| 字段 | P1（介质平流条目） |
|---|---|
| 原始目标 | 真实介质平流 |
| 当前资格 | 有序过剩热能转移（`OrderedExcessThermalEnergyLink`） |
| 收紧原因 | 无介质质量/比热/流率/驱动功状态（`normalized`模式），只搬热能不搬质量 |
| 保留的不变量 | 有序端点（tail/head）/快照-计划-提交纪律/热量两端守恒 |
| 被删除的宣称 | 不称"流体"、不称"方向"、不称"完整热力学闭合" |
| 升级前置 | 引入 `m`（介质质量）、`c_p`（比热）、`ṁ`（质量流率）、焓账本、驱动功账本 |
| 触发审查点 | 审查点1（P1-C通过后，世界物理资格审查） |
| 禁止捷径 | 不能只把类名改回 `Advective*` 就宣称已升级——必须真正引入上述物理状态 |

**新增三条（批判二十二，2026-07-17，P1-B3提交语义的结构性边界）**——均已用代码
实测复现验证真实存在，均为休眠缺口（当前代码库无生产调用点会触发，只在P1-C引入
长期运行的联合调度器后才成为活跃风险），**归类B类·能力暂缓**（有明确物理/工程
前置条件，保留升级入口）：

**状态（2026-07-17，批判二十四）：已由 P1-C0 补丁关闭，不再属于开放债务**——
不删除以下历史论证（记录问题曾经真实存在+修复过程），"当前资格"列已更新为
修复后状态。

| 字段 | P1-B3-①状态指纹范围 | P1-B3-②提交登记表生命周期 | P1-B3-③运行实例绑定 |
|---|---|---|---|
| 原始目标 | 计划提交前检测节点状态是否仍是生成计划时的状态 | 同一计划禁止提交两次 | 计划只能提交到生成它的那个物理运行实例 |
| 当前资格（已修复） | `JointThermalStepPlan`快照覆盖charge+`_q_in`+`_q_out`三元组，覆盖世界+皮肤+热源全部参与容器 | `AppliedPlanRegistry`由`JointThermalRuntime`持久持有，调用方拿不到"全新的" | `runtime_uid`取自`JointThermalRuntime`实例自身`id(self)`（非`id(world_graph)`），计划/回执核对可拒绝内容相同但实例不同的情形（已用镜像图+双runtime场景验证） |
| 收紧原因 | 未实现前不应声称"完整状态过期检测"——实测：外部`inject(+5)`再`inject(-5)`后`charge`精确回到快照值，但`_q_in`/`_q_out`已分别变化5.0，当前检测完全无法察觉，提交照常成功 | 未实现前不应声称"结构保证单次提交"——实测：提交一次后手动复原charge到快照值，用全新空`AppliedPlanRegistry`提交同一计划，第二次提交同样成功 | 未实现前不应声称"计划已绑定运行实例"——实测：两个地址生成顺序相同（uid/revision/charge全部相同）但独立构造的镜像图，用图A生成的计划成功提交到图B |
| 保留的不变量 | 快照-比对-拒绝的检测流程本身/回滚已正确覆盖charge+`_q_in`+`_q_out`三元组（回滚范围比检测范围更完整，本身没有错） | 单次提交的检测流程本身/`is_applied()`/`mark_applied()`接口契约 | 现有revision/地址版本/charge快照检测对"缺失节点"/"节点状态不匹配"两类错误依然有效，只是无法覆盖"完全同构的独立实例"这一类 |
| 被删除的宣称 | 不称"状态过期检测覆盖完整物理状态"，只称"检测charge这一个已知会变化的标量" | 不称"单次提交由结构保证"，只称"由调用纪律保证（调用者需复用同一registry实例）" | 不称"计划已绑定到具体运行实例"，只称"计划已绑定到该实例某时刻的内容快照" |
| 升级前置 | 计划快照字段扩展为完整状态指纹（charge+`_q_in`+`_q_out`三元组或等效状态指纹哈希），覆盖世界+皮肤+热源全部参与容器 | 引入持久化运行时对象（如`JointThermalRuntime`）长期持有`AppliedPlanRegistry`，不由单步调用者临时创建 | 引入`runtime_uid`（运行时实例构造时生成的稳定标识），计划生成时记录、提交时核对 |
| 触发审查点 | P1-C0（联合计划与统一提交，这三项本身就是P1-C0要直接实现的调度契约组成部分，不是等P1-C完成后才审查的资格问题） | P1-C0 | P1-C0 |
| 禁止捷径 | 不能只在`ThermalTransportPlan`上加`_q_in`/`_q_out`两个字段就宣称已解决——必须覆盖P1-C0联合步涉及的全部参与容器（世界+皮肤+热源），否则只是把同一个漏洞平移到新对象上 | 不能声称"以后调用方记得复用同一实例"就算解决——必须由持久化运行时对象结构性持有，调用方拿不到"全新的"registry | 不能只加一个"随便生成"的id字段——`runtime_uid`必须在运行时实例构造时一次性生成、终身不变，且计划/回执的核对逻辑要能真正拒绝内容相同但实例不同的情形（已用镜像图场景验证） |

**已否决假设档案**（A类，不进入升级清单，仅存档防止重犯）：
- 把无质量热能转移叫作"真实介质平流"（P1-B0/批判十八已否决）；
- 把有序边端点顺序（tail→head）叫作"空间方向"（P1-A/批判十七/十八/十九反复强调）；
- 把`J=a·E`这类无记忆瞬时函数叫作"历史依赖"（批判十六/十七已否决"L2能区分历史"）；
- 把`ThermalCell.capacitor.charge`（相对本节点`ambient_temperature`的偏差量）叫作
  "绝对热能零点"（批判十九用代码实测证伪）。

**八条真正会导致方案变形的信号**（持续监控清单，后续每轮评判可对照自查）：
①临时机制成为唯一生产路径却无升级接口；②原始目标从V3消失，只剩当前近似；
③物理过程为了通过测试改成无状态公式；④未来阶段开始依赖当前近似的偶然数值而非
其结构契约；⑤暂缓项没有明确的重新评估触发点；⑥`normalized`参数被当成现实物理
常数；⑦P2/L3为了迁就P1的限制而改变自己的本体目标；⑧"以后处理"项目不断累积但
从未进入阶段性资格审查。
