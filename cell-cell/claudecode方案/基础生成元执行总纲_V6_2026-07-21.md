# 基础生成元执行总纲 V6（接力方案，2026-07-21 创建）

> **拆分规则（用户 2026-07-17 反馈）**：每完成 3 个"小阶段"（子门级，如
> B0/B1/C0/C1）或完成 1 个"大阶段"（P1/P2/L3 这一级）之后，把当前接力方案
> 冻结为历史存档，开一份新接力方案继续，减少单份文档token消耗。**V5 自本行
> 起冻结**——自 V5 创建以来已完成 3 个小阶段（P2-A0 / P2-A 生成元核心 /
> P2-A1a），触发"3个小阶段"这条拆分规则。V5 保留供追溯这三个小阶段的具体
> 实施过程（更早历史见 V4/V3/V2）。**本文件（V6）只保留当前有效结构**。

**当前状态（2026-07-21）**：P1（大阶段）已冻结 → 世界物理资格审查（审查点1）
已通过 → P2-A（真实发生元）已启动：**P2-A0、P2-A 生成元核心、P2-A1a（生成元
核心输入包络扫描）均已完成**（首轮粗网格已跑出真实数据，发现响应曲线为非
单调带通形状，深查根因留待后续，不阻塞下一步）。**下一步：P2-A2（独立三点
动态皮肤）**，是当前唯一活跃阶段。

## 项目共同规范（不随阶段变化，详见 CLAUDE.md / `nexus_v1/RULES.md`）

- 结构优先，禁语义硬编码（不预设方向/事件/成功失败标签，行为从结构涌现）；
- 不改母本代码，新功能走独立文件/子类叠加；
- 每个新组件强制三问（Q1生物物理对应物/Q2物理结构Sources→Bundle→Targets/
  Q3参数依据）；
- Model before tune，参数需要推导或EXP标注来源；
- 差分评判协议：每轮评判只写结论/新问题（≤1阻塞+2非阻塞）/下一步，不复述
  历史推导；一轮只审一个阶段；报告用固定六段式（目标/实际改动/发现的真实
  问题/测试结果/冻结契约/未完成项）；
- 文档拆分节奏：每3个小阶段或1个大阶段切一份新接力方案（本文件即为例证）。

## 一、已冻结底座（不再修改机制，只在新物理输入下重新标定工作区间）

| 阶段 | 状态 | 核心交付物 | 详细论证 |
|---|---|---|---|
| T0~T1 | 已冻结 | `r≺^τ`时间关系原型 | V2/V3 |
| T3-B~C2 | 已冻结 | `r_ρ^τ`射影比例关系（`GradedPotentialRelay`+DN共享池） | V2/V3 |
| W0~W2A/L2 | 已冻结 | 局部热状态网络+世界定位热源+关系微分只读接口 | V3 |
| P0/P1-0 | 已冻结 | 世界-皮肤联合调度+接触边稳定性 | V3 |
| **P1（大阶段）** | **已冻结** | 身份拓扑契约 + 有序过剩热能转移 + 联合调度 + 三级账本/图同构 + 物理动力学长程资格 | V3/V4 |
| **P2-A0 + 生成元核心** | **已完成** | 轨迹身份核对+皮肤地址接通；十神经元句柄抽取（`nexus_v1/generators/`包：`BaseGenerator`/`OccurrenceClosure`/`GeneratorTrajectory`）；χ≠𝔫 | V5 |

**核心文件**：`nexus_v1/components/{dynamic_thermal_field,thermal_source_coupling,
skin_thermal_contact,structural_address,ordered_excess_thermal_energy_link,
thermal_transport_plan,joint_thermal_step_plan}.py` + `nexus_v1/generators/
{occurrence,base_generator,trajectory,input_envelope}.py`。

## 一.5、审查点1：世界物理资格审查（已执行，批判二十六/二十七，2026-07-17）

**回答的3个问题**：
1. **完整联合动力学是否具有足够步长收敛性？**——**是**（T-WQR-1）。
2. **不同真实物理耦合能否形成可区分且可追踪的皮肤轨迹？**——**否，
   已用反例实测坐实**（T-WQR-2/T-WQR-3，皮肤侧轨迹相对差异均为浮点精度
   量级，单点皮肤把不同世界过程压平成不可区分的轨迹）。
3. **当前normalized世界是否已足够支撑ξ^occ？**——**足够，但有明确边界**：
   P2-A只需单点局部发生检测，不要求区分"由哪些世界节点、什么次序共同
   造成"这类多源组合信息。

**3项独立裁定（均"带债务通过"）**：质量-焓平流 / **多点皮肤**（触发条件
已被反例实测坐实，裁定时限收紧为"P2-A完成后的感受边界资格审查（审查点2）、
进入P2-B之前"必须裁定——**P2-A2 正是回应这条债务的具体建设**）/ 驱动功·
熵账本（结构/行为算子资格门前复审）。

**审查结论**：三项裁定均为"带债务通过"，**审查点1通过**，P2-A可以启动。
测试：`test_world_physical_qualification_review.py`（T-WQR-1~3全部PASS）。
报告：`世界物理资格审查_2026-07-17.md`。完整论证见 V5。

## 二、当前执行主线

```
P2-A（真实发生元，当前阶段） → P2-B（回接关系原语） → P2-C（关系-关系生成）
  → 结构/行为算子资格门 → Ω原型或L3决策
```

### P2-A（P2-A0/生成元核心/P2-A1a已完成，P2-A2/A1b/A3待启动）：动态皮肤 → 真实发生元

链路：`world → contact → skin state → ξ_i^occ`。**ξ_s^occ资格边界（强制
约束）**：`ξ_s^occ`只能表示皮肤支撑上的聚合局部发生，不能表示上游物理
组成，不得靠后续神经结构"猜回"已验证不可区分的差异。

**已完成（P2-A0 + 生成元核心，2026-07-18~20）**：详见 V5——轨迹身份核对+
皮肤地址接通（T-P2A0-1~7）；十神经元句柄抽取+D_i^sim输入端口+trigger/
exit/rearm闭合状态机+Λ^phys轨迹记录器（T-P2AG-1~10）。报告：
`P2-A0_轨迹身份核对与皮肤地址接通_2026-07-18.md`、`P2-A_生成元核心_
单点真实发生元构造与标定_2026-07-20.md`。

**执行顺序修正（评判：`评判_P2A1顺序倒置修正_2026-07-21.md`）**：原
"P2-A1→P2-A2"顺序倒置（P2-A1标定`κ·q_skin`，但`q_skin`是P2-A2才建立
的信号），拆分并重排为 **P2-A1a → P2-A2 → P2-A1b → P2-A3**。

#### P2-A1a（已完成，2026-07-21）：生成元核心输入包络扫描

只用现有 `BaseGenerator.feed()` 扫描输入端口 `u`，不涉及皮肤/κ/q_skin，
回答"十神经元生成元本身能消费什么范围的输入"。新增
`nexus_v1/generators/input_envelope.py`（`InputEnvelopePoint`纯测量记录+
`scan_input_envelope()`，不做任何边界分类，分类留在调用方）+
`exp_P2A1a_input_envelope_scan.py`（13档粗网格首轮扫描）。T-P2AG-11/12
（工具机制验证）+ 既有T-P2AG-1~10 共12/12 PASS，全量回归21/21 PASS。

**真实发现（重要，非阻塞但需后续处理）**：实测响应曲线**不是单调的，是
带通（band-pass）形状**——u∈[0.0005,0.1]区间才有真实活动，u≥0.2时完全
静默（不是"持续高位不退出"式饱和）。`peak_pre_trace`（S_sat操作化定义）
区分度差，一旦触发即顶到~1.0不再随u升高。网格下限（0.0005）可能未覆盖
真正的u_silent（已勉强触发1次）。**用户已确认深查根因留待后续，不阻塞
P2-A2。** 报告：`P2-A1a_生成元核心输入包络扫描_2026-07-21.md`。

**下一步**：**P2-A2**（独立三点软件皮肤，复用`dynamic_thermal_field.py`
的`ThermalFieldGraph`建立`s1-s2-s3`，产生真实`q_i^skin(t)`，同时补驱动权
归属守卫`MANUAL_CALIBRATION`/`WORLD_COUPLED`防止`feed()`/`circuit.step()`
静默双重驱动）→ **P2-A1b**（皮肤-生成元接口标定：`κ_i`/offset/clipping/
`θ↑`/`θ↓`/`t_rearm`，替换本轮占位）→ **P2-A3**（成对时序分辨率标定）→
审查点2（裁定三点支撑是否足以进入P2-B，此时需明确"在线状态转换流"与
"完成后的`Occurrence`记录"两类接口区分——见`评判_P2A1顺序倒置修正_
2026-07-21.md`§二）。

### P2-B（P2-A通过后）：真实发生回接关系原语

让真实发生进入`r≺^τ`/`r_ρ^τ`——验证原语机制不改，只重新标定输入工作区间。
`document - 2026-07-21T085443.454.md`提出的`𝒩^(0)→𝒞^(1)`递归自然化定位
（见`评判_P2A核心确认与生长机制来源存疑_2026-07-21.md`）可作为设计输入，
不阻塞当前P2-A工作，留到进P2-B前处理。

### P2-C（P2-B通过后）：首次关系-关系生成

**信号域限制**：`GradedPotentialRelay`/共享分流池吃非负活动量，只有有序
过剩热能转移通量`J_e^oet=a_e·u_tail^+`天然非负。首轮 P2-C 只能用同一节点
三条真实有序传输出边构造`r_{ρ,J}^τ`（复用已冻结的三元共享池），不接带
符号的扩散/接触通量。

### 结构/行为算子资格门（P2-C通过后，L3决策之前）

**核心判据**：`Γ_with,ρ^+ ≁ Γ_without,ρ^+`。**范围声明**：这是P2-C之后
（至少3阶段之后）的路线记号，不要求现在设计具体验证方法。**存疑项**：
`document - 2026-07-21T085443.454.md`提出的"G0-G4地址化生长机制"插入
本资格门之前——**来源查无出处，用户与我方均无法确认"你刚刚恢复的"具体
指什么，按待验证处理，不采纳进执行主线**（见
`评判_P2A核心确认与生长机制来源存疑_2026-07-21.md`）。

### L3 决策（P2-C通过后再评估，门槛已细化）

不仅要求"P0+P1+P2都通过"，还需满足6项条件（连续物理过程无外部事件分段
标签/关系状态来自真实发生和真实输运/不同物理过程能形成不同关系轨迹/
区分不依赖坐标或预设类别/候选事件边界来自关系延拓破缺/地址和拓扑审计可
追溯真实物理支撑）。达到后才决定是否建设结构诱导事件原型，不预先承诺。

## 三、暂停项（不在当前主线，需要单独重新确认才能恢复）

- 旧 T2（`r_edge-lag`方向生成元）/ M2（运动方向关系）路线；
- T4 旧双轨（温感轨+运动轨）并行集成；
- 感温点从最小支撑向全 64 点扩展；
- `MotionState.polarization` 硬编码替换；
- 口器坐标 / 自我坐标类组件；
- P/R、Xin、Motor 与本项目关系原语的正式回接；
- 运动轨 M0（任务#3，温感轨核心交付物已冻结，尚未启动，与世界模型P1~P2无
  直接依赖，可并行考虑但需用户重新确认优先级）；
- source正式接入`StructuralAddress`仍暂停（P2-A当前范围只需要皮肤支撑
  地址，热源地址留到真正需要时再接入）。

## 四、报告存档索引（按时间倒序，仅列 V6 创建后的新报告；更早历史见 V5/V4/V3 索引）

- P2-A1a（生成元核心输入包络扫描）：`cell-cell/工作报告/P2-A1a_生成元核心输入包络扫描_2026-07-21.md`
- P2-A1顺序倒置修正（交叉比对评判）：`cell-cell/交叉比对/评判_P2A1顺序倒置修正_2026-07-21.md`
- P2-A 核心确认+G0-G4来源存疑（交叉比对评判）：`cell-cell/交叉比对/评判_P2A核心确认与生长机制来源存疑_2026-07-21.md`
- 更早历史（P2-A0、P2-A生成元核心、P1及更早）：见 V5/V4/V3 存档索引章节。

## 五、资格与升级登记表（方法论沉淀，持续有效）

**用途**：区分"资格收紧"（把尚未获得的资格从名称/结论中撤掉）与"路线降级/
方案变形"（取消最终目标）。防止"以后处理"项目无限累积失控，也防止当前近似
被误当成最终资格。

**三类降级分类**：
- **A类·纠错性撤回**：原概念本身不成立，不进升级清单，归档"已否决假设"；
- **B类·能力暂缓**：有明确物理/工程前置条件，保留升级入口；
- **C类·工程范围限制**：按需处理，不一定全部完成。

**四个集中审查点**：审查点1（P1整体完成——世界物理资格审查，已执行）/
**审查点2（P2-A通过后——感受边界资格审查，即将在P2-A2/A1b/A3完成后触发）**/
审查点3（P2-C通过后——关系资格审查）/审查点4（首次长程验证前——债务收口）。

**已否决假设档案**（A类，不进入升级清单，仅存档防止重犯）：
- 把无质量热能转移叫"真实介质平流"；
- 把有序边端点顺序（tail→head）叫"空间方向"；
- 把`J=a·E`这类无记忆瞬时函数叫"历史依赖"；
- 把`ThermalCell.capacitor.charge`叫"绝对热能零点"；
- **G0-G4地址化生长机制的来源归属**（未确认为真实先前讨论，按待验证处理，
  不算已否决，但同样不进入升级清单，见上方"结构/行为算子资格门"小节）。

**当前开放的B类条目**：质量-焓平流（带债务）/多点皮肤（带债务，触发条件
已被反例实测坐实，**P2-A2正是回应此债务的建设**）/驱动功·熵账本（带债务，
结构/行为算子资格门前复审）。

**八条真正会导致方案变形的信号**（持续监控清单，每轮评判可对照自查）：
①临时机制成为唯一生产路径却无升级接口；②原始目标从文档消失，只剩当前
近似；③物理过程为通过测试改成无状态公式；④未来阶段依赖当前近似的偶然
数值而非结构契约；⑤暂缓项无明确重新评估触发点；⑥`normalized`参数被当成
现实物理常数；⑦P2/L3为迁就P1限制而改本体目标；⑧"以后处理"项目从未进入
阶段性资格审查。
