# 基础生成元双轨实施总纲 V2（已纳入八份交叉比对批判 + 执行修正 + 世界模型重构立项）

## 执行状态（2026-07-16 更新，第十六节 W2A 已完成：世界定位+动态热源守恒注入）

**W2A 已完成**（详见 `cell-cell/工作报告/世界模型重构W2A_世界定位与动态热源守恒注入_2026-07-16.md`）：
新增 `nexus_v1/components/thermal_source_coupling.py`（`ThermalFieldLocator`反距离加权定位/
`DynamicHeatSource`功率型有限储备热源/`couple()`组合注入/`guarded_step()`fail-fast包装，
不改 `ThermalFieldGraph.step()` 本体）；`dynamic_thermal_field.py` 的 `DEFAULT_KAPPA_0` 命名
拆分为 `SEAWATER_DIFFUSIVITY_REFERENCE`/`NORMALIZED_KAPPA_DEFAULT`（保留兼容别名）。T-TSC-1~6
6/6 PASS，W1/W1.5（T-DTF-1~7）同步复核 7/7 PASS，全量回归 21/21，契约 13/15 不劣于父提交。
**W2B（皮肤热交换）仍需再次向用户确认，不在本次提交范围。**

## 执行状态（2026-07-16，第十六节：批判八评判——总纲清理已批准+W2A已启动）

**第八份交叉比对批判**（`交叉比对/document - 2026-07-16T221345.668.md`）确认 W1.5 已完成、
可进入 W2，指出总纲正文 4 处过时表述（禁新造类旧措辞/契约门槛写死15/15/T2主流程图未更新/
`DEFAULT_KAPPA_0` 命名混淆机制参数与物理参数），并给出 W2A→W2B→W2C 三阶段拆分设计
（世界定位+动态热源守恒注入 → 皮肤热交换 → 联合调度）。**我方评判：4 处清理技术判断准确，
已核实属实，采纳；W2A/B/C 设计质量高，采纳其拆分顺序；但批判把 T3′ 归入"等世界重建完
才做"与第十四节已明确的"R2(T3)与世界模型重构互不阻塞、可并行"矛盾，需澄清不采纳这个
暗示。用户已确认：4 处清理现在做 + 立即启动 W2A（不接皮肤）。**详见第十六节。

**第七份交叉比对批判**（`交叉比对/document - 2026-07-16T215047.390.md`）审查了已完成的 W0+W1
实现（commit `d19769f`），结论"不是世界模型立意未解决，而是生产接口尚未闭合"，总体正面，提出
"W1.5"四项收口建议。我方评判：技术判断大部分成立（量纲未闭合/球壳拓扑不应做生产空间/数值稳定
不等于物理合理三点均核实无误，通量方向措辞需修正），但批判把 `ThermalFieldLocator`/
`DynamicHeatSource`（实质是第十四节明确列为 W2、需重新确认才能开始的身体-世界耦合设计）包进
"W1.5"、建议不再征询直接做，**我方不同意这个绕过方式**，已向用户说明。**用户确认：只做真正
属于 W1 收口的 3 项（量纲标注/数值稳定守卫/球壳夹具警示），其余两项（ThermalFieldLocator/
DynamicHeatSource）留待 W2 重新确认时再处理。**详见第十五节。

## 执行状态（2026-07-16，第十四节：世界模型重构已立项，用户已选定"按批判重构 W0~W5"）

**第六份交叉比对批判**（`交叉比对/document - 2026-07-16T201948.954.md`）指出：我方"方向A"
（身体运动学扫掠）实际上让 T2 变成了**温感×前庭跨模态生成元**，与方案"温感轨/运动轨独立、不建
跨模态 Bundle"的原则冲突；根源是当前世界模型 `temperature_at()` 是瞬时查询函数、没有状态/通量/
耗散过程，无法支撑"纯温感传播"这类生成元。批判建议把世界模型从"查询式"重构为"状态式过程世界"
（局部热状态网络 + 扩散/介质搬运 + 皮肤-世界热交换），分 W0~W5 五阶段。

**我方独立评判**：诊断成立（方向A确实是跨模态，是我方"方向A"设计的真实漏洞），但"立即把世界
模型重构提升为 basegen 工程前置主任务"这个处方在我方评估中过于激进（违反"最小支撑"一贯纪律、
影响范围覆盖全项目热感知/热趋性系统、多个已验证系统需重新验证）——**已如实向用户说明此保留
意见**。**用户明确选择"按批判建议重构整个世界模型（W0~W5）"，覆盖我方的保留意见，此为用户
最终决策，本节按此决策制定执行计划。**详见第十四节。

**新发现（本轮，未经批判文档指出，我方独立核查）**：世界模型 `World.temperature_at(pos)`
（`components/world.py`）对给定世界坐标的温度是**瞬时确定的，没有传播速度/时间项**。这意味着
T2 报告依赖的物理假设——"球面相邻两点间存在真实空间传播延迟，`delay=d_ab/v_edge`"——**在当前
世界模型里不成立**。两点先后感受到温度变化的时间差，实际由**身体自身转向/平移运动学**决定
（把固定在 body-frame 的采样点先后带入温度梯度区），与两点间局部距离/所谓"传导速度"无关。
详见第十三节，含修正方向建议。**这是比第十二节 12.2 第3点更深层的问题**——不只是升格判据不
可操作，是 T2 当前设计的核心机制（delay_steps 按几何距离标定）本身缺乏物理基础，需要重新
定向后才能进入 R3 编码。

温感轨 T0（完整代码+报告）、T1（完整代码+报告）已构建并提交（分支 `feat/thermal-rprec`）；
T2（仅设计报告，未编码）、T3（仅路径判别实验，未构造完整生成元）已完成，按用户确认的范围
"T1完整实施，T2/T3只出前置报告"执行。第十一节《批判三执行修正清单》的 8 项修正已全部完成。

**第四、五份交叉比对批判**（`交叉比对/document - 2026-07-16T193223.428.md` + `...T193317.012.md`）
分别提出"放宽禁新增组件约束、按机制缺口准入"（批判四）与"四点具体修正+落地顺序重排"（批判五）。
已逐条独立评判，含 1 处代码实测验证（`dt=0` 只读共享池方案，5 项检验全部精确通过），详见
第十二节《批判四/五评判与执行修正》。**结论**：批判五四点全部采纳（其中 T3 同步池实现有更优
方案，需再次更新报告代码）；批判四核心原则（机制缺口准入制）采纳，但"立刻建四个新组件"的
执行顺序不采纳——改按批判五的"先用现有接口验证，撞到真缺口才新建"顺序。**尚未落地任何代码
改动，需用户确认后执行。**

## Context

用户提供《基础生成元》总纲（`cell-cell/交叉方案/基础生成元.md`）+ **两份**交叉比对批判稿：
- 批判一（`交叉比对/document - 2026-07-16T150111.470.md`）：指出 v1 的 6 处结构缺陷 → 催生 v2 主体。
- 批判二（`交叉比对/document - 2026-07-16T151133.148.md`）：确认「v2 可定为总纲 V2，不需重写整体路线」，追加 4 项细化修订 + 2 项补充。

本文件为整合两份批判后的 **V2 定稿**，作为真实可执行的双轨落地总纲。

**批判一 → v1 的 6 处结构缺陷（已修正）**：
1. 「双轨并行」易被误解为在 `VariantCircuit` 同时加两套 Phase 0-4 → 改为**设计并行、实施分轨串行，温感轨优先**。
2. Phase 3「替换 polarization」与「终止于 Collector 不接下游」自相矛盾 → 拆分，实置换移出 0-4。
3. 未限定每阶段 ξ 基数（64 源 → C(64,2)=2016 对爆炸）→ 改为**最小支撑**。
4. 运动 r→ 用 move_x↔y↔z 轴链无物理根据 → M2 暂缓（主线）/ 降格对向原型（可选）。
5. ξ 术语三义混淆 → 写入**代码命名**（ξ^occ / ξ^err / ξ^PI）。
6. 全网扩展 + 硬编码替换 → 移到 Phase 0-4 之后的独立阶段。

**批判二 → 4 项细化修订 + 2 项补充（已纳入）**：
7. **T2 收紧**：方向≠Bundle 命名，必须「传播痕迹 + 终点耦合」三要素（详见第四节 T2）。
8. **T3 降格**：`synapse_gain=-1` 是减法抑制 ≠ 除法归一化，首版命名 **r_part（参与竞争关系原型）**，两级验收，实测后才升格 r_ρ（详见第四节 T3）。
9. **关系层独立账本**：排除全局 census 是暂时隔离、非无能耗，Phase 0 建独立统计，Phase 4 强制验收（详见第七节约束 8）。
10. **frozen 权重远离饱和**：`0<w_0=w_max<1`，首轮扫描 `[0.1,0.5]`，禁 `=1.0`（与 `memory: project_memristor_saturation_edge_bug` 一致；详见约束 2）。
11. **T0 选点冻结保存**：点索引/极性/邻接边/理由/峰值周期落盘，T1-T4 只读冻结集合（详见 T0）。
12. **门控结构** Gate A→D（详见第一节）。

**我方对批判二的独立评判（非照单全收，已代码核实）**：
- 四项修订 + 两补充**方向全部成立，采纳**。
- **但批判二点8 前提有误**：项目**有** divisive normalization 物理组件（`DivisiveNormalizationReceptor`，Carandini-Heeger 分流抑制，非 Python 除法）。批判"无除法组件"不准确。降格 r_part 的结论仍成立，但**真正原因**是现有 DN 为 per-neuron 时间增益、非跨通道横向池——据此在 T3 增设「路径 B：共享池分流抑制」逼近真占比（详见 T3）。
- **补充 A（T2）**：r→ 与"恰好邻接的 r≺"的区别不只是邻接，须是传播支路的**空间不对称性**（详见 T2）。
- **补充 B（frozen 权重）**：`禁=1.0` 的对象是**有效写入权重**，几何衰减的 base=1.0（有效值<1）可接受（详见约束 2）。

**已确认决策**：运动 M2 = 两案都写进计划（暂缓为默认主线，对向原型为可选分支）；实施顺序 = 温感轨优先。

---

## 一、核心原则：设计并行，实施分轨串行

```
共同总纲（术语/组件约束/census/frozen/报告格式/测试门槛）
   ├── 温感轨  T0 → T1 → T2 → T3 → T4      ← 首轮实施
   └── 运动轨  M0 → M1 →（M2）→ M3 → M4    ← 温感轨跑通后再切入
```

- **两轨文档设计可并行**，但**代码实施分轨串行**：绝不在同一未验证提交中同时给 `VariantCircuit` 加两轨。
- **温感轨优先**：已有 64 个明确 ξ^occ、RC 动态已测、不涉及运动侧语义选择、不影响现有 Motor/polarization。
- **每 Phase 独立分支**（非一个 `feat/base-generators-all`；分支命名已按批判三/五修正——`-rdir`→`-redge-lag`（呼应 T2 命名降级），`-rrho`→`-rpart`（呼应 T3 命名降级），2026-07-16 追加）：
  ```
  feat/thermal-basegen-p0 / -rprec / -redge-lag / -rpart / -integration
  feat/motion-basegen-p0  / -rprec / -rpart / -integration   （M2 独立分支待定）
  ```
- 每 Phase 专项探针 + 全量回归 21/21 双通过才进下一 Phase。回归失败可精确归因到单轨单阶段。

**门控结构（纳入批判二点12，温感轨适用，运动轨走同款）**：
```
共同总纲
    │
    ▼
Gate A：T0 真实 ξ^occ 选点、冻结与关系层账本核实
    │
    ▼
Gate B：T1 时间关系电路（可直接按方案设计进入）
    │
    ▼
Gate C：T2 方向传播—终点耦合定义（须先出电路定义报告，方向≠Bundle命名）
    │
    ▼
Gate D：T3 竞争参与度可否达到占比近似（r_part → 二级验收 → 是否升格 r_ρ）
    │
    ▼
T4 并行集成（关系层账本强制验收 + 决定 r→/r_part 资格）
```
未通过对应 Gate 不得进入下一 Phase。

---

## 二、术语与命名规范（写入代码，不止写报告）

三个「ξ」是不同工程对象，必须在**代码命名 + 注释**中区分：

| 符号 | 工程对象 | 含义 | 本方案用法 |
|---|---|---|---|
| **ξ^occ** | 感温/运动 Collector（尖峰发生检测器） | 局部物理发生 | 本轮双轨的 ξ 源 |
| **ξ^err** | 误差 Neuron/Collector（pred−actual） | 预测—实际残差发生 | 运动路线2，后续独立构建 |
| **ξ^PI** | `BundleConfig.xin_tension` | Bundle PI 张力参数 | 禁止直接读取喂生成元 |

- 新生成元命名前缀统一体现 `occ`（如 `occ_rprec_*`、`occ_rpart_*`），避免与 `xin_tension` 混淆。
- 报告与注释中一律用上表符号，不再泛称「ξ」。

**关系生成元命名（纳入批判二点8）**：占比关系首版一律命名 **r_part / 参与竞争关系原型**（代码前缀 `rpart`），**不得**在实测「共同尺度近似不变」前提前叫 `r_ρ` 或宣称除法归一化。达到二级验收后才升格 `r_ρ`（重命名 + 报告确认）。

---

## 三、运动侧 ξ 源路线（路线1 优先，路线1/2 并存非替换）

**核心事实**（已代码核实）：Bundle 只能读 Neuron 的 `pre_trace`/`activation`/`calcium_rate`（`bundle.py:243-256`）；`xin_tension` 是 `BundleConfig` 上的 float（`bundle.py:44`），Bundle 读不到 → 路线3（直读 xin_tension）绕 Bundle = 硬编码违规，不采用。

**首轮走路线1（运动局部发生生成元 ξ^occ）**：以 `move_x/y/z`、`yaw_ccw/cw` 的 spiking `pre_trace` 为 ξ^occ 源。零新结构，与温感轨 ξ^occ 口径严格对称。

**重要更正（纳入批判点3）**：路线1 的准确表述是**「运动局部发生生成元轨」**，不是「真 ξ 预测残差轨」。路线2（ξ^err，用已有 `err_dT/err_domg` 范式包 efference error）表达的是**不同的物理对象**：
- 路线1（ξ^occ）：运动**发生了什么**；
- 路线2（ξ^err）：预测与运动之间**破缺了什么**。

二者**未来可能并存，而非必然替换**。路线2 作为后续独立构建，不称为路线1 的「升级」。

---

## 四、温感轨 T0 → T4（最小支撑，不铺满 64 源）

**总目标不是「把所有 ξ^occ 都接入」，而是：证明一套最小基础关系生成元能由真实 ξ^occ 稳定产生。** 全网络扩展是后续独立阶段。

**执行顺序修正（2026-07-16，纳入批判五）**：本节 T0→T1→T2→T3→T4 的**编号顺序**保持不变（Phase 定义/验收标准仍按此编号组织），但**实际代码落地顺序**改为第十二节 12.3 定义的 R0→R1→R2→R3→R4——**T3（r_part/r_ρ）先于 T2（r_edge-lag）落地**，理由见第十二节：T3 的物理目标已更清晰（路径A淘汰、路径B机制已验证闭合），T2 还卡在升格判据这类定义问题的收口上。落地时以第十二节的顺序为准。

**当前流程状态（2026-07-16 更新，纳入批判八点3）**：T2（r_edge-lag）**已暂停**——原物理前提（热传导延迟）已被第十三节撤销，"方向A"重定向被第十四节确认为跨模态生成元、不再算温感轨内部的 r→，正式的纯温感方向关系需等新世界模型（W3+）产生真实局部通量后才能重新设计（届时归入第十四节 W5 的 `r_flux`）。**T0/T1-B1 保持已完成状态；T3(R2) 不受此暂停影响，与世界模型重构互不阻塞、可并行**（第十四节 14.5 已明确写下这条），批判八的流程图把"T3′"归入"新世界回接"是指**用新世界重新验证**这一未来动作，不代表当前 T3(R2) 被阻塞——这个区分需要保留，不能让流程图暗示 T3 停摆。当前实际流程：
```
旧温感轨：T0 → T1-B1 完成；T1-B2 真实输入验收暂停（等新世界，或维持旧世界单独验收均可，未强制二选一）
世界重构：W0 → W1 → W1.5 完成 → W2（已启动，见第十六节）→ W3 → W4
T3(R2)：不受阻塞，可随时独立推进（尚未开始正式编码）
新世界回接（未来）：T0′ → T1-B2′ → T3′ → T_flux → T4′（用新世界重新验证，非当前 T3 的前置条件）
```

复用 Ω 层的 `_init_*`/`_step_*`/config-factory 范式（`variant_adapter.py:1791-1908`）与探针模板（`tests/test_thermal_coupling_generators.py`）。

### T0 —— 基线与夹具隔离（不新增关系电路）【Gate A】
- 核实并选定 2~3 个真实感温 ξ^occ（从 `thermal_quantum_collectors` 中按 `skin_network.py` 球面点局部邻接挑选）。
- **选点结果必须冻结落盘**（纳入批判二点11）：保存 ① 点索引 ② warm/cool 极性 ③ 邻接边 ④ 选点理由 ⑤ 各点输入峰值与爆发周期。T1~T4 **只读该冻结集合**，不得每次初始化重新动态搜最近邻（否则不同运行支撑不同，破坏可重复性）。存为常量表或 JSON 夹具。
- 统一读取方式（`pre_trace`），隔离 `SpikeGenerator`（仅夹具，验证后替换真实 ξ^occ）。
- 建立通用**峰值/衰减/静息**探针 + RC 标定夹具（照 `_diag_omega_burst.py`），**逐生成元独立电容常量**（禁复用 ξ 层 `C=0.5`）。
- **建立关系层独立账本**（纳入批判二点9）：新增专项统计（先在测试中按保存容器统计，或加 `get_relation_generator_neurons()`/`get_relation_generator_bundles()`），报告 ① 关系中继神经元数 ② 关系 Collector 数 ③ 每步膜积分调用次数 ④ 峰值功率/近似能耗 ⑤ 新 Bundle 数 ⑥ 相对原系统运行成本增量。**明确：排除全局 census 是暂时隔离，不是无能耗。**
- **核实 `delay_steps`**（纳入批判一§六）：即使字段存在（`bundle.py:266-269`），需核实 ① 是否对当前 Bundle 类型生效 ② 延迟队列存 `activation` 还是最终电流 ③ 多源 Bundle 延迟单位 ④ 是否与 `pre_trace` 二次时间扩展 ⑤ 是否进入 census/复制。**结论前，T1 主方案锁定为非 spiking Neuron 的 RC 历史痕迹**，`delay_steps` 仅用于 A/B 对照探针。
- 确认回归基线 21/21。

### T1 —— r≺ 时间关系（二元，最小支撑）
- **只选 2 个真实 ξ^occ**（ξ_a, ξ_b），完成一个二元时间关系生成元 r≺^ab。
- 双向结构：`ξ_a→历史痕迹支路(非 spiking Neuron + 膜 RC)─┐ + ξ_b─┘→ ξ_a≺ξ_b Collector`，反向对称。
- 多时间尺度 + 独立 Collector，暂不接下游。

### T2 —— r→ 方向关系（三节点局部邻接链）【Gate C：先出电路定义报告】
**编码前必须先单独出一份「方向关系电路定义报告」**（纳入批判二点7）。核心：**方向不是 Bundle 名称**——把 Bundle 命名为 a→b 不会让它自动成为方向关系；若只建 `ξ_a→D_a→b`、`ξ_b→D_b→a`，输出只能证明「哪个源激活」，退化成源身份检测。

方向检测必须**同时**满足三要素：
1. **结构邻接**：(a,b) 确实相邻（用 T0 冻结的邻接边）；
2. **起点痕迹**：ξ_a 的活动沿 a→b 支路传播（传播中继/RC 痕迹 `P_a→b[ξ_a](t)`）；
3. **终点耦合**：传播痕迹与 ξ_b 的**当前**活动在终点耦合。

最小结构（每条邻接边双向）：
```
ξ_a → a→b 传播中继/RC痕迹 ─┐
                            ├→ D_a→b Collector    d_a→b(t) = C(P_a→b[ξ_a](t), ξ_b(t))
ξ_b ────────────────────────┘
```
- 与 r≺ 的区别（我方补充，批判未点破）：仅「邻接」不足以区分 r→ 与「恰好邻接的 r≺」——若传播支路与 r≺ 历史支路用同一种**对称** RC 机制，两者无法区分。真正的区别须是：**r→ 的 a→b 与 b→a 传播支路物理不对称**（方向性的延迟/衰减，体现空间传播），而 r≺ 的两条历史支路对称（只比先后）。此点写入 T2 定义报告。
- **运行时方向不得靠坐标计算**（禁 `sign()`/点积/坐标差/写 ±X 标签），方向从「传播痕迹 × 终点发生的重合」物理涌现。
- 不复用温感 Ω 的 RF collector 冒充（那是预设方向模态）。

### T3 —— r_part 参与竞争关系（先二元后三元；升格 r_ρ 需实测）【Gate D】
**首版命名 r_part（参与竞争关系原型），不叫 r_ρ**（纳入批判二点8，结论保留）。但**前提须纠正**（我方代码核实）：批判称「项目无精确除法组件」不准确——`DivisiveNormalizationReceptor`（`compensation.py:295-324`）已实现 Carandini-Heeger 除法归一化 `R_i=L_i^n/(σ^n+Σ_j L_j^n)`，且是**分流抑制物理电路，非 Python 除法**（`I_eff=I_raw×σ/(σ+V_pool)`，复用 Capacitor 池），已在 shadow enc 使用（`shadow_sandbox.py:52`）。**局限**：它是 per-neuron 独立池（`shadow_sandbox.py:537`），追踪单神经元自身输入的时间平均（增益控制），**不是**跨通道横向池 Σ_j——这才是"降格 r_part"结论仍然成立的**真正原因**。

因此 T3 设两条候选路径，设计报告须先择定：
- **路径 A（减法抑制，保守，批判原方案）**：`ξ_a→独立兴奋通道─┐ + ξ_b─┘→共同总量池→共享减法抑制(synapse_gain=-1.0)→ y_a/y_b`。产生 `y_a≈φ(I_a−λI_Σ)`，与除法归一化不天然等价（共同放大 αI 时可能整体增大/饱和/归零）→ 只能停在 r_part。
- **路径 B（分流抑制，逼近真占比，我方新提）**：把「共同总量池」接 **DN 式分流** `×σ/(σ+V_pool^shared)`（V_pool 来自 a、b 共享横向池，而非 per-neuron），物理逼近 `I_a/(σ+ΣI)`，仍是合法分流抑制电路（非 Python 除法）。**需核实**现有 `DivisiveNormalizationReceptor` 能否复用为共享横向池版，或需在 T3 用一个被 a/b 共同驱动的 pool 神经元 + 各通道 shunting 搭建。路径 B 有望直接达二级验收，值得优先试。
- 两路都**禁** Python 直接算 `I_a/(I_a+I_b)`。先二元后三元（二元通过才扩展三元）。
- **两级验收**：
  - **一级（必须达到）**：较大输入→较大输出 / 交换输入则输出交换 / 单输入时对应通道占优 / 等强输入输出近似相等 / 静默时无虚假占比。
  - **二级（实验性目标）**：共同放大时排序保持 / 输出差异对共同尺度相对稳定 / 多通道竞争后近似归一化 / 输出总量变动在有限范围。
- **只有二级（共同尺度近似不变）经实测证明后，才升格重命名 r_ρ 并在报告确认**；否则一律停在 r_part。

### T4 —— 三路并行无串扰（同一最小支撑）
- 用 **T1~T3 的同一最小支撑集合**（不扩展到全皮肤网络），同一组 ξ^occ 同时接 Ω≺（r≺）/ Ω→（r→）/ Ω_part（r_part）。
- 验证：单 ξ^occ 能否被三类 Bundle 同时读取 / 是否对上游 Collector 形成意外负载 / 三类关系互不替代 / 三类 RC 无串扰 / census 完整 / frozen 权重不变 / 全量回归通过 / 运行成本可接受。
- **关系层独立账本列为强制验收项**（纳入批判二点9）：报告关系层神经元/Collector/膜积分次数/近似能耗/运行成本增量，确认隔离≠零能耗。
- **在 T4 才决定** r→、r_part 是否达到「基础方向生成元」「基础占比生成元」资格（r_part 达二级验收才升格 r_ρ）。
- **仍不接** Γ/Motor/Shadow/TOPR/Xin/STDP/DA/CPC。

---

## 五、运动轨 M0 → M1 →（M2）→ M3 → M4

温感轨全部通过后再切入。运动轨 ξ^occ = `move_x/y/z`、`yaw_ccw/cw` 的 `pre_trace`（路线1）。

- **M0**：明确运动 ξ^occ，建立探针与 RC 基线（同 T0 骨架）。
- **M1**：二元 r≺ 时间关系（如 move 发生 vs yaw 发生，谁先）。
- **M2（方向关系，两案并存）**：
  - **默认主线：暂缓 M2**。运动轨走 M0→M1→M3→M4。理由：move_x↔y↔z 轴链无物理根据，只会生成「手工排列的轴顺序」。等找到真实传播支撑（路线B：前庭 ξ→中继/运动神经元→Body 反馈 ξ 的因果链，比较 π_{a→b} vs π_{b→a}）后单独设计——但路线B 会触碰 Motor/Body，超出「只读 ξ、终止 Collector」范围。
  - **可选分支：降格为对向结构关系原型**。若必须完整走 0→4，用真实 push-pull 结构：`yaw_cw↔yaw_ccw` 或前庭量子链路 `ξ_yaw+↔ξ_yaw-`（MET/antimet 反向物理通道，已存在于 `_QUANTUM_DIRECTIONS`）。**必须明确标注它是「极性竞争关系」，尚不等于普遍空间传播方向生成元。**
  - 走哪条到 M2 阶段再定。
- **M3**：轴间参与竞争原型 **r_part**（ξ_x, ξ_y, ξ_z 的竞争参与度，两级验收同 T3，未升格前不叫 r_ρ），**输出停在 Collector，与现有 `polarization` 并行存在，不替换任何东西**（见第六节）。
- **M4**：M1/M3（及可选 M2）已有关系并行，无下游替换。

---

## 六、Phase 3/M3 与 polarization —— 拆分，实置换移出 0-4（纳入批判点2）

v1 的「Phase 3 替换 polarization」与「终止于 Collector」矛盾，因替换 = 接入现有运动状态计算 = 改变下游行为。拆为三段：

- **M3（= 3A）参与竞争关系原型**：`(ξ_x,ξ_y,ξ_z) → (r_part,x, r_part,y, r_part,z)`（命名同 T3，未达二级验收前不叫 r_ρ），输出停 Collector，与 `MotionState.polarization`（`variant_adapter.py:2162-2168`）**并行存在，不替换**。回归只验隔离性。
- **3B 占比输出校准与影子比较**（离线）：比较 ρ_struct（r_part 或已升格的 r_ρ 输出）与 ρ_python（现有 polarization），覆盖 排序一致性 / 共同尺度变化 / 静默 / 单轴主导 / 两轴竞争 / 三轴等强。仍不切生产路径。
- **后续独立 Phase 5：硬编码替换**：只有结构输出（且已达 T3/M3 二级验收升格为 r_ρ）证明足够稳定后，才用其替代 `polarization = max|ν_i|/Σ|ν_i|`。

故 0-4 方案中「替换 polarization」改述为：**为未来替换 `polarization` 提供结构化候选输出和影子比较数据。**

---

## 七、关键约束（落地时写死）

1. **census + 关系层账本**：新 collector/中继神经元 → **排除** `get_all_neurons()`（护 T4.1 energy_per_neuron）；新 bundles → **进** `get_all_bundles()`（供 Noether/Xin/熵账本）。照 `variant_adapter.py:4279-4290 / 4381-4396`。**但排除全局 census 只是暂时隔离、非无能耗** → 必须另建关系层独立账本（T0 建立，T4 强制验收），统计关系神经元/Collector/膜积分次数/近似能耗/成本增量。
2. **全 frozen，权重远离饱和**：`learning_rule="frozen"`, `remodel_cost_kappa=0.0`（`bundle.py:336-337` 短路 learn）。权重满足 `initial_weight == weight_max`，且**默认必须 `0 < w_0 = w_max < 1`，禁 `=1.0`**（纳入批判二点10；`memory: project_memristor_saturation_edge_bug`：w=1 附近与随机扰动/PowerRail 饱和相撞）。首轮从 `w_0 ∈ [0.1, 0.5]` 扫描，具体值由实测电流与 RC 充电时间决定。**例外（我方补充）**：约束的真正对象是**有效写入权重**，非 base 常量本身——若权重经构造期几何衰减写入（如 Ω 层 `_RF_BASE_WEIGHT=1.0 × cos²θ`，有效值 <1），base=1.0 可接受；新生成元若用类似衰减写入，需在参数表中注明"有效权重<1"并给出推导，不能只看 base 常量判断是否违反本约束。
3. **独立 RC 常量**：每生成元自带电容/r_leak，禁复用 ξ 层 `C=0.5`（Ω 教训，`memory: project_omega_coupling_generator_complete`）。
4. **不改母本**：全走 `VariantCircuit` 新 `_init_*`/`_step_*` 叠加。
5. **最小支撑**：T1/M1=2 源，T2=3 邻接源，T3/M3=先2后3；不铺满全网。全网扩展 = Phase 0-4 之后独立阶段。
6. **禁止直接输出时间、方向、占比等语义结论的无状态计算器；允许新增具有内部状态、局部守恒、有限耗散和独立探针的物理机制**（2026-07-16 按批判八点1替换旧版"禁新造类"表述，与第十二节 12.1 已采纳的"机制缺口准入制"保持一致，不再依赖后文覆盖前文——原"`DelayLine`/`RatioDivider`/`DirectionOperator` 一律禁"是旧版残留，已被 12.1 的准入七问取代）。新增机制必须通过机制缺口准入七问（12.1）。历史支路用非 spiking Neuron RC 膜状态；抑制用 `synapse_gain=-1.0`；Memristor 权重非负。
7. **强制三问**：每个新 Bundle 先答 Q1 生物对应物 / Q2 物理结构 Sources→Bundle→Targets / Q3 参数依据（`# BIO:`/`# REF:`/`# EXP:`），答不出即停。
8. **关系层 step 调度协议**（纳入批判三点7，2026-07-16 追加）：T1（多路电流须先求和再统一 step）、T3（共享池须先汇总再统一更新）各自独立踩到了同一类坑——任何新关系生成元只要有"多路输入汇聚到同一个下游状态（神经元或共享池）"的结构，都必须遵守以下统一 8 步调度顺序，不得逐一重新摸索：
   ```
   1. 所有 ξ^occ 完成本步更新
   2. 所有 ξ→关系中继 Bundle propagate
   3. 按目标神经元/共享池汇总全部输入电流
   4. 每个 trace/relay/shared pool 只 step()/更新一次
   5. 所有中继→Collector Bundle propagate
   6. 按 Collector 汇总输入
   7. 每个关系 Collector 只 step() 一次
   8. 更新关系层独立账本
   ```
   适用范围：T2 的 `P_{a→b}`+raw ξ_b → Collector 接线（同 T1 collector 多路输入模式）；T3 的共享池 `dn.normalize(i_a+i_b,dt)`（第3/4步的具体应用，已在 T3 报告 §5.2 验证）；T4 集成阶段的三路并行读取。

---

## 八、后续独立阶段（Phase 0-4 之后，不在本轮）

- **Phase 5**：`polarization` 硬编码替换（依赖 M3+3B 稳定性证明）。
- **全网络扩展**：从最小支撑扩到全 64 感温源 / 全运动轴。
- **运动 ξ^err 轨（路线2）**：用误差神经元构建真正预测残差生成元，与 ξ^occ 轨并存。
- **运动 M2 路线B**：真实因果传播链方向生成元（触碰 Motor/Body）。

---

## 九、验证方式（端到端，每 Phase 收尾必做）

1. **专项探针全通过**：`PYTHONIOENCODING=utf-8 python -m nexus_v1.tests.test_<track>_<phase>`（峰值/衰减/静息/frozen权重/census+关系层账本 六类，照 `test_thermal_coupling_generators.py` 扩展）。命名：温感 r≺=`T-TRP`、r→=`T-TRD`、r_part=`T-TRPT`；运动 r≺=`T-MRP`、r_part=`T-MRPT`。
2. **全量回归 21/21**：`PYTHONIOENCODING=utf-8 python -m nexus_v1.tests.test_regression`（exit 0）。
3. **契约不回退**：`python nexus_v1/run_contracts.py`（**契约结果不得劣于父提交**——若父提交已有失败，保存失败签名并确认新分支没有新增失败，`F_new ⊆ F_parent`；2026-07-16 按批判八点2修正，不再断言固定"15/15"，与第十一节点2 已确立的方法论保持一致），`python nexus_v1/run_audit.py`（信号深度不降 / |V|<100 / 能量>0 / 无 NaN）。
4. **工程报告**：每 Phase 一份存 `cell-cell/工作报告/`，11 段标准结构。

---

## 十、首轮里程碑

**温感轨 T0**：选定 2~3 个感温 ξ^occ + 冻结选点落盘 + 峰值/衰减/静息探针骨架 + 关系层独立账本骨架 + `delay_steps` 五项核实 + RC 标定夹具 + 回归 21/21 基线确认 + T0 工程报告。

（存档状态：v1 落地方案 `cell-cell/claudecode方案/基础生成元双轨落地方案_2026-07-16.md` 已标注废弃；v2 初版 `cell-cell/claudecode方案/基础生成元双轨落地方案_v2_2026-07-16.md` 需在本次批准后更新为本文件的最新内容（含批判二的 4 项修订 + 我方独立评判）；路线分析报告 `cell-cell/工作报告/基础生成元方案评估与运动侧ξ源路线分析_2026-07-16.md` 保持不变，路线1/2 并存结论未变。）

**T0/T1/T2/T3 执行状态（2026-07-16）**：T0/T1 已完整落地（代码+报告，分支 `feat/thermal-rprec`）；T2 仅设计报告未编码，T3 仅路径判别实验未编码。第三份交叉比对批判（见第十一节）审查了实际产出并指出需修正处，**在继续 T2/T3 正式编码前必须先完成第十一节的修正**。

---

## 十一、批判三执行修正清单（已独立评判，含 1 处代码实测反驳）

批判来源：`cell-cell/交叉比对/document - 2026-07-16T185742.365.md`。逐条独立评判，**5 处接受需修正，1 处用实测反驳（不需要按批判建议行动，但需在报告里补充正确证明）**。

### 接受，需要修正

1. **T0 选点不是真冻结，是"确定性重算"**：`site_selection.py` 在模块导入时用 `fibonacci_sphere_points()+最近邻搜索` 重新计算，虽无随机性，但依赖生成公式/半径/遍历顺序——公式一旦变化，"冻结"结果会跟着变。**修正**：把计算结果的最终值写死为字面常量（索引/边/极性），与生成公式解耦；原计算逻辑降级为"验证工具"（如 T-T0-2 那样用来复核，而不是用来生产选择）。
2. **T0 契约门槛应为"不劣于父提交"而非"15/15"**：当前 13/15 的 2 项失败虽然大概率与 T0 无关（T0 零改动母本），但没有对照父提交验证过。**修正**：在父提交（`4cf106a`）上也跑一次 `run_contracts.py`，存下失败签名，Gate A 的验收改为"T0 分支失败集合 ⊆ 父提交失败集合"，不再断言"15/15"。
3. **T1 的 12 条关系 Bundle 没有进入 `get_all_bundles()`——违反本计划自己写的约束1**：约束1原文"新 bundles → 进 get_all_bundles()"，但 `RPrecCircuitT1` 没有覆写这个方法，T-TRP-1 测试把"Bundle 不在 census 里"断言成 PASS，是真实的实现 bug。**修正**：`RPrecCircuitT1` 覆写 `get_all_bundles()` 返回 `super().get_all_bundles() + self.rprec_relation_bundles()`（不改母本、不改 `VariantCircuit`，纯子类覆写），T-TRP-1 断言随之改为"Bundle 应该在 census 里，Neuron 不应该"。
4. **T1 命名精度：应称"T1 二元时间关系电路原型"，Gate B 拆分 B1/B2**：B1（关系电路逻辑在合成输入下成立）已完成；B2（真实 ξ^occ 动力学适配）未完成——T1 报告已如实记录这一点，本次只需把这个区分正式写进方案/报告的措辞里，不需要代码改动。
5. **T2 `delay_steps=round(d/v_cond)` 量纲错误**：`d` 是模型球面单位（半径2.0，非米），`v_cond` 类比了真实生物速度（"myelinated axon ~100m/s"），两边单位不匹配，中间缺一个未定义的长度尺度映射 `ℓ0`。**修正**：T2 设计报告改为直接把速度参数定义成"模型距离单位/仿真步"（`v_edge` 量纲：model-unit/step），不再引用真实 m/s 生物锚点，避免引入定义不出来的 `ℓ0`。
6. **T2 命名降级为 `r_edge-lag` 原型，留到 T4 证明它不能被 r≺ 替代后再升格 `r→`**：这是我方对自己不一致的纠正——T3 已经用"先叫 r_part、实测通过二级验收才升格 r_ρ"的克制命名纪律，但 T2 直接叫了 r→ 没有同等克制。**修正**：T2 设计报告的生成元命名统一改为 `r_edge-lag`（有向邻接时延关系）原型，Phase 4 集成阶段证明其判别行为与 r≺ 有本质区别（不能被 r≺ 参数化后模拟出来）后才升格 `r→`。
7. **补一份关系层 step 调度协议**：T1（多路电流须先求和再统一 step）、T3（共享池须先汇总再统一更新）各自独立踩到了同一类坑。**修正**：制定统一 8 步调度协议（见下），写入约束追加条款，T2 及后续 Phase 直接照做：
   ```
   1. 所有 ξ^occ 完成本步更新
   2. 所有 ξ→关系中继 Bundle propagate
   3. 按目标神经元汇总全部输入电流
   4. 每个 trace/relay/shared pool 只 step 一次
   5. 所有中继→Collector Bundle propagate
   6. 按 Collector 汇总输入
   7. 每个关系 Collector 只 step 一次
   8. 更新关系层独立账本
   ```

### 用代码实测反驳（不接受批判的判断，但要点仍需体现在报告里）

8. **T3"路径B修复方案超出现有组件范围"——反驳，已用代码验证**：批判说我在 T3 报告里提出的两种修复思路（新增"先汇总再分别读出"调用模式 / 两个 DN 实例共享外部 pool 状态）都要改动或新增接口，因此"路径B工程实现路径尚未闭合"。**实测反驳**：`DivisiveNormalizationReceptor` 已公开 `pool_activity` 属性（`compensation.py:337-338`，非本次新增）。合法实现：① 调用一次 `dn.normalize(I_a+I_b, dt)`（只为触发池按总电流更新一次，返回值丢弃）；② 读公开的 `dn.pool_activity`，手算与 `normalize()` 内部相同的 `norm_factor=σ/(σ+pool)` 公式；③ 分别乘到 `I_a`、`I_b`。全程只用两个已有公开成员，未新增方法、未碰 `_pool_voltage`。实测：等强输入下两路精确对称（0.5653=0.5653，比原顺序调用法的 1e-4 级偏差更干净），2:1 比例在 1x/2x/4x 共同放大下比值仍精确锁定 2.0000，总量仍亚线性饱和增长。**结论**：Gate D 不需要暂停，路径B工程实现路径已闭合，T3 报告需要补上这个正确实现方式，把"路径B物理机制胜出但实现未闭合"改回"路径B物理机制胜出且实现路径已闭合"。
9. **T3"比值保持不是充分证明"——批判的抽象论点成立，但我方数据已经排除了它暗示的反例**：任何常数乘性增益 `y=g·I` 都会保持比值不变，所以"比值不变"单独不能证明这是真正的共享池分流。但 T3 报告已经测过总量随共同放大的变化（1.42→1.86→2.21，明显亚线性/饱和），如果是常数增益，总量应线性增长，实测不是——这已排除"纯常数增益"的替代解释。**处理方式**：不是"尚未证明"，是"证据已经存在但报告没有把这一层论证显式讲清楚"，T3 报告需要补充这一段论证（对比常数增益假设 vs 实测的饱和特征），并补充批判建议的更严格验证（显式验证 `V_pool=F(I_a+I_b)` 且所有通道用同一次更新后的 `G`——第8点的实测已经做到了这一点，直接复用）。

### 执行顺序（按批判建议的顺序，采纳，全部已完成）

1. [x] 修正 T0 的静态冻结表 + 契约基线对照方式（`site_selection.py` 改字面常量；父提交 `4cf106a` 实测 12/15，分支 13/15，失败集合 {C2,C3}⊆{C2,C3,C6}）；
2. [x] 修正 T1 子类的 `get_all_bundles()`，补 census 断言（`RPrecCircuitT1` 覆写；T-TRP-1 断言改为"应该在里面"）；
3. [x] T1 状态正式记为"B1 通过、B2 待真实源适配"（报告标题+顶部说明已改）；
4. [x] 修正 T2 量纲问题，命名降级为 `r_edge-lag` 原型（`v_edge` 改模型单位，不再引用真实 m/s 生物锚点）；
5. [x] T3 报告补充同步共享池的正确实现方式（已验证，只用 `.normalize()`+`.pool_activity` 两个现有公开接口），路径B结论改为"已闭合"；等强场景差值从 1e-4 降到精确 0；补充常数增益假设排除论证（2x/1x=1.30、4x/1x=1.53，远低于常数增益预期的 2.0/4.0）；
6. [x] 补写关系层 step 调度协议，追加进约束第8条；
7. [x] 更新工作看板 + git 存档（commit `01dafc6`）。
8. 完成以上后，T2/T3 的正式编码实施才开始（不在本轮批准范围内，需用户另行确认）。

---

## 十二、批判四/五评判与执行修正（2026-07-16，含 1 处代码实测验证）

批判来源：批判四 `交叉比对/document - 2026-07-16T193223.428.md`（放宽禁新增组件约束，提议机制缺口准入制）；批判五 `交叉比对/document - 2026-07-16T193317.012.md`（审查批判三修正后的报告，提 4 点具体问题 + 落地顺序重排）。**两份批判之间存在实质分歧**：批判四主张立刻新建 4 个组件；批判五主张先用现有接口验证，撞到真缺口才新建。**我方独立评判：站批判五一边，批判四的核心原则采纳、执行顺序不采纳。**

### 12.1 批判四：核心原则采纳，执行顺序不采纳

**采纳**：约束6"禁新造类"收紧为**「禁止语义硬编码组件，允许新增有真实内部状态的物理机制」**——这与原始《基础生成元》总纲的真实意图一致（总纲禁的是 `RatioDivider`/`DirectionOperator` 这种**名字里写死语义结论**的组件，不是禁止任何新类）。批判四诊断的三个信号（T1 真实源振荡与关系窗口不能自然衔接、T2 `delay_steps` 内部 FIFO 状态不可观测/不可组合、T3 需要外部 Python 重写公式）均已被我方报告独立证实，不是凭空指控。新组件准入的 7 条判据（缺口已证明/语义不匹配/有内部状态/不能直接输出语义标签/仍经 Bundle 接口/有独立账本探针/只解决一个缺口）合理，采纳为约束6的补充细则。

**不采纳**：批判四建议立刻开"M0 机制补全阶段"，不经验证就新建 `RelationStepScheduler`/`SharedDivisivePool`/`EdgeTransportState`/`OccurrenceRenewalDetector` 四个组件。**违反"Model before tune"**——应先证明现有接口确实无法满足，而非假设不满足就直接建。**已用代码反证其中一项**：批判四认为 T3 必须新建 `SharedDivisivePool` 才能避免"调度代码重写归一化公式"，但批判五提出的 `dt=0` 只读方案（见 12.2 第1点）已用现有 `DivisiveNormalizationReceptor.normalize()` 接口验证可行，不需要新组件。

### 12.2 批判五：四点全部采纳，第1点已代码验证并有更优实现

1. **T3 同步共享池"实验合法，生产实现仍不合法"——成立，已用代码验证修复方案**。原实现 `norm_factor=sigma/denom` 是在调度代码里手算一遍除法公式（即使读的是真实池状态），仍是外部重写。**已验证的正确方案**：
   ```python
   dn.normalize(i_a + i_b, dt)     # 更新共享池一次（真实dt）
   y_a = dn.normalize(i_a, 0.0)    # dt=0，只读取，走 normalize() 自己的除法逻辑
   y_b = dn.normalize(i_b, 0.0)    # dt=0，只读取
   ```
   实测 5 项检验全部精确通过：① `dt=0` 时池状态完全不变（3 次读取同一 pool_activity 值）② 等强输入精确对称（0.5653=0.5653，无浮点偏差）③ 调用顺序交换后输出完全不变 ④ 二通道比值精确 2.0000 ⑤ 三通道比值精确 3.0/2.0。**这比批判三修正版（手算 norm_factor）更优、更"合法"（未在调度层重写除法公式，只调用 `.normalize()` 自身）**，需要再次更新 T3 报告和 `_calib_t3_path_ab.py`。
2. **r_ρ 应定义为射影比例 `[y_1:y_2:...:y_n]`，非"和为1"占比坐标——成立**。采纳为正式数学定义；`Σy_i=1` 只能是探针读数，不得进生产信号路径。
3. **T2 升格条件"不能被 r≺ 模拟"不可操作——成立，是 T2 报告的真实逻辑漏洞**。只要允许 r≺ 用足够窄的 RC 核，纯延迟就是它的特例，波形层面无法证伪。**采纳批判五给出的拓扑判据**替代原表述：
   - 边依赖性：移除边 e 的 Bundle 后关系应归零，即使 (a,b) 仍以相同时差发生；
   - 非边排除性：冻结拓扑中无直接邻接的点对（如 31,23）不应因人为时差匹配而产生直接边关系；
   - 方向交换性：交换发生顺序，关系应对应交换；
   - 路径可组合性：链上依次触发 31→28→23 应形成两条局部边关系，不应自动产生 31→23 直接关系；
   - 标签协变性：交换节点编号但保持拓扑结构，输出应随结构交换，不绑定点编号。
4. **T1-B2 不应用外部 dT 命令起始时刻当真值，应用真实 ξ 发生时刻——成立，是方法论的真实疏漏**。`r≺` 的输入是 ξ^occ 内部发生，非外部刺激命令。**采纳**：以真实 Collector 脉冲/`pre_trace` 上升沿定义 `t_a^(k)`、`t_b^(m)` 作为真值，检查 `r≺` 是否在对应 fast/slow 窗口响应，不再要求关系输出跟随 dT 命令的预设时序；覆盖首次发生 a≺b/b≺a、同期发生、停止驱动后自主复发、多周期发生、不同 `PYTHONHASHSEED`。**此修正几乎零成本**（不改电路，只改验证方法），应作为 T1-B2 的首选诊断手段，只有该方法论下关系仍不稳定，才考虑批判四提议的 `OccurrenceRenewalDetector`。

**另需采纳的 T1 测试改进（批判五 R1 节，非独立编号但值得纳入）**：T-TRP-5 的"同时触发容差<3.0"过宽，不能充分证明无方向偏置；应改为**输入交换协变性测试** `D_{a≺b}^{before} ≈ D_{b≺a}^{after}`（交换源/目标后关系应对应交换），并在多个固定 `PYTHONHASHSEED` 下验证偏差符号不长期固定——比单次宽容差判据更可靠。

### 12.3 我方综合裁决：执行顺序按批判五重排，机制新建按"先验证后新建"原则收紧

采纳批判五的 R0→R1→R2→R3→R4 顺序（优先级高于第四节原定的 T0→T1→T2→T3→T4 顺序，T2/T3 互换先后）：

- **R0（收口）**：清理总纲残留旧命名（`-rdir/-rrho` 分支示例 → `-redge-lag/-rpart`）；T0 描述改为"字面常量作为生产冻结值，几何计算只用于验证"（已完成，见第十一节）。
- **R1（T1-B2 真值修正，不改参数）**：新增 `test_basegen_thermal_t1_real_occurrence.py`，用真实 Collector 发生时刻（非外部 dT 命令时刻）作真值；补充输入交换协变性测试替代过宽的 ratio<3.0 判据。只有该方法论下关系仍不稳定，才进入参数重标或考虑 `OccurrenceRenewalDetector`。
- **R2（优先实施 T3，非 T2）**：T3-A 公共接口合法性探针（验证 `normalize(total,dt)` 更新 + `normalize(channel,0)` 无副作用读出 + 多通道同池 + 调用顺序无关，即 12.2 第1点已完成的验证）→ T3-B 真实 Neuron/Bundle 二元电路（T0 冻结 28/31 两点）→ T3-C 资格验收（先 r_part，共同尺度不变性通过后升格**射影** r_ρ，测试覆盖 1:1/2:1/3:1/1:2/共同放大1x2x4x/池启动/池衰减/输入比动态变化/三元31,28,23/静默恢复）。
- **R3（再实施 T2 `r_edge-lag`）**：**首版用路径 E1（直接延迟 Bundle：`ξa→delayed Bundle┐ + ξb(raw)┘→D_a⇢b Collector`），不用路径 E2（延迟Bundle+中继）**——E1 组件最少、Bundle 本身就是边运输支撑、不引入额外 RC 展宽；只有确实需要"边自身是可继续参与组织的过程量"时才升级 E2（对应批判四的 `EdgeTransportState`，先验证 E1 是否已够用）。**延迟参数不直接由距离精确区分**：两条冻结边距离差仅 0.0696，经取整/pre_trace展宽/Collector积分后很可能对应相同有效时延——首轮用"邻接边存在+统一标定的边传播延迟"，只有测出有效时间核宽度 W_eff 后，才要求两条边的取整延迟差值 > W_eff。
- **R4（T4 集成）**：验证清单追加"T1-B2 结果不因 T2/T3 接入而退化"；资格状态表用分级（Gate通过/机制候选通过/尚未成立），不再用单一 PASS 表示所有层次。

### 12.4 资格状态表（采纳批判五 §三，替代此前笼统的"通过/未通过"表述）

| 对象 | 当前状态 | 下一资格条件 |
|---|---|---|
| T0 | Gate A 通过 | 保持冻结与基线不回退 |
| r≺ | B1 电路原型通过 | B2 真实 ξ 发生顺序验证（R1） |
| r_edge-lag | 定义报告完成 | 边依赖性/非边排除性/方向交换性/路径可组合性/标签协变性（R3） |
| r_part | 路径B机制候选通过（含 dt=0 只读验证） | 真实 Bundle/Neuron 电路 + 共同尺度稳定（R2） |
| r_ρ | 尚未成立 | 射影关系在真实输入下稳定 |
| r→ | 尚未成立 | r_edge-lag 证明具有独立结构边支撑（拓扑判据全过） |
| T4 | 未开始 | 三类关系并行无串扰 |

### 12.5 落地进度（2026-07-16，全部完成）

1. [x] `_calib_t3_path_ab.py` 新增 `run_path_b_synced` v2（`dt=0` 只读方案替换 v1 的 `norm_factor` 手算版）+ `test_shared_pool_synced_interface_legality()`（5 项接口合法性检验，全部精确通过）；
2. [x] 更新 T3 报告：新增 §5.2b `dt=0` 只读方案说明；新增 §6.1 r_ρ 射影比例定义（12.2 第1/2点）；
3. [x] 更新 T2 设计报告：§6.3 升格条件改为 5 项拓扑判据；§七追加 E1/E2 路径选择建议（12.2 第3点）；
4. [x] 新增 `test_basegen_thermal_t1_real_occurrence.py`（T-REAL-1~4，4/4 PASS）+ T1 测试新增 T-TRP-8 交换协变性（8/8 PASS）（12.2 第4点 + 补充项）；
5. [x] 方案第一节分支命名 `-rdir/-rrho`→`-redge-lag/-rpart`；
6. [x] 方案第四节追加执行顺序说明（T3 先于 T2，指向 12.3）；
7. [x] 更新看板 + git 存档（commit `d252a22`）。
8. 之后才进入 R2/R3 的正式编码实施（R3/T2 需先完成第十三节的重新定向，见下）。

---

## 十三、T2 物理前提重审：温度场无传播速度，观测时差是身体运动学产物（2026-07-16，我方独立发现）

### 13.1 核查结论

用只读探索核查 `components/world.py` 的世界热场模型，得到以下事实链：

1. **`HeatSource.effective_temperature()`**（`world.py:39-95`）：单点位置 + 有限空间范围，`温度 × (1-d/r)` 线性衰减；热源可缓慢漂移（`_drift` speed=0.001/step），但**没有传播速度概念**。
2. **`World.temperature_at(pos)`**（`world.py:553-571`）：对给定世界坐标，所有热源做**瞬时空间叠加**——只有距离项，**没有任何时间/波速/传播项**。给定坐标 → 温度当场确定，不存在"这个温度值传到这里花了多久"的物理过程。
3. **`SkinPatch.world_position(body)`**（`world.py:177-194`）：把采样点的 `local_offset`（身体局部坐标系固定偏移）先绕 `body.yaw` 旋转、再加 `body.position` 平移，得到世界坐标。
4. **T0/T2 用的 32 个感温球面采样点**（`variant_adapter.py:1702-1729`，`fibonacci_sphere_points(32,2.0)`）**全部是 body-frame 固定偏移**——身体转向/平移时，这些点随身体刚体运动。

**结论**：给定当前世界模型，感温点 28 号与 31 号先后"感受到"温度变化的时间差，**主要由身体整体转向速度（yaw 角速度）/ 平移速度决定**——两点被身体运动依次带入温度梯度区的先后——**与两点间的局部球面距离（~1.09）/ 所谓"热传导速度"几乎无关**。也就是说：

> **T2 报告 §四.2 依赖的物理假设"`delay_ab = d_ab/v_edge`（几何距离/传导速度=传播延迟）"，在当前世界模型里没有对应的真实物理过程。**

### 13.2 这比第十二节 12.2 第3点更深一层

12.2 第3点（批判五）指出的是"升格判据不可操作"——即使 delay 机制本身没错，也无法证明它和 r≺ 有本质区别。**本节指出的是更早一步的问题**：delay 机制本身要建模的"沿边传播"这件事，在当前世界物理里根本不存在对应物——不是"证明起来困难"，是"要证明的东西不是真的"。如果直接按原报告 §四.2 的设计实施，会构造一个**结构合法但物理叙事虚构**的组件：Bundle/delay_steps 的接线方式没问题（符合"结构优先"），但支撑其参数取值的"# BIO: 轴突传导延迟"式注释会是编造的——真正驱动观测到的时差的是身体运动学，不是沿球面局部边的热传导。这正是 RULES.md"每个组件映射到真实生物/物理对象"要严格把关的那类问题，只是这次不是 Python 硬编码语义，而是**参数标定的物理叙事本身站不住脚**。

### 13.3 重新定向的候选方向

**方向 A（推荐）：把"传播延迟"重新锚定为"身体运动学扫掠顺序"，而非"热传导"**

现象本身是真实的、结构化涌现的：身体绕 yaw 轴旋转时，body-frame 上固定的两个球面点，会以一个**由角速度和两点角距离决定的、确定的先后顺序**依次扫过温度梯度——这不是编造的，是真实运动学。**重新定义**：`r_edge-lag` 不再检测"热沿边传播"，改为检测"运动扫掠是否以与两点几何布局一致的顺序、在合理时间窗内先后激活"——本质上是一种**运动诱发的空间梯度扫掠一致性关系**，物理对应物明确写为"前庭-皮肤耦合的空间扫掠信号"，而非"轴突传导延迟"。

**具体标定路径**：`v_edge` 不再是虚构的"传导速度"，改用项目**已有、已标定的真实角速度常量**——`memory: project_vestibular_dim_anchoring` 记录的权威锚定（P0 冻结值，`vest-P3-complete` tag，`nexus_v1/vestibular/DIMENSIONAL_CONTRACT.md`）：canal 六轴均值 1.80 deg/s/unit，**yaw 专轴值 1.21 deg/s/unit**（T2 关注的是 yaw 转向扫掠，应取 yaw 专轴值而非六轴均值）。用两点间的**角距离**（非原来的欧氏弦距离）除以 yaw 角速度（1.21 deg/s/unit），得到该角速度下"扫过这两点"所需的真实步数——这是可推导、有生物物理依据的标定，不是拍脑袋凑数量级。**待核实**：此锚定值来自 P0 测量，15 天前的记忆，实施时需对照 `DIMENSIONAL_CONTRACT.md` 现状重新确认数值未过期。

**方向 B（保守）：暂缓 T2，仅保留温感轨 T1/T3，T2 移入"后续独立阶段"**

若认为方向 A 的重新定向本身还需要更多论证/实验（如先验证 yaw 转向时感温点是否确有可测的先后模式），可以暂缓 T2 的正式实施，先完成 R2（T3）+ T4 集成（用 T1+T3 两类关系），把 T2 列为独立后续任务——这不违反已批准的"R2 优先于 R3"顺序，只是把 R3 的门槛从"设计报告完成即可编码"提高到"物理前提重审通过后才能编码"。

**我方倾向方向 A**：现象是真实的、有生物学对应（前庭-皮肤耦合，运动觉与体表温度觉共享同一具身运动学时钟，这本身是一个有趣且合理的生物学假说，比"皮肤内部热传导"更贴近"生成元检测结构涌现关系"的项目哲学），而且复用了项目已完成、已验证的前庭角速度标定，不需要凭空造一个"传导速度"。

### 13.4 是否需要工作报告 + 后续方案

**是**。已执行（commit `f464c48`）：
1. [x] 工作报告：`cell-cell/工作报告/基础生成元T2物理前提重审_温度场无传播速度_2026-07-16.md`，记录 13.1 核查过程（含 `heat_source.py` `CylindricalHeatSource` 的补充核实，World 2.0 主力热源同样瞬时确定）；
2. [x] T2 设计报告已更新为方向A：§四.2 物理机制改为"身体运动学扫掠"，`v_edge`→`ω_yaw`(=1.21 deg/s/unit，已核实 `DIMENSIONAL_CONTRACT.md` 未过期)，`d_ab`→`θ_ab`（角距离），§五参数表、§七已知局限同步更新；
3. [x] 更新方案 v2 存档 + 看板 + git；
4. **不改变执行顺序**（此结论被第十四节的新决策取代，见下）。

**追加（第十四节引出）**：第六份交叉比对批判指出方向A实为跨模态生成元，与方案原则冲突；用户已决定重构世界模型，详见第十四节。

---

## 十四、世界模型重构立项：InstantFieldWorld → DynamicThermalField（W0~W5，用户已选定）

批判来源：`cell-cell/交叉比对/document - 2026-07-16T201948.954.md`（第六份）。

### 14.1 批判诊断（我方核实成立）

方向A的延迟尺度 `τ_ab = θ_ab/ω_yaw` 由**前庭运动状态**（`ω_yaw`）决定，不是温度场自身的性质——这让 `r_edge-lag` 实质上是**温感×前庭跨模态生成元**，即使没有literal 跨模态 Bundle（`ω_yaw` 只是构造期标定常量，非运行时读取），其**现象本身的物理意义仍然内在地绑定前庭动态**（身体静止则现象不存在，转速不同则标定失配）。这与方案"温感轨/运动轨独立构造、不建跨模态 Bundle"的原则实质冲突。**我方核实此诊断成立**，是我方"方向A"设计的真实漏洞。

根源诊断：当前世界模型 `World.temperature_at(pos)` 是纯查询函数（`T=F(x)`），没有局部状态、通量、耗散的过程性——批判称之为"世界不是被查询的背景，应是与身体共同演化的局部过程"。

### 14.2 我方保留意见（已如实告知用户，用户已在充分知情下做出选择）

我方评估"立即将世界模型重构提升为 basegen 工程前置主任务"这个处方**过于激进**，理由：① 与"最小支撑、不铺满"的一贯纪律冲突；② `World.temperature_at()` 是全项目热感知的中枢依赖（64点量子元系统、`SomatosensoryChain`、历史热趋性实验/DR5指标均依赖），重构影响面覆盖整个项目而非仅 basegen；③ 批判自己的影响评估（§八）显示 T0/T1-B2/T3真实输入验收/T4 全部要重做或暂停，成本远超"修一个生成元的物理前提"。

**用户决策**：在知悉以上保留意见后，明确选择"按批判建议重构整个世界模型（W0~W5）"。**此为用户最终决策，本节按此执行**，我方保留意见仅作存档记录，不再作为执行障碍。

### 14.3 执行原则（我方在用户决策框架内，套用项目既有工程纪律，降低风险）

用户选定方向不变，但**具体怎么做**仍适用本项目一贯的工程纪律，不能因为规模大就放弃：

1. **不改母本，新建旁路**（对应约束4）：新世界模型实现为独立类 `DynamicThermalField`（批判 W0 建议的 `InstantFieldWorld` 命名对应现有 `World`/`HeatSource`/`CylindricalHeatSource`，**保持不动，不删除、不原地修改**），新类与旧类并存，`VariantCircuit` 默认仍用旧世界，只有显式选择新世界模型的实验/子类才切换。这样现有回归 21/21、热趋性实验群、DR5 指标在整个重构期间**不受影响**，可随时对照验证新世界没有破坏旧行为。
2. **最小可实施版本先行**（对应约束5"最小支撑"）：采纳批判 §十的"最小可实施版"——稀疏图（~10² 节点）、每节点仅连有限邻居、热源仅向最近节点注入——而不是一开始就上高分辨率 PDE。这是"最小支撑"纪律在世界模型这个更大尺度问题上的同构应用。
3. **分阶段验证，每阶段独立报告**（对应第一节 Gate 精神）：W0→W1→W2→W3→W4→W5 严格串行，每阶段跑一次"新旧世界行为对照"（旧世界回归 21/21 不受影响 + 新世界自身的物理自洽性检查，如能量守恒进入耗散账本），不合格不进入下一阶段。
4. **强制三问适用**：`ThermalCell`/`ThermalLink` 等新组件同样要回答 Q1 生物对应物（热泉/海底环境扩散，REF 已有 `heat_source.py` 引用的 Kelley 2002）/ Q2 物理结构（局部状态+边通量，非语义硬编码）/ Q3 参数依据（`κ_ij`/`a_ij`/`σ_i` 等扩散系数需要推导或标注 EXP 来源，不能拍脑袋）。
5. **能量守恒进入 Noether 账本**：批判 §十已经提出"允许数值误差但必须进入账本"——这与项目已有的 `ledger/noether_probe.py` KCL 精神一致，新世界的能量更新应可被现有账本体系读取或至少提供对等的独立探针。

### 14.4 阶段划分（沿用批判 W0~W5 编号，纳入 14.3 的项目纪律）

- **W0（冻结旧世界，零风险）**：把现有 `World`/`HeatSource`/`CylindricalHeatSource` 明确标注为 `InstantFieldWorld`（对外文档/注释层面的重命名说明，不改代码本体），作为回归/对照基准，不删除。**这是唯一不涉及新代码的阶段，可立即做。**
- **W1（局部热状态网络，独立新模块，不接入生产电路）**：新增 `nexus_v1/components/dynamic_thermal_field.py`，实现 `ThermalCell`（局部能量/温度状态）+ `ThermalLink`（节点间扩散通量 `J_ij=κ_ij(T_i-T_j)`）+ 热源局部注入 + 环境耗散。只做单元测试级验证（数值稳定性、能量守恒、热源注入后梯度是否正确形成），**不连接 SkinPatch/Body，不影响任何现有系统**。
- **W2（身体-世界热交换）**：新增 `SkinThermalState`/`ThermalContact`，让皮肤点拥有自己的热状态并与最近的 `ThermalCell` 节点交换热量（对流系数 `h_a`）。仍是独立旁路，只有专门测试会实例化它。
- **W3（介质搬运/热羽流）**：加入 `J_ij^adv = a_ij(t) E_i(t)`，产生真实有向通量 `J_ij ≠ J_ji`，这是给"环境通量方向 r_flux"提供物理支撑的关键阶段。
- **W4（发生生成元重新标定）**：在 W1~W3 就绪后，用新世界重新测量 T0 冻结点（或新选点）的 ξ^occ 动态（启动延迟/burst/衰减），因为输入来源从瞬时查询变成了状态交换，动态特征会不同。
- **W5（基础关系生成元回接）**：按批判建议顺序 `r≺→r_part→r_flux→r_sweep`——先纯局部关系，再环境方向，最后跨模态具身关系（`r_sweep` 就是本次发现的"方向A"，届时正式定位为跨模态生成元，不再冒充温感轨内部的 r→）。

### 14.5 首轮里程碑

**W0 + W1**：① 旧世界标注 `InstantFieldWorld`（文档/注释层面）；② 新建 `dynamic_thermal_field.py`（`ThermalCell`+`ThermalLink`，最小可实施版：~10² 节点稀疏图）；③ 独立单元测试验证数值稳定性+能量守恒+热源注入后梯度正确形成；④ 全量回归 21/21 确认不受影响（新模块完全旁路，理论上必然不受影响，但仍需实测确认）；⑤ 工程报告存档。

**范围声明**：W0+W1 是本次批准的执行范围。W2 及以后（身体-世界交换、介质搬运、发生生成元重标、关系生成元回接）需要在 W0+W1 验证结果出来后，再次向用户确认是否继续——这是一个多阶段、跨多次会话的大工程，不在一轮里做完，符合项目"Model before tune"的一贯做法。

**与 R2/R3 的关系**：R2（T3 正式实施，见第十二节）与本次世界模型重构**互不阻塞、可并行**——T3 的共享分流池机制是纯 Bundle/Neuron 电路判定，不依赖世界模型。R3（T2/`r_edge-lag`）正式实施则**依赖本次重构**（至少到 W3 产生真实环境通量），在此之前保持暂缓状态。

---

## 十五、批判七评判与 W1 收口三项（2026-07-16，已批准待执行）

批判来源：`cell-cell/交叉比对/document - 2026-07-16T215047.390.md`（第七份）。审查对象是已完成的 W0+W1 实现（commit `d19769f`）。

### 15.1 我方逐条评判

| 批判点 | 判断 | 依据 |
|---|---|---|
| 1. "对称扩散无方向"表述需修正 | **接受，报告措辞不精确** | `J_ij=G_ij(T_i-T_j)` 瞬时确有方向（热→冷）；混淆的是"瞬时扩散方向"与"独立于梯度的持续流向"（后者才是 W3 要解决的）。 |
| 2. 量纲未真正闭合（`kappa_0/d` 缺面积/体积因子，`capacitance=1.0` 无量纲） | **接受，已核实为真**（读代码确认 `dynamic_thermal_field.py` 无 `A_ij` 项） | 建议显式标注 normalized/physical-anchor 两种模式，成本低。 |
| 3. Fibonacci 球壳不应做生产世界拓扑 | **接受，本是设计意图，需写得更明确** | 风险：W2 若误用同一几何生成器，会让"世界"和"皮肤表面"重叠。 |
| 4. 旧热源"温度"不能直接当新世界"功率" | **认可但当前不适用** | W1 代码从未读取 `HeatSource.temperature`，是对 W2 的前瞻提醒，非当前 bug。 |
| 5. 数值稳定≠物理合理，应加扩散数守卫 | **接受，合理的工程加固** | `node0.T=489.672` 只是 `Q=CV`(C=1) 的正常结果非 bug，但缺少显式稳定性判据（`Δt·ΣG_ij/C_i≤η`）确实是短板。 |

### 15.2 "W1.5" 范围裁定（用户已确认）

批判提出的四项"W1.5"收口，我方拆分为两类：
- **真正属于 W1 自身收口，本轮执行**：① 热量维度契约标注（normalized/physical-anchor 模式显式声明）；② 数值稳定守卫（扩散数检查工具+测试）；③ Fibonacci 球壳"仅测试夹具"的显式警示注释/docstring。
- **实质是 W2 设计决策，留待 W2 重新确认时处理**：`ThermalFieldLocator`（世界坐标定位查询接口）、`DynamicHeatSource`/`ThermalSourceCoupler`（身体-世界耦合、热源语义重设计）——这两项已经在做第十四节明确列为 W2、需要用户重新确认的工作，批判建议"不再征询直接做"与方案范围声明冲突，**不采纳这个绕过方式**。用户已确认按此裁定执行。

### 15.3 本轮执行清单

1. **修正报告措辞**：`世界模型重构W0_W1_局部热状态网络最小实现_2026-07-16.md` §十 已知局限 第3点，把"尚未产生真实有向环境通量"改为精确表述——"已支持局部扩散通量方向（瞬时，由梯度符号决定）；尚未支持独立于梯度的持续介质流动方向（热羽流/环流偏置），这是 W3 任务"。
2. **量纲模式显式标注**：在 `dynamic_thermal_field.py` 模块 docstring 补充一段"当前模式：normalized（机制验证用，`capacitance`/`kappa` 是无量纲数值约定，`kappa_0` 虽以真实海水扩散率为锚点但未含面积/体积因子）；physical-anchor 模式（含 `A_ij`/`V_i` 真实物理量纲）留待需要精确物理标定时再实现"，并加一个模块级常量 `THERMAL_FIELD_MODE = "normalized"` 供未来代码/测试显式判断当前模式。
3. **数值稳定守卫**：在 `dynamic_thermal_field.py` 新增 `diffusion_number(graph, dt) -> Dict[int, float]`（逐节点计算 `Δt·ΣG_ij/C_i`）与 `is_stable(graph, dt, eta=1.0) -> bool` 两个只读诊断函数（不改 `step()` 行为、不做隐式子步进，符合"不应靠调小测试输入掩盖，应给出明确诊断"的批判意见，但暂不实现自动子步进——那是更大的行为改动，超出本轮"加诊断"的范围，如需要留后续任务）。在 `test_dynamic_thermal_field.py` 新增 T-DTF-6（稳定参数下 `is_stable=True` 且长程运行确认不发散）+ T-DTF-7（构造一组明显超过 `η=1` 的参数，确认 `is_stable=False` 能被正确识别，同时不断言"系统必须崩溃"——只验证诊断准确，不验证行为兜底）。
4. **球壳夹具警示**：在 `build_fibonacci_shell_graph` 的 docstring 顶部加一行明确警示——"TEST FIXTURE ONLY. 不要在 W2 身体-世界耦合中直接用它做生产世界坐标空间——皮肤点本身也用同一 `fibonacci_sphere_points` 布点，两者共用同一几何生成器会有把'世界'和'身体表面'意外重叠的风险。生产世界拓扑（稀疏三维网格/固定点云/自适应节点图）是 W2 `ThermalFieldLocator` 设计范围内的工作，不在本文件。"
5. 全量回归 21/21 确认（新增诊断函数为纯只读工具，预期零影响，仍需实测）。
6. 更新工作报告 + 方案 v2 存档 + 看板 + git 提交。

**明确不做（留待 W2 重新确认）**：`ThermalFieldLocator`、`DynamicHeatSource`、`ThermalSourceCoupler`、皮肤热状态、热接触机制、W2 调度顺序、W2 测试清单——这些是第十四节 W2 阶段的内容，批判七的第四节"W2：身体-世界热交换"和第五/六节的调度顺序/测试清单，本轮仅作为**已阅读、已理解、留档供 W2 立项时直接参考**，不在本轮实现。

**追加（第十六节引出）**：第八份交叉比对批判确认 W1.5 完成、可进入 W2，给出 W2A/B/C 拆分设计；用户已确认启动 W2A（仅世界定位+动态热源守恒注入，不接皮肤）。详见第十六节。

---

## 十六、批判八评判：总纲清理 + W2A 启动（2026-07-16，已批准待执行）

批判来源：`cell-cell/交叉比对/document - 2026-07-16T221345.668.md`（第八份）。确认 W1.5 完成可进入 W2，指出总纲正文 4 处过时表述，给出 W2A→W2B→W2C 三阶段设计。

### 16.1 我方评判

**总纲清理 4 点**：核实全部属实（见本节顶部执行状态摘要的表格），已直接修正方案正文（约束6、验证方式3、第四节流程状态），不单独列执行项。

**T3′ 归入"新世界回接"的表述有歧义，需澄清**：批判流程图把 T3′ 放进"新世界回接：T0′→T1-B2′→T3′→T_flux→T4′"，容易被误读为"T3 要等新世界建完才能做"，这与第十四节 14.5 已经明确写下的"R2（T3）与世界模型重构互不阻塞、可并行"矛盾。**我方判断**：批判的本意应是"用新世界重新验证 T3"是一个未来动作，不是说当前 T3(R2) 被阻塞——但表述本身确实会引起误读，已在第四节流程状态中加了澄清，不采纳"T3 停摆"这个暗示。

**W2A/B/C 设计**：技术质量高，认可其拆分顺序（先世界定位+热源守恒注入，再皮肤交换，最后联合调度），与本项目一贯的"最小支撑、分阶段验证"纪律同构。稳定性守卫从"诊断"升级为"执行门"（W2 fail-fast）的建议合理，但**不通过修改已测试完成的 `ThermalFieldGraph.step()` 本体来实现**——那会改变 W1/W1.5 已验证过的行为；改为在 W2A 新增一个独立的 fail-fast 包装函数，不碰 `step()` 源码。

### 16.2 用户决策

用户已确认：① 总纲正文 4 处清理**现在做**（已完成，见本节顶部及正文相应位置的修正）；② **现在启动 W2A**（世界定位+动态热源守恒注入），**不接皮肤**（W2B 留到 W2A 通过后再确认）。

### 16.3 W2A 设计（世界定位 + 动态热源守恒注入）

**新文件**：`nexus_v1/components/thermal_source_coupling.py`（与 `dynamic_thermal_field.py` 分开，关注点不同：后者是图/扩散，前者是定位/注入语义），不改 `dynamic_thermal_field.py` 的 `ThermalFieldGraph.step()` 本体。

**`ThermalFieldLocator`**（Q1 生物对应物：定位查询，同 `skin_network.py`"坐标生成器不应知道电路"的纯几何工具定位——不是 Bundle，是几何/数学工具，TYPE:MATH，与 `fibonacci_sphere_points`/T0 `site_selection.py` 的最近邻查找同类）：
```
locate(x: Tuple[float,float,float], graph: ThermalFieldGraph, k: int = 4) -> Dict[int, float]
```
返回 `{node_id: weight}`，`weight>=0`，`Σweight=1`。实现：k 近邻反距离加权（inverse-distance weighting，标准插值方法，非拍脑袋），只读 `graph.cells[*].position`，不修改任何状态。

**`DynamicHeatSource`**（Q1 BIO：海底热泉功率输出，同 `heat_source.py` 场景但换成功率语义而非温度语义；Q3：`energy_remaining`/`power`/`efficiency` 首轮为 EXP 标注的测试尺度常量，不宣称精确物理标定，与 `THERMAL_FIELD_MODE="normalized"` 一致）：
```
state: energy_remaining E_s, power P_s, position x_s, efficiency η_s
release(dt) -> float   # 返回本步实际可注入功率，满足 P_s·dt <= E_s，energy_remaining 相应扣减；耗尽后返回0
```

**`ThermalSourceCoupler`**（组合函数，不新增有状态类；Sources=`DynamicHeatSource.release()` + `ThermalFieldLocator.locate()` → Targets=注入字典，供 `ThermalFieldGraph.step(dt, external_injections=...)` 直接使用，不改 `step()` 接口）：
```
couple(source: DynamicHeatSource, locator_weights: Dict[int, float], dt: float) -> Dict[int, float]
    power = source.release(dt)
    return {node_id: power * weight for node_id, weight in locator_weights.items()}
    # 由 Σweight=1 保证 Σ注入 = power，天然守恒，不需要额外归一化步骤
```

**Fail-fast 包装**（不改 `ThermalFieldGraph.step()` 本体，新增独立函数）：
```
def guarded_step(graph, dt, external_injections=None, eta=1.0):
    if not is_stable(graph, dt, eta):
        raise ValueError(f"Unstable thermal diffusion configuration: max diffusion_number={max(diffusion_number(graph, dt).values()):.4g} > eta={eta}")
    graph.step(dt, external_injections)
```
W1/W1.5 已有测试继续调用 `graph.step()` 本身（不受影响）；W2A 起的新代码用 `guarded_step()`。暂不做自动子步进（批判自己也建议"等 W4 跨尺度标定后再决定"）。

**`DEFAULT_KAPPA_0` 命名拆分**（`dynamic_thermal_field.py`，批判八点4）：保留 `DEFAULT_KAPPA_0` 作为向后兼容别名（W1.5 测试已引用，不破坏），新增更精确的命名 `SEAWATER_DIFFUSIVITY_REFERENCE`（= 现有 `_TAU_REF_STEPS` 推导链的说明性别名）+ `NORMALIZED_KAPPA_DEFAULT`（= `DEFAULT_KAPPA_0` 的新首选名），docstring 说明二者关系，避免单一常量名同时暗示"机制测试参数"和"现实物理参数"两种角色。

### 16.4 W2A 测试清单（采纳批判五测试列表，适配为具体断言）

新文件 `nexus_v1/tests/test_thermal_source_coupling.py`，命名 T-TSC-1~6：
1. Locator 权重非负且和为 1（多组随机世界坐标 + 现有 `build_fibonacci_shell_graph` 测试图）；
2. 总注入量等于热源本步释放功率（`Σcouple(...).values() == source.release(dt)`，浮点误差内）；
3. 热源剩余能量正确减少（`energy_remaining` 前后差 = 释放量）；
4. 热源耗尽后不再注入（`release()` 返回 0，`energy_remaining` 不变）；
5. 移动热源位置后，注入总量仍精确等于释放功率（不要求"连续无跳变"这种更难验证的性质，只验证每次查询各自守恒——已经是批判清单第5点的核心可验证部分）；
6. `guarded_step()` 对不稳定配置在执行前拒绝（复用 T-DTF-7 的构造方式，断言 `raises ValueError`，且断言失败时 graph 状态未被修改——即 fail-fast 真正发生在 `step()` 调用之前）。

### 16.5 范围声明

W2A 完成后（全部 T-TSC 通过 + 全量回归 21/21 + 契约不劣于父提交），**W2B（皮肤热交换）仍需再次向用户确认**——这不是本轮批准范围，第十四节/十五节确立的"分阶段确认"纪律在 W2 内部同样适用，不因为已经启动 W2 就对 W2B/W2C 免检。
