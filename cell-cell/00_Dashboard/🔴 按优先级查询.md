---
tags: [MOC, Dataview, 查询, 优先级, P0, P1, P2]
type: query
date: 2026-06-14
---

# 🔴 按优先级查询

> P0 = 阻塞关键路径 | P1 = 架构修复 | P2 = 待推进

---

## 🚨 P0 — 项目阻塞项

```dataview
TABLE concepts, type, aliases
FROM ""
WHERE contains(concepts, "P0") OR contains(tags, "P0")
SORT date DESC
```

---

## 🔴 P1 — 架构修复

```dataview
TABLE concepts, type
FROM ""
WHERE contains(concepts, "P1") OR contains(tags, "P1")
SORT date DESC
```

---

## 🟡 P2 — 待推进

```dataview
TABLE concepts, type
FROM ""
WHERE contains(concepts, "P2") OR contains(tags, "P2")
SORT date DESC
```

---

## 🔥 当前执行线 (顺序)

| 序 | 编号 | 任务 | 状态 | 查询词条 |
|----|------|------|------|----------|
| 1 | B.06 | ν_th = dE_thermal/dt | ❌ | `contains(concepts, "B06")` |
| 2 | — | 基底噪声 | ❌ | `contains(concepts, "基底噪声")` |
| 3 | — | 抗饱和 | ❌ | `contains(concepts, "抗饱和")` |
| 4 | C3' | 环流耦合 | ❌ | `contains(concepts, "C3")` |
| 5 | — | L2 反馈修复 | ❌ | `contains(concepts, "L2反馈")` |

---

## 🧩 降解项追踪

```dataview
TABLE date, type
FROM "docs/degradation_registry" OR "docs/fix_registry"
WHERE date
SORT date DESC
```

---

## 🔀 待决决策 (9 项)

```dataview
TABLE concepts
FROM #决策
WHERE date
SORT date DESC
```
