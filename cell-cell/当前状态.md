# 当前状态 — 接力文档

> 每次会话结束时更新此文件，新会话开始时先读此文件。
> 最后更新：2026-06-25

---

## 工作目录

**`J:\cell-cc\cell-cc-other\`** — 所有代码修改在此目录。不是 `J:\cell-cc\nexus_v1\`。

运行命令必须从 `cell-cc-other/` 目录：
```bash
cd /j/cell-cc/cell-cc-other
PYTHONIOENCODING=utf-8 python -m nexus_v1.tests.test_regression
```

---

## 已完成

### STDP 冷启动实验 — 五补丁 + Phase 1 + Phase 2 + metrics（已提交至 `273cb9f`）

| 补丁 | 文件 | 状态 |
|------|------|------|
| A：AGC→Langevin（去均值→AGC乘→RMS钳位） | `circuit/variant_adapter.py:703` | ✅ |
| B：TemporalBindingLayer（τ_w=30步，仅前庭） | `components/binding_temporal.py` | ✅ |
| C：YolkSac（λ=0.002/步，初始200单位） | `components/yolk_sac.py` | ✅ |
| D：DADifferentialGate（η_da=7.5，clip=5.0） | `components/da_differential_gate.py` | ✅ |
| E：Efference 抑制比例监控（INFRA） | `circuit/variant_adapter.py:1031-1052` | ✅ |
| Phase 1：process_hunger 禁用 | `components/spinal_reflex.py:219` | ✅ |
| Phase 2：Binding→Motor 侧通道 | `circuit/variant_adapter.py:1054-1069` | ✅ |
| metrics：yolk_level / yolk_depleted / efference_supp_ratio | `circuit/variant_adapter.py:885-887` | ✅ |
| Phase 3 实验脚本 | `tests/exp_phase3_1M.py` | ✅ |

**回归验证：21/21 PASS**
**熵审计：5/6 信号深度**（Motor层静默，Col→Motor 弱耦合已知问题）

### 关键属性确认（VariantCircuit）

- `self.yolk_sac` — YolkSac 实例 ✅
- `self.da_gate` — DADifferentialGate 实例 ✅
- `self.binding_layer` — TemporalBindingLayer 实例（注意：不是 `_binding_cells`）✅
- `self.agc` — AGC 实例 ✅
- `self._efference_supp_count/total/ratio` — Patch E 监控计数器 ✅
- `ms.yolk_level` / `ms.yolk_depleted` / `ms.efference_supp_ratio` — metrics 字段 ✅

---

## Phase 3 — 1M 步长程实验（已完成）

**完成时间：** 2026-06-25，6113s / 101.9min  
**最终报告：** `cell-cell/工作报告/Phase3-STDP冷启动-最终报告_2026-06-25.md`

### 验收结果

| 标准 | 值 | 状态 |
|------|-----|------|
| CR1: max(Δw_ij)>0.02 | 0.4004 | ✅ PASS |
| CR2: Δx(200k)>0 | +0.0232 | ✅ PASS |
| CR3: min(fill)>0 | 0.0000 | ❌ FAIL |
| CR4: R_supp<0.9 | 0.0000 | ✅ PASS |

**VERDICT: PARTIAL — 3/4 PASS**

### 关键发现
- therm 束权重跳变（100k前）：0.101 → 0.497（Δ+0.40，STDP 工作正常）
- 四方向 therm 束差异极小（@1M：front 0.451 vs back 0.449 vs right 0.449 vs left 0.443）
- 代谢崩溃 @270k：yolk补充速率 < 基础代谢，yolk不够撑到首次有效进食
- DA 最大脉冲@960k: 0.3350，引发 fill=0.0013 短暂回升后立即归零

---

## 当前任务：Phase 4 设计（待确认）

**动手前需确认三问：**
1. `ThermalMembrane` 是否给 front/back/left/right 提供差异化梯度激活？
2. `basal_drain` 当前值和来源（方案B依赖此值）
3. `body.position` 初始化逻辑在哪里控制？

**推荐方案（Phase 4 草案）：**
```python
yolk_initial_level = 500.0   # 200 → 500（维持至 250k步）
initial_body_dist  = ≤5.0    # 紧靠热源起步
total_steps        = 500_000  # 先500k验证
```

## 待做

- [ ] 确认上述三问（查代码或问用户）再动手设计 Phase 4
- [ ] 若 ThermalMembrane 无空间分辨，需先修复传感器架构

---

## 关键文档位置

| 文档 | 路径 |
|------|------|
| 实现方案（批次1-4） | `cell-cell/claudecode方案/STDP冷启动实验-批次*.md` |
| 裁定文档 v3 | `cell-cell/交叉方案/决策文档 — STDP冷启动实验-五补丁最终裁定（v3 — 含量纲修正）.md` |
| 冷启动实验总方案 | `cell-cell/交叉比对/STDP独立涌现趋热行为 — 冷启动实验方案.md` |
| Phase 3 日志 | `cell-cell/工作报告/phase3_1M_run.log` |
| 项目规则 | `nexus_v1/RULES.md` |

---

## 注意事项

- `cell-cc-other/` 与主 `nexus_v1/` 是两套独立代码，勿混淆
- 运行测试必须 `cd /j/cell-cc/cell-cc-other` 后再执行
- Phase 3 脚本使用 `DT=0.001`（匹配 η_da=7.5 标定公式）
- `jq` 已安装（Warp hook 依赖）
- 模型档位：`opusplan`（plan 模式用 Opus，否则 Sonnet）
