# 工作报告：熵账本修复 + 全量测试

> **时间戳**: 2026-06-15
> **版本**: nexus_v1 v1.7.2
> **Regression**: 21/21 PASS（所有修复后持续验证）

---

## 一、已完成工作（完整清单）

### 全量实测
- 4 入口 + 37 test_*.py = 41 项，19 PASS / 16 FAIL / 1 TIMEOUT
- 数据：`cell-cell/test_runs/run_v1.7.2_2026-06-15/`
- 报告：`cell-cell/报告/全量全链路实测报告_v1.0_2026-06-15.md`

### 规范校验（V01-V15）
- 报告：`cell-cell/报告/熵账本构建规范_代码校验报告_v1.0_2026-06-15.md`
- V03/V09/V13 是规范错误（基于过时代码），不执行
- V01 修复方案错误（见下"未完成"部分）

### 熵账本修复（已完成）

| 条目 | 内容 | 文件 |
|------|------|------|
| V02 ✅ | `_effective_activity()` 已有 FIX(V02)（已存在） | governance/ledger.py |
| V04 ✅ | KCL 两处直接 charge 写绕过 → 补 `_q_in/_q_out`，imbalance 归零 | variant_adapter.py L744, L794 |
| V05 ✅ | K_ema > 10000 告警（DEG-KEMA）接入 check_anomalies() | energy_ledger.py |
| V06 ✅ | Col 全零 DEG-COL 告警接入（check_anomalies 已有，现已调用） | variant_adapter.py |
| V08 ✅ | DA 热耗散 > 50% 告警接入（同上） | variant_adapter.py |
| V12 ✅ | ρ 向量从二元升级为 4 分量概率单纯形 (p,r,x,u) sum=1 | ledger/toprxin.py |
| V14 ✅ | 学习冻结 DEG-FREEZE 告警接入 _ledger_pre_step() | variant_adapter.py |
| V15 ✅ | GuidedConstructionAuditor + _ledger_alerts 实例化 | variant_adapter.py |
| — ✅ | Transfer entropy 改用 spike_proxy（pre_trace）修复虚假零相关 | energy_ledger.py |
| — ✅ | test_chain_diagnostic.py conductance() → conductance 测试 bug | tests/test_chain_diagnostic.py |

---

## 二、未完成工作

### V01：体感层不可见（⚠️ 架构决策待定）

**状态**：规范修复方案本身错误（`self.somatosensory` 不存在），需先做架构决策。

**两条路**：
| 选项 | 描述 | 影响 |
|------|------|------|
| A：确认 ThermalMembrane 方案 | 当前 therm 轴已追踪（enc_reg_therm/col_therm 在熵账本中），宣布 V01 已通过 therm 轴覆盖解决 | test_smoke_soma/test_skin_patches 仍 FAIL（这两个测试本来就针对 SomatosensoryChain） |
| B：实例化 SomatosensoryChain | 在 VariantCircuit.__init__ 中加 `self.somatosensory = SomatosensoryChain()`，接入 get_all_neurons()/get_all_bundles() | 12 个神经元（Therm+Noci+Relay×4 patch）纳入熵账本；test_smoke_soma/test_skin_patches 可能 PASS；B.06 热趋性开发前提 |

**文档要求**（`熵账本构建规范-约束与纪律.md` 四）：
> "V01 (体感接入): **必须在 B.06 开发之前修复**。B.06 添加热感知链时如果不接入熵账本，会制造新的盲区。"

**结论**：若计划开发 B.06 热趋性，必须先选定路线并修复 V01。

---

### V07：EntropyLedger 体感层分类缺失

依赖 V01。V01 选 B 后，在 energy_ledger.py 的 layer 分类里加 `Soma_Therm`/`Soma_Noci`/`Soma_Relay`。

---

### O1：Motor 层不激活（系统行为问题，非熵账本问题）

**根因链**：侧向抑制过强(0.3) → Col 不放电 → STDP 迹为零 → Col→Mot 权重不增 → Motor 无电流(0.0027 vs 需要≥0.1)

**修复路径（待执行）**：
- R6：调低 LateralInhibition strength（需查文献确认合理值）
- 或：提升 Col→Mot synapse_gain

---

### 纪律性检查清单（当前状态）

```
✅ V02 修复: GovernanceLedger 增益链一致（已存在）
⬜ V01 修复: 体感层接入（待架构决策）
✅ V04 修复: KCL 电荷账本归零
✅ V05 修复: K_ema/影子发散被记录
✅ V06 修复: Col 全零被记录
⬜ V07 修复: EntropyLedger 体感层分类（依赖 V01）
✅ V08 修复: DA 热耗散异常被记录
✅ V11 修复: Xin 守恒已实现（noether_probe 有实现）
✅ V12 修复: ρ 向量升级为连续测度（4分量单纯形）
✅ V14 修复: 学习冻结告警
✅ V15 修复: 异常报告可从 _ledger_alerts 读取

完成 9 / 11（V01/V07 待架构决策）
```

---

## 三、修改的代码文件

| 文件 | 修改内容 |
|------|----------|
| `nexus_v1/circuit/variant_adapter.py` | import + GuidedConstructionAuditor实例化 + _ledger_alerts + V04 KCL双处修复 + check_anomalies接入 + DEG-FREEZE接入 |
| `nexus_v1/ledger/energy_ledger.py` | _layer_spike_proxy + _k_ema_history + transfer entropy修复 + A4 K_ema alert + print_report更新 |
| `nexus_v1/ledger/toprxin.py` | RhoVector 4分量单纯形 + BundlePhaseIntensity.rho + mean_rho + summary输出 |
| `nexus_v1/ledger/__init__.py` | 导出 RhoVector |
| `nexus_v1/tests/test_chain_diagnostic.py` | conductance() → conductance（测试 bug 修复）|
| `j:\cell-cc\CLAUDE.md` | 追加 bash 分类器设置说明 + 00_Dashboard 搜索说明 |

**未改动**：neuron.py / bundle.py / hebbian.py（物理核心）

---

## 四、FAIL 根因速查

| 分类 | 测试 | 根因 |
|------|------|------|
| Motor不激活 | test_motor_isolation, test_chain_diagnostic, test_thermotaxis* | Col→Mot电流0.0027，侧向抑制断链 |
| 体感链未接入 | test_smoke_soma, test_skin_patches | SomatosensoryChain未实例化 |
| 影子层发散 | test_shadow_stress, test_shadow_expansion | K_ema~49k，Shadow Col全零 |
| 组件断言 | test_entropy_ledger, test_ecm_vascular等6项 | 各组件内部断言，需逐条查 raw/ |
| test_pnn_critical | — | TIMEOUT >300s |

---

## 五、环境备忘

- 权限：`C:\Users\shaoc\.claude\settings.json` → `permissions.defaultMode: "acceptEdits"`
- 全量测试：`python run_all_tests_capture.py`
- 回归：`python -m nexus_v1.tests.test_regression`

---

## 版本链

| 版本 | 时间戳 | 内容 |
|------|--------|------|
| v1.0 | 2026-06-15 早 | 全量实测 + 规范校验 + R3/R4/R5 |
| v1.1 | 2026-06-15 中 | energy_ledger 深度修复（spike_proxy + K_ema alert）|
| v1.2 | 2026-06-15 晚 | V04 KCL归零 + V12 ρ单纯形 + test_chain_diagnostic修复 |
