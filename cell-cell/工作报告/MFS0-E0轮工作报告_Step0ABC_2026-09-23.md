# MFS0-E0 轮工作报告 — Step 0 / A / B / C

日期：2026-09-23
执行依据：`cell-cell/交叉比对/MAINLINE V2 — MFS0-E0 修订执行方案.md`（34 节，
status=ADOPTED_WITH_AMENDMENTS，R-1~R-4 已裁定）+ 我方
`cell-cell/交叉比对/MFS0-E0方案评判_2026-09-23.md`（E-1~E-12 全部 INCORPORATED）
+ 用户 2026-09-23 两项开工前裁定（R-3 落地方式、τ_e 依据更换）
代码路径：`research/memory_feedback_substrate_v0/`（新建，6 个脚本）；
`research/posterior_v0/`（仅终态追加式留痕，数值未动）
数据路径：`research/memory_feedback_substrate_v0/data/`（8 个 JSON）
关联提交：`82140c8`（R-3）/ `9c4dfb8`（Step 0）/ `6eb9f75`（Step A）/
`061d661`（Step B）/ `0454443`（Step C）

---

## 一、目标（总览）

MFS0-E0 是对我方 Posterior-0 终裁建议中 **D-2 论点**的元件级回应——「载体级加法性是
纯 RC + 全 frozen + 禁塑性的架构必然，要取得载体级重组织必须先开元件级设计轮」。本轮问：

> 一个已资格化的 G1 relation occurrence，能否留下有限的物理修改资格 `e`；后来发生的
> 另一个真实事件能否经独立物理状态 `M`，把这段历史选择性沉积为持久结构变化 `Δw`？

**本轮范围由方案 §34 自限**：`READY_TO_EXECUTE_STEP_0 = YES` /
`READY_FOR_MAIN_FACTORIAL = NO` ⇒ 只做 Step 0 / A / B / C；§31 禁跳步
（A/B/C 未 PASS 不得进 factorial）。Step D~H 与 hold6 属下一批。

另含一项前置动作：**RULING-P0-1 落地**（R-3）。

---

## 二、实际改动

### 2.1 Posterior-0 终态降格（R-3，追加式留痕）

| 文件 | 改动 |
|:---|:---|
| `research/posterior_v0/data/qualification_summary.json` | 原 `terminal_state` 区块**原样保留**（记录 2026-09-21 测量时终态）+ 加 `superseded_by` 指针 + 新增 `ruling_applied` 区块承载生效终态 |
| `research/posterior_v0/POSTERIOR0_FINAL_RULING.md` | 顶部加裁定提示 + 文末加《裁定落地》节 |

生效终态：`effective_terminal = C` / `POSTERIOR_EFFECT_ADDITIVE_ONLY = TRUE (at carrier)` /
`REORGANIZATION_V0_QUALIFIED = FALSE` / `READY_FOR_POSTERIOR_1 = FALSE` /
`POSTERIOR0_TERMINAL_C_FROZEN = TRUE`，**+ `THRESHOLD_READOUT_INTERACTION` 保留为正发现**。

`(at carrier)` 限定为必需：方案 §34 对 C 的定义文字是 `I_W = 0`，而实测
`I_W^traj(act) = 1.651e-2 ≫ 0` 且因果闭合齐全，不带限定会与数据直接矛盾。

**数值完整性**：六门数值、轨迹、CSV、`p0_hold_seal.json` 逐字节未改（`git status` 仅两个
文档文件变更）。

### 2.2 新建 `research/memory_feedback_substrate_v0/`

| 文件 | 作用 |
|:---|:---|
| `mfs0_common.py` | TYPE:INFRA 共享层——预注册常量、三条物理装配（e / M / 写入门）、两个机器级守卫、预算账本 |
| `preregistration.py` | Step 0：`preregistration.json` + `frozen_upstream_seal.json`（14 目标 SHA256）+ `budget_ledger.json` |
| `eligibility_physical.py` | Step A：`Past → e` 资格化（MFS0-M2） |
| `semantic_erasure_audit.py` | Step B1：语义擦除审计 |
| `modulation_physical.py` | Step B2：`Later → M` 资格化（MFS0-M3） |
| `persistent_state_audit.py` | Step C1/C3：`z=w` 五问 + RailLatch 正对照 |
| `readout_feasibility.py` | Step C2：M5 可行性的开工前判定（关键门） |

**production 零改动**——全部元件为既有原语（同 D2-1 `TYPE:HYBRID` 先例）：

```
Past : RelationOccurrencePortV1 → build_phase_drive → RelationInputNeuron
       → frozen SynapticBundle → Neuron(_membrane=Capacitor)  ⇒ e = V_{C_e}
Later: site23 window → build_phase_drive → RelationEventAdapter
       → PhysicalEntryGate(b↑) → PhysicalHistoryKernel(H_τ^Δ)  ⇒ M = h^Δ
Write: FET_e.conduct(e) · FET_M.conduct(M) = I_write → Memristor.update → w
```

两个机器级守卫（非注释承诺）：`assert_no_step_era_import()`（禁继承 bundle 步制常量）、
`assert_paths_independent()`（对象级 id 检查，兑现 R-1 禁 `W→H_τ^Δ`）。

### 2.3 两项开工前修正（用户裁定）

**(a) τ_e 依据更换（值不变 0.300 s）**。方案 §4.2 的推导「旧 eligibility 300 steps ×
dt 0.001 = 0.300 s」不成立：`bundle.py:126 eligibility_tau=300` 进入
`decay = 1 − dt/τ`（:459-460），dt=0.001 下等效 **300 秒**，差 1000×。改由两条独立依据：
(i) BIO 突触前残余 Ca²⁺ 亚秒时标（Zucker & Regehr 2002，项目内同引用链见
`history_kernel.py` Q1）；(ii) **STRUCT 决定性约束**——资格窗必须短于它所标记的关系发生
自身的衰减窗，否则 NC3（`e_W(t_Δ)≈0`）在现有 occurrence 库存内无法构造；
`τ_e=0.300 < τ₂=0.456 < τ_h=0.600`。

**(b) 写入极性 = DEPLETION（执行方决定，方案未指定）**。`w∈[0,1]` 而 R-4 冻结
`w₀=0.90` ⇒ 向上仅 0.10 余量、向下 0.90 余量；项目已登记高 w 撞 `w_max` 钳位→假性效应的
失败模式。实现 `update(pre=0.0, post=1.0) ⇒ dw = −0.5·I`。诚实条款：pre/post trace 在此
只承担极性与标度，是 API 常数不是物理量，乘法的物理性全部由 TIER-2 双 FET 承担。

---

## 三、发现的真实问题

### 3.1 WRITE_DT_CORRECTION — `Memristor.update` 缺 dt 因子（新发现，E-10 同族）

`Memristor.update` 的实现是 `dw = 0.5·current·(pre−post)` 后直接 `w += dw`
（`semiconductor.py:246-280`），**没有 dt**。但它自称 Strukov 型，而 Strukov 方程是
`dw/dt ∝ i(t)`，离散化必须是 `Δw = k·i·Δt`。dt=1.0 旧约定下两者等价；在本轮冻结的
`dt_G=0.001` 下放大 1000×——实测重叠窗内 `I_write` 峰 ≈ 1.0 ⇒ 单步 `dw ≈ −0.5` ⇒
**一步即撞 `w_min` 钳位**。

处置：本轮不改 production，在调用侧传 `current = I_write·dt` 补上时间积分。该修正是
**量纲正确性而非效应量调参**：对所有臂、所有 timing 一视同仁，且在任何 factorial 之前确定。

这是评判 E-10 所述步制地雷的第三个实例（前两个：`bundle.py` 的
`eligibility_tau`/`da_ema_tau`；`compensation.py` 的全部 τ 注释）。**建议下轮登记为新 DEG**
（本轮未登记，因未修改 production 行为）。

### 3.2 我方 E-4 的数值自纠

评判 E-4 称 `w₀` 从 0.109 移到高 w 端可获"三个数量级"改善。**该表述不准确**：990 是
`w→1.0`（`R→r_min=0.1`）处 `dG/dw` 的绝对值，不属于 `w₀=0.90`（`R=1.09`）。本轮实测的
operative 数字：

| 量 | `w=0.109` | `w₀=0.90` | 改善 |
|:---|---:|---:|---:|
| 相对灵敏度 `dG/G per dw` | 1.1098 | 9.0826 | **8.18×** |
| 绝对灵敏度 `dG/dw` | 0.1244 | 8.3326 | 66.98× |

### 3.3 线性外推在本轮 Δw 量级失效

`ΔG/G` 起初用线性式 `rel_w0·Δw`。在 `Δw=2.25e-1`（w: 0.90→0.675）处线性外推给 −204%，
而精确值 `(G(0.675)−G(0.90))/G(0.90) = −67.1%`。`G(w)` 是双曲的，已改为精确式并标注
`linear_approximation_valid`。

### 3.4 Step B 首跑观测窗不足（非物理失败，已诚实登记）

首跑 `t_total=12000` 时 `finite_decay_to_floor` FAIL——M 末值 `2.13e-6 > ε_M`。解析核对
`ln(0.998/1e-6)·600 = 8288` 步 ⇒ 需 `t ≥ 12450`。延长观测窗至 14000 后实测存在窗尾
**12450，与解析预测精确吻合**。`τ_M / w₀ / amplitude / timing` 一律未动；首跑两臂登记为
`attempted_run`，重跑两臂正式计费。

### 3.5 语义扫描器首版缺陷（已修 + 自检）

扫描器把 `build_modulation` docstring 里的**否定性声明**（"无 valence——不表示
reward/punishment"）误判为符号引用。根因：跳过逻辑只按行首 `"""` 判断，未跟踪多行
docstring 状态。已改为 `_code_lines()` 跟踪三引号开合，并自检确认真实代码引用仍会被抓到。

---

## 四、测试结果

```
回归状态：test_regression 21/21 PASS（exit 0）；contracts 13/15（失败集合 ⊆ 父提交失败
集合，C2/C3 ⊆ DEG-004，级联根因行明示 C2(HC)→C3(Aff)）；TSS version_pairing PASS /
fast 43 passed
```

### 四门判定

| Step | 门 | 判定 | 关键实测 |
|:---|:---|:---|:---|
| A | **MFS0-M2** Physical Eligibility | **PASS** | `e_peak` W=1/W=0 = 3.305369 / **0.000e+00**；`τ_e` 实测 **0.300000 s，误差 0.0000%**；`t_peak`=3970=W 窗末；电荷 in/out=0.957743 |
| B1 | SEMANTIC_ERASURE_AUDIT | **PASS** | 10 个 valence 污染点逐条拒绝（附 production 坐标）；M 链 5 源 × 10 禁用符号 = **0 命中** |
| B2 | **MFS0-M3** Physical Later Modulation | **PASS** | `M_peak` Δ=1/Δ=0 = 0.998335 / **0.000e+00**；`τ_M` 实测 **0.600000 s，误差 0.0000%**；collector 发放 60 次但 **b↑=1**（t_open=1204 > 窗长 639） |
| C1/C3 | 五问 + 正对照 | **PASS** | Q1–Q5 全 OK；同 w 重跑响应差 **RMS = 0.000e+00 逐位**；零输入 5000 步 w 漂移 = **0.000e+00** |
| C2 | **READOUT_FEASIBILITY** | **PASS** | sham-repeat RMS = 0.000e+00（replay floor 精确为零）；`ε_Q` 冻结 = 1e-6 |

### 两个窗口（预注册要求分别报告）

| 窗 | 区间 | 长度 |
|:---|:---|---:|
| e 存在窗 `{t: e>ε_e}` | [2618, 8473] | 5856 步 |
| **e 写入可达窗** `{t: FET_e.conduct(e)>0}` | **[2893, 4689]** | 1797 步 |

二者相差 3.3×——**存在 ≠ 可用**。写入可达窗非空是 Step D 能否构造 NEAR 的前置条件，
本轮实测为真，不构成阻塞（对比 Posterior-0 的时点硬阻塞）。

### 预期 Δw（用 Step A/B 已资格化实测包络积分，不引入新自由参数）

| later 候选 | 窗 | 写入步数 | `I_peak` | 预期 \|Δw\| |
|:---|:---|---:|---:|---:|
| s23_ov | [2927, 3566] | 722 | 0.2462 | 6.08e-2 |
| s23_hold_timing | [3542, 4181] | 722 | 1.1958 | **2.25e-1** |
| s23_lag | [4159, 4798] | 528 | 1.0076 | 6.81e-2 |
| s23_far_b | [4577, 5216] | 110 | 0.0926 | **2.22e-3** |

四者均落在 e 写入可达窗内；`|Δw|` **跨两个数量级且强依赖 later 时点**——这正是 M4 主判据
（timing selectivity，评判 E-1 改判后的判据）所需的区分度，Step D 选点有充分依据。

### ε_Q 的冻结依据（预注册 `deferred_to_step_C` 的兑现）

`ε_Q = 1e-6`。方案 §18 优先级第一条 deterministic replay floor **实测为精确 0**
（sham-repeat RMS = 0.000e+00），退到数值精度地板，同 Posterior-0 口径。

**与 LIM-RPREC-READOUT-001 的 1% 不是同一种东西**：那是 r_prec 轮预注册的效应量判据，
其读出要与训练期积分稀释（环节 B 35.8×）在 live 学习回路里竞争；MFS0 是确定性重放，
任何非零差异皆可分辨。**这是阈值含义的差别，不是对它的放宽。**

### 预算

`new_g0_trajectories 0/0` / `eligibility_calibration 2/6` /
`modulation_calibration 2/6 (+2 attempted)` / `persistent_baseline 3/6`；
Step D 之后各项全部 0。

---

## 五、冻结状态

- **Posterior-0**：`POSTERIOR0_TERMINAL_C_FROZEN = TRUE`（RULING-P0-1，2026-09-23）。
  按 §3 R-3，MFS0 的结果**不得反向修改**该结论。禁 reopen Posterior-0 / 禁 Posterior-1。
- **G0 / D2-0 / D2-1**：全程 READ_ONLY。`frozen_upstream_seal.json` 已封存 14 个目标的
  SHA256，收口重算要求 `BEFORE == AFTER`（下一批执行完毕时验证）。
- **本轮冻结且不得回调**：`MULTIPLICATION_TIER=2`（含 LIMITATION 登记）、
  `M_SOURCE=H_tau_delta`、`TAU_E=0.300 s`、`INITIAL_W=0.90`、`W_METABOLIC_LEAK=OFF`、
  `ELIGIBILITY_AMPLITUDE_ENCODING=NO`、`NEW_G0_TRAJECTORIES=0`、`ε_Q=1e-6`。
- **R-2 强制 LIMITATION 已登记**：两因子由真实器件产生 + 任一阈下 DEG-019 硬截零
  （负例精确 0），但二者相乘在 Python 层仍是 `a*b`，未解串联节点 KCL/负载线
  （项目内唯一档 3 = `rail_latch.py:13-21`）⇒ **本轮不得宣称 fully emergent analog
  multiplication**。
- **w 的持久性身份**（方案 §8）：marginal continuous persistence / integrator-like，
  **不是** bistable latch（A8 F4 实测 λ=1.000，无回复力而非双吸引子）。

**门状态**：Step 0 / A / B / C 全部 PASS ⇒ 按方案 §34，
`READY_FOR_MAIN_FACTORIAL` 具备翻为 `YES` 的条件（待用户签收本批）。

---

## 六、未完成项

1. **Step D~H + hold6 + 六门终裁**（下一批）：Step D 冻结 NEAR / TIMING CONTROL →
   Step E 2×2 factorial + NC1~NC6 → washout → Step F 未来查询 → Step G w-only 移植 →
   Step H 未来通路阻断 → hold6 → MFS0-M1~M6 + 终态 A~F。
2. **Step 0 中 deferred 的三个阈值仍待冻结**：`ε_sel` / `S_min` / `predicted_I_z`——
   须在 Step D 选点之后、factorial 之前落值（§17 要求三阈值全部预注册；§16 要求
   `predicted_I_z` 先于测量给出）。
3. **WRITE_DT_CORRECTION 建议登记为新 DEG**（§3.1）：`Memristor.update` 缺 dt 因子。
   本轮未登记，因未修改 production 行为；若下轮确认影响面更广则正式登记（编号从 DEG-024 起）。
4. **上游 SHA 收口重算**：`frozen_upstream_seal.json` 的 `BEFORE == AFTER` 验证在
   全轮收口时执行（本批未到收口点）。

---

## 复现入口

```bash
# Step 0 / A / B / C（顺序不可颠倒，§31 禁跳步）
cd research/memory_feedback_substrate_v0
PYTHONIOENCODING=utf-8 python preregistration.py
PYTHONIOENCODING=utf-8 python eligibility_physical.py
PYTHONIOENCODING=utf-8 python semantic_erasure_audit.py
PYTHONIOENCODING=utf-8 python modulation_physical.py
PYTHONIOENCODING=utf-8 python persistent_state_audit.py
PYTHONIOENCODING=utf-8 python readout_feasibility.py

# 回归（repo 根）
PYTHONIOENCODING=utf-8 python -m nexus_v1.tests.test_regression
PYTHONIOENCODING=utf-8 python nexus_v1/run_contracts.py
PYTHONIOENCODING=utf-8 python -m tss.tests.test_version_pairing
PYTHONIOENCODING=utf-8 python -m pytest tss/tests -m fast -q
```

## 原始数据位置

`research/memory_feedback_substrate_v0/data/`：`preregistration.json`、
`frozen_upstream_seal.json`、`budget_ledger.json`、`eligibility_characterization.json`、
`semantic_erasure_audit.json`、`modulation_characterization.json`、
`persistent_state_characterization.json`、`readout_feasibility.json`
