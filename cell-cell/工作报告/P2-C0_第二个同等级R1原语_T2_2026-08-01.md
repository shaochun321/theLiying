# P2-C0：第二个同等级R1原语（T2，边 28↔23）

日期：2026-08-01
状态：完成
commit：cf01f06
评判依据：document - 2026-08-01T222037.115.md（"先复用现有结构模板构造第二条时间
关系，不同时展开空间、尺度和账本系统"）

---

## 一、目标

P2-B1X2全线冻结后，评判要求以最省成本的方式建立第二条同等级R1原语，为后续
P2-C1（关系-关系生成）准备两条独立的R1关系。明确不做：空间/尺度投影、账本
系统、新神经结构、参数搜索。

---

## 二、站点选择

直接读取 `FROZEN_THERMAL_SITES["t2_chain"]` 的第二条边：

- **T1（已有）**：NB1(31) ↔ CENTER(28)，d=1.0916，检测 A≺B（site_a=28，site_b=31）
- **T2（本轮）**：CENTER(28) ↔ NB2(23)，d=1.1612，检测 B≺C（site_b=28，site_c=23）

两条关系共享枢纽节点28，构成三点链 31↔28↔23，与 `t2_chain` 的几何定义完全吻合。
站点28在T1里是"第一个发放的站点A"，在T2里是"触发第二条关系的上游站点B"。

---

## 三、实际改动

新增 `nexus_v1/relations/temporal_r_prec_t2.py`（约230行）：

**RPrecCircuitT2（同 RPrecCircuitT1 结构完全对称）**：
- 所有参数直接复用T1标定常量（`_TAU_FAST_STEPS=50`/`SLOW=600`/`_R_LEAK_TRACE=5.0`等），
  无新标定——三问中Q3的答案是"直接复用T1已验证常量，站点对物理特性相同"
- 所有bundle_id加`t2_`前缀，所有属性名加`rprec2`前缀，与T1命名空间完全隔离
- `get_all_bundles()`正确覆盖把T2的12条关系bundle加入全局census
- `step_rprec2(dt)`与`step_rprec(dt)`完全对称

**RPrecCircuitT2Plastic**（对应T1Plastic）：
- 叠加`bundle_rprec2_to_da`（b≺c_fast → DA，STDP可塑），参数复用T1Plastic常量

---

## 四、发现的真实问题

本轮无新问题——纯结构复用，无需新标定。

唯一值得记录的是T-T2-3测试设计判断：本轮把T3的测试场景设定为"热源放在枢纽站点28上，温度300K"（与T1的forward场景完全相同），5000步内真实产生了`r_prec.b_prec_c_fast`类型的RelationOccurrence（t_detect=536），比预期更直接——因为站点28是T2里的"上游站点B"，加热28正好让B先于C响应，自然触发b≺c关系。

---

## 五、测试结果

| 测试 | 结果 |
|------|------|
| T-T2-1：RPrecCircuitT2独立构造（站点/collector/trace来自t2_chain） | PASS |
| T-T2-2：T1/T2可共存（12+12条bundle，交集=空集，命名无冲突） | PASS |
| T-T2-3：真实驱动产生RelationOccurrence（t_detect=536，type=b_prec_c_fast） | PASS |
| 全量回归 | 21/21 PASS |

---

## 六、冻结状态

```
P2-C0：第二条同等级R1时间关系原语（T2，28↔23），通过
  RPrecCircuitT2：参数复用T1，命名隔离，bundle census覆盖正确
  RPrecCircuitT2Plastic：可塑ℓ_out叠加，结构与T1Plastic对称
  资格：与T1完全相同的生产路径（wrap_collector_occurrence_tap +
        RelationFinalizer）可产生正式RelationOccurrence
P2-C1：两条R1关系组合（关系-关系生成），待执行
```

---

## 七、未完成项

- **P2-C1**：两条独立R1关系（T1: 31↔28, T2: 28↔23）的组合与不可约性验证
  ——评判最初定义：多条R1关系之间的关系-关系生成，即"A≺B AND B≺C → 是否涌现
  A≺C这条高阶关系"这类问题。T1和T2共享枢纽28，是进行关系-关系验证的天然场景。
- T2的E2E端到端测试（类似P2-B1X1d对T1的标定，确定性时序场景下T2可靠产生
  RelationOccurrence）——本轮T-T2-3用boosted场景验证路径可用，实际确定性标定
  留给P2-C1前或与P2-C1合并
