# 交叉报告核查总报告

**日期：** 2026-07-09  
**代码基线：** J:\cell-cc main @ 44d0add（variant_adapter.py 3734 行）  
**范围：** cell-cell/交叉报告/ 全部 11 份报告逐条核查 + 环境分叉问题

---

## 零、首要发现：代码副本分叉（信息遗留的根源）

**项目存在 5 份 `variant_adapter.py` 副本，行数从 1747 到 3734 分叉：**

| 路径 | 行数 | git commit | 状态 |
|------|------|-----------|------|
| `J:\cell-cc\nexus_v1\circuit\variant_adapter.py` | **3734** | 44d0add (main) | **权威最新，交叉报告引用此版** |
| `C:\...\worktrees\cell-cc\lechuguilla-firestorm\nexus_v1\` | 3560 | 2009cb2 | 工作树，较新 |
| `J:\cell-cc\cell-cc-other\nexus_v1\` | 1995 | — | 旧 other 分支 |
| `C:\...\worktrees\cell-cc-other\mule-deer-gulch\nexus_v1\` | 1925 | — | 旧工作树 |
| **`C:\...\worktrees\cell-cc\mallow-dust-devil\nexus_v1\`** | **1747** | **95a79b7 (Initial commit)** | **❌ 极旧，当前会话工作目录** |

**当前会话的工作树（mallow-dust-devil）停留在"Initial commit"，比 main 落后约 2000 行。** 本次审计首轮温感链路分析因误读该旧副本，产出了多项完全错误的结论（所有标识符"不存在"、"variant_adapter 只有 1747 行"）。事后在 J:\cell-cc 绝对路径重做，结论全部反转。

**结论：** 任何审计、开发、AI 辅助分析必须锚定 `J:\cell-cc` 绝对路径。`cell-cc-other` 和 `mallow-dust-devil` 工作树是历史快照，不反映当前代码状态。

---

## 一、11 份交叉报告逐条核查结果

### 1. DA_STDP_audit_2026-07-08.md — **最准确，推荐参考**

| 断言 | 代码实际（J:\cell-cc 行号） | 判定 |
|------|---------------------------|------|
| DA 浓度赋值 `variant_adapter.py:2224-2225` clamp(mean_da, 0, 1) | L2224-2225 逐字一致 | ✅ 准确 |
| `dopamine.step()` 不调用，注释 "Don't call" | L2226 注释一致 | ✅ 准确 |
| D2 类默认 `da_r_leak=100.0` vs 配置值 1.0，τ_D2=1s 生效 | neuron.py:180 默认 100；variant_adapter.py:656 配置 1.0；neuron.py:364-369 实例化传配置值 | ✅ 准确 |
| pre_trace 缺 (1-decay)，neuron.py:589 | 逐字一致 | ✅ 准确 |
| gain_factor() 双调用 @2483(突触) / @2686(学习) | 两处均命中 | ✅ 准确 |
| RPE 截断 `da_differential_gate.py:69` max(0,...) | 语义准确；变量名 `_rpe_da`/`_da_drive` 与现 `raw`/`_da_output` 轻微不符 | ⚠️ 语义准确/变量名过时 |
| 通路表（shadow w0.05/sg0.1; xin w0.1/sg0.5; relay_to_da stdp w0.1/wmax0.3/sg0.2/τ300/da_ema5000; hunger w0.04/sg1.0; satiety w0.3/sg-1.0; intake w1.0/sg0.05; cpg w0.1/sg0.1; shadow_nu w0.3/sg1.0; thermo_delta w0.1/sg1.0) | 逐条核对，**全部一致** | ✅ 准确 |
| DA 通路共 10 条（含 RPE 直注） | 代码确有 10 条（relay×4+shadow+xin+thermo_delta×12+shadow_nu+hunger+satiety+intake+RPE gate） | ✅ 准确 |
| eligibility_tau=300 vs da_ema_tau=5000 失配（10×） | bundle.py:105 τ=300；L121 τ=5000；relay_to_da 显式设 300（variant_adapter.py:3022） | ✅ 准确 |
| 所有"修复建议"均未实现 | grep 确认无 baseline_subtract/dDA/da_lr_gate/d1_gain_factor 实现 | ✅ 准确 |

**小结：** 最可靠的参考报告。唯一瑕疵是 RPE 段变量名轻微过时。

---

### 2. 核查.md — **准确（脚手架文档）**

逐条引用的标识符（relay_to_da、thermo_delta_to_da、shadow_to_da 等）在最新代码中全部存在且参数一致。作为审计清单使用准确。

**补充：** 核查.md 提出的"relay→enc 束"（§1.2）和"relay_to_da 两段结构"确实存在于最新代码（见报告 D 温感链路详述），但核查.md 未明确区分两条 DA 通路的差异（relay_to_da via proj 三级 vs thermo_delta_to_da 直连）。

---

### 3. DA_architecture_report_2026-07-08.md — **大体准确，两处需修正**

| 断言 | 判定 | 具体修正 |
|------|------|---------|
| DA 是三层系统（容器/神经元池/输入通路） | ✅ 准确 | — |
| DA 输入通路表（10 条） | ✅ 参数准确 | — |
| 温觉→DA 仅一条通路（relay_to_da） | ❌ **错误** | 实际为**两条**：`thermo_delta_to_da`×12（frozen phasic，transducer_neurons.py:315）+ `relay_to_da`×patch（STDP tonic，经 proj 三级，variant_adapter.py:2996-3034） |
| 温觉只进 DA，不进运动主链 | ❌ **错误** | 温觉通过 relay→enc→col→motor（4+8=12 条 bundle）和 HC-016A thermo→yaw 进入运动主链（见报告 D） |
| `dopamine.step()` 死代码 | ✅ 准确 | — |
| D2R ec50=0.3，d2_conductance=0.5 | ✅ 准确（variant_adapter.py:649-653） | — |

---

### 4. T092_analysis_report_2026-07-08.md — **结论成立**

| 断言 | 判定 |
|------|------|
| DA 始终>0，LTP gate 永远部分开启 | ✅ 准确（bc_current=0.1→baseline≈0.09；8条兴奋性通路） |
| 无基线减法（LTP ∝ DA_ema 而非 DA_ema-baseline） | ✅ 准确（bundle.py:409-411） |
| τ_DA_ema=5000 vs τ_eligibility=300 失配 10× | ✅ 准确 |
| 随机 DA 下权重更快饱和 | ✅ 结论合理（DA_ema 更高→LTP 更强） |
| 字符串比较 bug（大小写）已修复 | ✅ 准确（报告自述） |

**注：** 该报告基于 DT=1.0 实验，时标失配分析在 DT=1.0 下是正确的（不存在 DT=0.001 时的额外问题）。

---

### 5. 平行构建更新分析报告 (2026-07-07).md — **准确，路径过时**

| 断言 | 判定 |
|------|------|
| P0 改动（da_ema_tau=5000/lambda_metabolic=1e-6）bundle.py:112-130 | ✅ 准确（J:\cell-cc bundle.py L112-130 命中） |
| β=dt/(dt+τ) dt-safe 声明 | ⚠️ **误判**：τ 用"步"非"秒"，不是真正 dt-safe（见报告 B 详述） |
| T-073/T-039b/T-072 实验结论（权重保留率/DR5） | ✅ 实验输出（无法静态核实，但参数范围与代码一致） |
| 文件路径引用 `file:///D:/cell-cc/2026.7.6/` | ❌ **过时**：盘符/路径已迁至 J:\cell-cc | 

---

### 6. STDP_freeze_rootcause_2026-07-08.md — **机制准确，三处具体错误**

| 断言 | 判定 | 具体问题 |
|------|------|---------|
| 三因子路径不读 post_trace，bundle.py:389-435 | ✅ 准确 | — |
| 两因子唯一读 post_trace @bundle.py:439 | ✅ 准确 | — |
| CPG 化石通路存在，cpg_to_da frozen w0.1 sg0.1 | ✅ 准确（variant_adapter.py:3052-3065） | — |
| CPG 振幅"0.0011V" | ❌ **错误（差 10×）** | 代码注释 L3043/3046 写 amplitude=0.005，换算 I_peak≈0.011V，报告多了一个零 |
| pre_trace "20.5× 稳态放大"（neuron.py:589） | ⚠️ **DT 陷阱** | 推导前提是 dt=0.001；实验脚本 DT=1.0 时 decay≈exp(-50)≈0，pre_trace 归零，放大不成立 |
| D2R da_r_leak @neuron.py:96-98（写 96-98） | ⚠️ 行号差 1 行 | NOTE 注释在 L96-99 |
| d2_da_r_leak 配置值 @variant_adapter.py:657（写 657） | ⚠️ 行号差 1 行 | 实际在 L656 |

---

### 7. DA-STDP 系统修复方案（工程级）.md — **建议正确，三处字面错误**

| 断言 | 判定 | 具体问题 |
|------|------|---------|
| 所有修复（P0-1/P0-2/P0-3/P1-1/P1-2/P1-3/P2）均未实现 | ✅ 准确（grep 确认） | — |
| trace_tau_pre 字面值"0.02" | ❌ **错误** | 代码 neuron.py:94 字面值为 **20.0**（代码内部 ×0.001 转秒，但字段值是 20.0） |
| CPG 创建位置"variant_adapter.py:L620-640" | ❌ **错误** | 实际在 **L3044**（_da_cpg = CPGNeuron(...)） |
| RPE 段变量名 `eta_da × delta_fill`/`_rpe_da` | ⚠️ 过时 | 现 `da_differential_gate.py` 变量名为 `raw`/`_da_output`，计算逻辑相同但命名不符 |

---

### 8. DA=1.0 根因诊断与修复方案（修订版）.md — **最过时，数值推导有误**

| 断言 | 判定 | 具体问题 |
|------|------|---------|
| shadow_to_da @`D:/cell-cc/2026.7.6/.../variant_adapter.py#L2817-L2830` | ❌ **全部错误** | 盘符 D:→J:，路径含 2026.7.6 旧目录，行号 L2817 vs 实际 L2903-2921 |
| shadow_to_da w=0.05 参数值 | ✅ 准确（数值正确） | — |
| shadow I_max 推导基于 sg=1.0 | ❌ **过时** | shadow_to_da 现 sg=**0.1**（T-084 已降），导致 I_max 推导量级差 10× |
| Fix-1 shadow w=0.05→0.01 已实现？ | ✅ 仍为建议（代码仍 0.05） | — |
| satiety_to_da @L3054-3063，w=0.3 sg=-1.0 | 参数准确，行号过时（实际 L3145-3154） | ⚠️ 行号错位 ~90 行 |
| Fix-2 satiety sg=-5.0 已证不可逆 | ✅ 机制分析正确（参见 project_da_fix2_critique 记忆） | — |

**总评：** 诊断方向（shadow 正反馈、satiety 不足）在结构上仍成立，但所有行号为旧快照，数值推导因 sg 过时而差 10×。**不应直接引用此报告的行号或 I_max 数值。**

---

### 9. T060_T063_comprehensive_report.md — **参数准确，一处实质性错误**

| 断言 | 判定 | 具体问题 |
|------|------|---------|
| D1 arc bundle 参数（phasic→spinal：w0.1 wmax0.3 lr0.005 eligibility_trace） | ✅ 准确（variant_adapter.py:829-845） | — |
| T-043 交叉抑制 frozen w=0.020 sg=-1.0 | ✅ 准确（variant_adapter.py:892-899） | — |
| relay→yaw 直接通路（HC-016A，先天反射） | ✅ 准确（_init_yaw_bundles @3241） | — |
| **§4.2 表格：phasic_left→spinal_ccw 标注为 frozen** | ❌ **实质性错误** | 该束实为 **STDP+eligibility_trace**（variant_adapter.py:829-835，`_stdp_d1_cfg`）。此错误影响"方向性是结构而非学习"的核心论证——该束恰是可学习的 |
| 实验 DT=1.0 | ✅ 准确（exp_T060 L18: DT=1.0） | — |
| DR5≈50%、权重分化等实验数值 | 无法静态核实（运行时输出） | — |

---

### 10. T062_200k_report.md — **参数准确，元数据错配**

| 断言 | 判定 | 具体问题 |
|------|------|---------|
| 监控束 `bundle_d1_phasic_left_to_spinal_ccw`/`_right_to_spinal_cw` | ✅ 准确（variant_adapter.py:834-837） | — |
| wmax=0.3（`_D1_WMAX`） | ✅ 准确（variant_adapter.py:826） | — |
| 实验 DT=1.0 | ✅ 准确（exp_T062_200k_new_metrics.py:20） | — |
| **脚本名写 `exp_T060_new_metrics_baseline.py` STEPS=200,000** | ❌ **错配** | `exp_T060_new_metrics_baseline.py` 实际是 50k 脚本（L17: STEPS=50_000）；200k 对应的是 `exp_T062_200k_new_metrics.py` |
| 路径 `D:/cell-cc/2026.7.6/` | ❌ 过时（应为 J:\cell-cc） | — |

---

### 11. implementation_plan（T-090）.md — **准确（含自述风险）**

| 断言 | 判定 |
|------|------|
| T-090 4/4 PASS（E1=0.311, E2'=0.150, E3 w=1.0, E4 sg_asym=0%） | ✅ 报告自述实验结果，参数与代码（gated_reflex.py）一致 |
| 诈胡风险（权重饱和/开关式增益/E2'余量仅 0.15） | ✅ 报告已自述；T-092-T-099 诈胡拷打实验方案列出 |
| T-092 随机 DA 下权重同样饱和（DA-gating 失效） | ✅ 与 T092_analysis 报告结论一致 |

---

## 二、跨报告共性问题汇总

### 2.1 所有"修复建议"均未落地
grep 确认（J:\cell-cc 全库）以下修复**全部仅为纸面方案，代码中不存在实现**：

| 建议 | grep 关键词 | 状态 |
|------|-----------|------|
| pre_trace 归一化（乘 1-decay） | `(1-decay_pre)`, `baseline_norm` | **未实现** |
| 移除 CPG 通路 | cpg_to_da 仍在 L3044-3065 | **未实现** |
| 拆分 gain_factor/da_lr_gate | `d1_gain_factor`, `da_lr_gate` | **未实现** |
| 缩短 da_ema_tau（5000→500） | 仍为 5000.0（bundle.py:121） | **未实现** |
| baseline 减法（DA_ema - DA_baseline） | `baseline_subtract`, `DA_baseline` | **未实现** |
| dDA/dt 门控（变化率替换绝对值） | `dDA`, `da_derivative` | **未实现** |
| RPE 负相位（撤源时 LTD） | da_differential_gate 仍截断负值（L69） | **未实现** |
| hunger 由驱动改为调制 | hunger_to_da frozen w=0.04 sg=1.0 未改 | **未实现** |

### 2.2 文件路径普遍过时
多份报告引用 `D:/cell-cc/2026.7.6/` 旧快照路径。实际代码在 `J:\cell-cc\nexus_v1\`。所有行号按旧快照有 ±80~290 行的系统性错位。

### 2.3 DT 分裂未被任何报告点破
所有报告均未明确说明"当前 T 系列实验用 DT=1.0，而所有 τ/β 注释基于 DT=0.001"这一根本性分裂及其后果（见报告 B 详述）。

### 2.4 温觉架构认知不足
DA_architecture_report 等报告低估了温觉链路的复杂度。实际代码中温觉已建立了空间分辨率进运动主链的通路（relay→enc→col→motor + HC-016A + D1 arc），不只是进 DA（见报告 D 详述）。

---

## 三、结论矩阵

| 类别 | 代表条目 | 结论 |
|------|---------|------|
| **设计（物理正确）** | D2R τ=1s，relay_to_da 三级结构，HC-016A frozen 先天反射，Ia 互抑，T-043 交叉抑制 | 架构合理，按设计工作 |
| **Bug（代码错误）** | thermo_delta_to_da DA 通路漏报、T060 报告把 STDP 束标为 frozen、CPG 振幅数值差 10×、trace_tau 字面值误写 | 报告笔误或代码注释错误 |
| **化石（存在但无意义）** | CPC da_current（已被 da_gate 取代但未删）、dopamine.step()（从不调用）、CPG 通路（设计目的已改变） | 可清理但不影响当前功能 |
| **已过时** | D:/2026.7.6 旧路径引用、shadow sg=1.0 推导（现 sg=0.1）、DA=1.0 修订版全部行号 | 不应再直接引用 |
| **设计缺陷（未修）** | pre_trace 缺 (1-decay)、DA_ema 无基线减法、da_ema_tau/eligibility_tau 失配 10×、DT 分裂 1000× 漂移 | 已分析，修复尚未执行 |
