# TSS-0：时间/空间/尺度生成算子类型契约

日期：2026-08-03
状态：完成
commit：f62469c
评判依据：document - 2026-08-03T131304.153.md（主线纠偏）

---

## 主线纠偏（评判核心裁定）

正确主线：基础生成元 → 时间/空间/尺度生成元 → 耦合生成元，每级都是
"由物理结构实现的算子，不是学习结果"。

- 当前T1（28≺31）归位为Θ^(1)的一个**原型**，只覆盖"≺"一种时间投影
- 当前R2^fork归位为"关系活动的AND型再组合"，是高阶链路可行性实验，
  不等于耦合生成元体系，不继续扩展R2-R2
- 生成元存在资格⊥STDP/DA：非学习型生成元在STDP=0/DA=0时仍能完成
  完整算子链路
- **S0竞争路线暂停**：S0-a/b/bX1成果归类为"未来软竞争辅助支线"，
  不继续S0-c/d

---

## 实际改动

新增`nexus_v1/relations/generator_contract.py`（纯类型定义，不写动力学）：

- `GeneratorOperatorContract`：六项通用契约（输入生成元/物理载体/作用
  区间/可读取量/禁止决策字段/`exists_without_learning`标记）
- `GeneratorThetaBinding`：Θ^(1)映射，`build_theta_binding_from_rprec_t1()`
  绑定RPrecCircuitT1的真实trace/collector对象
- `GeneratorSigmaBinding`/`GeneratorLambdaBinding`：Σ/Λ纯类型占位
  （评判明确暂无真实资产，本轮不构造）

新增`test_generator_contract.py`，T-TSS0-1~4，4/4 PASS。

未触碰任何既有代码，未跑全量回归（评判"未触碰冻结代码时不跑完整回归"）。

---

## 冻结状态

```
TSS-0：Θ/Σ/Λ类型契约，通过
S0-a/b/bX1：暂停，归类为未来辅助支线（不删除，不继续推进）
下一步：TSS-1（第一条真正的空间生成算子）
```
