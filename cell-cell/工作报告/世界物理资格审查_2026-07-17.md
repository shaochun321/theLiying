# 世界物理资格审查（审查点1，差分格式）

## 1. 目标
批判二十六/二十七：P1-C2条件通过、P1大阶段可冻结，但不能立刻启动P2-A——
先执行"世界物理资格审查"（资格与升级登记表审查点1），回答3个问题+对
质量-焓平流/多点皮肤/驱动功账本三项独立裁定。

## 2. 实际改动
新增`test_world_physical_qualification_review.py`（T-WQR-1~3）：
- T-WQR-1：完整联合路径（source+diffusion+contact+OET+skin同时激活）
  步长收敛测试，`dt→dt/2→dt/4`对齐同一物理终点，用`dt/2048`做参考解。
- T-WQR-2：多点皮肤最小反例①——同总能量、不同世界节点分布（Γ1:
  node0=4.0,node1=0.0 vs Γ2: node0=2.0,node1=2.0）。
- T-WQR-3：多点皮肤最小反例②——同总量、不同接触次序（h分配互换）。

无生产代码改动（本轮是诊断/审查性质，不新增机制）。

## 3. 发现的真实问题
- **阻塞项已修复**：T-C2-6此前只验证纯扩散通道的步长收敛，未覆盖完整
  联合路径。T-WQR-1补齐后确认收敛性成立（`q_world`/`q_skin`误差随步长
  减半单调下降）。
- **多点皮肤反例坐实**（批判二十七预判的"可能较早触发"项，已用代码验证
  为真）：T-WQR-2/T-WQR-3两组反例，皮肤侧轨迹相对差异均为浮点精度量级
  （`1.6e-16`/`0.0`）——单点皮肤对"不同分布"和"不同次序"两类世界过程
  差异完全不可区分。这不是假设性风险，是实测确认的真实限制。

## 4. 测试结果
T-WQR-1~3全部PASS；与既有29项相关测试+回归21/21全部PASS。

## 5. 冻结契约（审查点1裁定）
| 项目 | 裁定 | 触发条件 |
|---|---|---|
| 质量-焓平流 | 带债务通过 | OET无法产生P2所需真实过程差异，或项目开始宣称真实介质输运 |
| 多点皮肤 | 带债务通过（触发条件已被反例坐实） | P2-A实现后若ξ^occ确实需要区分多源组合信息，升级到2-3点最小多点结构 |
| 驱动功·熵账本 | 带债务通过 | 结构/行为算子资格门前复审 |

**审查结论**：三项均带债务通过，**审查点1通过，P2-A可以启动**。当前
normalized世界对P2-A既定范围（单点局部发生时序：启动/峰值/burst/衰减）
已经足够；对"区分多源组合"这个更宽的能力则不足，已登记为多点皮肤的
触发条件。

## 6. 未完成项
- `JointThermalTrajectory.append()`无`(runtime_uid,plan_id,step_index)`
  交叉校验（非阻塞，登记为debt，轨迹开始承担标定证据/谱系/对照/算子
  资格证明之前需补上）。
- P2-A（真实发生元）未开始实现。
