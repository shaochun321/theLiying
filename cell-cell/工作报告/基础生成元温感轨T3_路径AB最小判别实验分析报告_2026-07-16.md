# 工程报告：基础生成元温感轨 T3——路径A/B最小判别实验分析报告（Gate D，编码前）

**日期**：2026-07-16（追加修订二：纳入交叉比对第五份批判，`dt=0` 只读方案取代 `norm_factor` 手算版；r_ρ 改为射影比例定义）
**类型**：分析报告（编码前判别实验，Gate D 强制要求）
**状态**：判别实验完成，路径B工程实现路径**已闭合**（见 §五 修订二），**未构造完整 r_part 生成元**——本报告只做路径择定，正式实施留待后续独立任务
**依据**：`cell-cell/claudecode方案/基础生成元双轨落地方案_v2_2026-07-16.md` 第四节 T3
**实验脚本**：`nexus_v1/tests/_calib_t3_path_ab.py`（一次性诊断脚本，同项目既有 `_diag_*.py`/`_calib_*.py` 惯例）

**结论修订摘要（纳入交叉比对第三份批判点8/9）**：交叉比对批判认为本报告最初提出的两种"共享池"修复思路（新增调用模式 / 两实例共享内部状态）都超出了"只用现有组件"的范围，因此判定"路径B物理机制胜出，但工程实现路径尚未闭合"。**已用代码实测反驳**：`DivisiveNormalizationReceptor` 本来就公开了 `pool_activity` 属性（`compensation.py:337-338`，非本次新增），只用它 + 已有的 `.normalize()` 方法就能实现合法的同步共享池，无需新增/修改任何接口。实测效果**优于**报告最初的顺序调用版（等强场景从 1e-4 级偏差降到精确为 0）。

**追加修订二摘要（纳入交叉比对第五份批判点1/2）**：第五份批判进一步指出，上一版修复虽然读的是真实池状态，但 `norm_factor=sigma/(sigma+pool)` 仍是在**调度代码里手算**了一遍 `.normalize()` 内部的除法公式，不算严格意义上"只用现有组件"。**已用代码验证更优方案**：`dn.normalize(i_a+i_b,dt)` 更新池一次，随后 `dn.normalize(i_a,0.0)`/`dn.normalize(i_b,0.0)`（`dt=0`）做只读——`dt=0` 时不注入电荷也不衰减，但仍会走 `.normalize()` 自身的除法逻辑算出结果，全程**不重写任何公式**。5 项接口合法性检验（池状态不变/等强对称/顺序无关/二通道比值精确/三通道比值精确）全部精确通过，详见 §五 修订二。**同时**，第五份批判指出 T3 目前证明的是"比例射线"（`y_i/y_j=I_i/I_j`），不是分量和为 1 的占比坐标——r_ρ 的正式定义改为**射影比例** `[y_1:y_2:...:y_n]`，详见 §六 修订。

---

## 一、任务背景

T3（r_part 参与竞争关系）要求编码前先做路径 A（减法抑制）/ 路径 B（分流抑制，复用 `DivisiveNormalizationReceptor`）的最小判别实验，用方案定义的两级验收标准评分，择定一条路径后才进入正式实施（Gate D）。

---

## 二、实验设计

用项目既有物理组件（非 Python 除法）分别搭建两条路径的最小标量模型：

- **路径A（减法抑制）**：`ξ_a, ξ_b → 独立兴奋通道 → 共同总量池(RC积分 I_a+I_b) → 共享减法抑制(synapse_gain=-1.0) → y_a=φ(I_a−w·pool), y_b=φ(I_b−w·pool)`。
- **路径B（分流抑制）**：复用既有 `DivisiveNormalizationReceptor`（`compensation.py:295`，Carandini-Heeger 分流抑制物理电路），**用同一实例依次对 I_a、I_b 调用 `.normalize()`**，使其内部 `_pool_voltage` 同时追踪两路输入，构成共享横向池版本。

8 个测试场景覆盖：静默、单通道A、单通道B（交换对称性）、等强、A占优2:1（含共同放大 1x/2x/4x）、极端3:1。

---

## 三、实验结果

| 场景 | I_a | I_b | 路径A y_a | 路径A y_b | A比值 | 路径B y_a | 路径B y_b | B比值 |
|---|---|---|---|---|---|---|---|---|
| 静默 | 0.0 | 0.0 | 0.0000 | 0.0000 | — | 0.0000 | 0.0000 | — |
| 单通道A | 1.0 | 0.0 | 0.8787 | 0.0000 | ∞ | 0.7301 | 0.0000 | ∞ |
| 单通道B(交换) | 0.0 | 1.0 | 0.0000 | 0.8787 | 0 | 0.0000 | 0.7301 | 0 |
| 等强 | 1.0 | 1.0 | 0.1091 | 0.1091 | 1.000 | 0.5750 | 0.5750* | 1.000* |
| A占优2:1 | 2.0 | 1.0 | 0.9877 | **0.0000** | ∞（失效） | 0.9484 | 0.4742 | **2.000** |
| A占优2:1(共同放大2x) | 4.0 | 2.0 | 1.9755 | **0.0000** | ∞（失效） | 1.2432 | 0.6215 | **2.000** |
| A占优2:1(共同放大4x) | 8.0 | 4.0 | 3.9509 | **0.0000** | ∞（失效） | 1.4720 | 0.7359 | **2.000** |
| 极端3:1 | 3.0 | 1.0 | 1.8664 | **0.0000** | ∞（失效） | 1.2105 | 0.4035 | **3.000** |

（*：等强场景下路径B的 y_a/y_b 实测存在 1e-4 量级的微小差异，见 §五 原因说明，非真实占比偏置）

---

## 四、两级验收评分

### 一级验收（必须达到）

| 标准 | 路径A | 路径B |
|---|---|---|
| 静默无虚假占比 | ✅ PASS | ✅ PASS |
| 单通道占优 | ✅ PASS | ✅ PASS |
| 交换对称 | ✅ PASS（精确对称） | ✅ PASS（精确对称） |
| 等强输出近似相等 | ✅ PASS（精确相等） | ⚠️ 近似通过（差异 1e-4 量级，见 §五） |
| 较大输入→较大输出 | ✅ PASS | ✅ PASS |

两条路径在一级验收上**均可通过**（路径B 的等强情形有极小的实现artifact，非本质缺陷，见 §五）。

### 二级验收（实验性目标，真正的分水岭）

| 标准 | 路径A | 路径B |
|---|---|---|
| 共同放大时排序保持 | ✅ 保持（y_a 始终>y_b=0） | ✅ 保持 |
| **输出比值对共同尺度稳定**（核心指标） | ❌ **完全失效**：2:1 输入在 1x/2x/4x 共同放大下，比值从 987728676 飙升到 3950914706（弱通道被减法抑制**直接压到 0**，比值发散、失去意义） | ✅ **精确稳定**：2:1 输入在 1x/2x/4x 共同放大下，比值恒为 **2.000**（方差仅 0.0001，浮点误差量级） |
| 输出总量变动在有限范围 | ❌ 总量随放大线性增长不封顶（0.99→1.98→3.95，正比于输入） | ✅ 总量随放大**亚线性增长且趋于饱和**（1.42→1.86→2.21，典型除法归一化的饱和特征） |
| 多通道竞争后近似归一化 | ❌ 退化为"赢者通吃"（弱通道被完全清零，不是"归一化占比"而是"淘汰"） | ✅ 输出比值 = 输入比值，是真正意义上的占比关系 |

**路径B 在二级验收的核心指标（共同尺度稳定性）上以压倒性优势胜出**——2:1 输入比在三个不同放大倍数下，路径B 的输出比值锁定在 2.000（与理论 `I_a/(I_a+I_b)` 型除法归一化完全一致），而路径A 一旦一个通道的兴奋度不足以对抗共享抑制池，就会被直接压到 0，比值发散到无意义的天文数字——这正是方案原文警告的"`y_a≈φ(I_a−λI_Σ)`…不保证共同尺度不变"在实测中的具体体现，而且比理论警告的"整体增大/饱和/归零"更糟：它直接**归零**，不是"漂移"。

---

## 五、路径B 同步共享池的正确实现（2026-07-16 修订，取代原"顺序调用"分析）

### 5.1 原顺序调用版的不对称原因

`DivisiveNormalizationReceptor.normalize()` 设计为**单实例、单路输入**调用（`compensation.py:340-360`）。最初实验为构造"共享池"，用同一实例依次调用两次（先 `.normalize(I_a)` 再 `.normalize(I_b)`），导致：调用 I_a 时，池状态尚未包含 I_b 本步的贡献；调用 I_b 时，池状态已包含 I_a 本步的贡献——这是**调用顺序**造成的极小先后不对称（1e-4 量级）。交叉比对批判指出这"不是单纯数值误差，而是接口语义不匹配"，评判为正确。

### 5.2 同步共享池的合法实现（只用现有公开接口，实测验证）

`DivisiveNormalizationReceptor` 已公开 `pool_activity` 属性（`compensation.py:337-338`，只读，非本次新增）。合法实现方式：

```python
dn.normalize(i_a + i_b, dt)              # ① 只调用一次，池按总电流更新，返回值丢弃
denom = max(sigma + dn.pool_activity, sigma * 0.01)
norm_factor = sigma / denom              # ② 手算与 normalize() 内部相同的公式
y_a = i_a * norm_factor                  # ③ 同一个 norm_factor 分别乘到两路
y_b = i_b * norm_factor
```

全程只用两个已有公开成员（一个方法 `.normalize()`、一个属性 `.pool_activity`），**未新增方法、未修改/直接读写内部 `_pool_voltage`**，是合法的"只用现有组件"实现——回应交叉比对批判"建议的修复超出了只使用现有组件"的判断（不成立）。这个实现同时天然满足批判第9点提出的更严格要求："所有通道使用同一次 pool 更新后的 G"——因为 `norm_factor` 是从同一次 `dn.normalize(i_a+i_b,dt)` 调用后读出的，`y_a`、`y_b` 保证共享同一个增益。

### 5.2b 修订二（纳入交叉比对第五份批判点1）：`dt=0` 只读方案，比 5.2 更严格地"只用现有组件"

第五份交叉比对批判进一步指出：5.2 的 `norm_factor = sigma / denom` 一行**仍然是在调度代码里重写了 `.normalize()` 内部的归一化公式**——即使读的是真实池状态，这个除法本身是外部手算的，不是调用 `DivisiveNormalizationReceptor` 自己的方法算出来的。这个批评成立，是比 5.2 更严格的"只用现有组件"标准。

**修订二实现**（`_calib_t3_path_ab.py` 的 `run_path_b_synced` 已更新为此版本）：

```python
dn.normalize(i_a + i_b, dt)     # ① 真实 dt：池按总电流更新一次，返回值丢弃
y_a = dn.normalize(i_a, 0.0)    # ② dt=0：只读，但仍走 normalize() 自己的除法逻辑算出结果
y_b = dn.normalize(i_b, 0.0)    # ③ dt=0：只读，同上
```

原理：`dt=0` 时，`normalize()` 内部 `abs_input*dt/C=0`（不注入电荷）、`exp(-dt/tau)=exp(0)=1`（不衰减）——池状态**原地不变**；但函数仍会执行 `denominator=sigma+pool_voltage; norm_factor=sigma/denominator; return input_current*norm_factor` 这几行**它自己的**代码算出返回值。也就是说：这次调用**没有产生副作用（未改变池），但确实调用了 `.normalize()` 的真实除法逻辑**——不是外部重写。

**5 项接口合法性检验**（`test_shared_pool_synced_interface_legality()`，全部精确通过）：

| 检验 | 结果 |
|---|---|
| ① `dt=0` 时池状态完全不变 | `pool_before==pool_mid==pool_after`（三次读取值完全相同：1.153232） |
| ② 等强输入精确对称 | `y_a==y_b`（0.565348==0.565348，比 5.2 版的 1e-4 偏差更干净） |
| ③ 调用顺序交换后输出不变 | 先读 a 后读 b、与先读 b 后读 a，结果逐位相同 |
| ④ 二通道比值精确 | 2:1 输入 → 输出比值精确 2.000000 |
| ⑤ 三通道同池比值精确 | 3:2:1 输入 → 输出比值精确 3.000000 / 2.000000 |

**结论**：修订二是比 5.2 更严格意义上的"只用现有组件"（连除法公式本身都不重写，只调用 `.normalize()` 的 `dt=0` 只读特性），后续正式实施与本报告的实测数据均以修订二为准。

### 5.3 实测对比（`_calib_t3_path_ab.py` 重跑结果）

| 指标 | 顺序调用版（原） | 同步共享池版（修订，仅用现有接口） |
|---|---|---|
| 等强场景 y_a vs y_b 差值 | ~1e-4（顺序不对称） | **精确为 0**（0.00e+00） |
| 2:1 比例在 1x/2x/4x 共同放大下的比值方差 | 0.0001 | **0.000000**（更优） |
| 实现是否新增/修改接口 | 未新增，但批判认为"建议修复"超范围 | **确认只用两个已有公开成员，未新增/未修改** |

同步版**结果不仅没有变差，反而更干净**（等强场景从近似相等变为精确相等）。

### 5.4 排除"常数乘性增益"替代假设（回应批判第9点）

交叉比对批判指出："比值保持不变"本身不能证明这是真正的共享池分流——任何常数乘性增益 `y=g·I` 都会同样满足 `y_a/y_b=I_a/I_b`。这个抽象论点成立，但本报告的原始数据已经包含反驳这个替代假设的证据：若为常数增益，输出总量应严格正比于放大倍数（1x→2x→4x 应呈 1:2:4）。实测同步版总量：

```
1x: 总量=1.3933
2x: 总量=1.8146   （2x/1x = 1.3024，远小于常数增益预期的 2.0000）
4x: 总量=2.1379   （4x/1x = 1.5345，远小于常数增益预期的 4.0000）
```

总量增长明显亚线性/饱和，**排除了"纯常数增益"这个替代解释**，确认观测到的是真正的分流归一化饱和特征（`I_eff=I_raw×σ/(σ+V_pool)`，`V_pool` 随总输入增大而增大，压低归一化因子）。

---

## 六、结论：择定路径B（分流抑制/共享池），可望直接进入 r_ρ 而非停留 r_part

基于实测结果，**推荐路径B 作为 T3 正式实施的路径**，理由：

1. 一级验收两条路径都能过，不构成区分点；
2. 二级验收的核心指标"共同放大时输出比值稳定"上，路径B 精确保持（方差量级仅为浮点误差），路径A 完全失效（发散到天文数字，本质是弱通道被清零）；
3. 路径B 复用**已有物理组件**（`DivisiveNormalizationReceptor`，非 Python 除法，符合项目"不新造类"的约束）；§5.2b 修订二的同步共享池实现（`dt=0` 只读）已验证只用现有公开方法本身（不重写除法公式）即可正确工作，**工程实现路径已闭合**，无需新增或修改任何组件接口；
4. 由于二级验收（共同尺度近似不变）已经在这个最小标量模型上**清晰通过**，正式实施 T3 时**有较大机会直接满足升格 r_ρ 的条件**（而不必长期停留在"r_part 参与竞争关系原型"），但仍需在真实 ξ^occ 驱动、真实 Neuron/Bundle 接线（而非本报告的简化标量 RC 模型）下重新验证一遍二级验收，不能仅凭这次简化模型的结果就直接宣称 r_ρ 达成——**升格判定权在正式实施后的实测，本报告只负责路径择定**。

**路径A（减法抑制）不再作为 T3 的主选路径**——它在最基本的"占比关系"语义上就会失败（赢者通吃而非按比例分配），只在"纯粹的二元 WTA 竞争"这种不需要保留输入比例信息的场景下才有意义，与 r_part/r_ρ 的设计目标（保留占比信息）不符。

### 6.1 r_ρ 的正式数学定义修订（纳入交叉比对第五份批判点2）

第五份批判指出：路径B 输出满足 `y_i=I_i×σ/(σ+V_pool)`——所有通道乘的是**同一个**归一化因子，所以 `y_i/y_j=I_i/I_j` 精确保留了相对参与关系，但**不保证** `Σ_i y_i=1`（实测总量随共同放大呈亚线性增长而非恒定，见 §5.4）。因此正式升格后的 `r_ρ` **不应称为"和为1的占比坐标" `(ρ_1,...,ρ_n), Σρ_i=1`**，而应定义为**射影比例关系**：

```
r_ρ = [y_1 : y_2 : ... : y_n]        （射影等价类，所有共同缩放等价）
[y_1:...:y_n] = [α·y_1:...:α·y_n]    对任意 α>0
```

即 `r_ρ(α·I) ≈ r_ρ(I)`——共同尺度不变性本来就是本报告 §5.4 验证的性质，只是数学表述从"占比坐标"精确化为"射影比例"，避免暗示 Σ=1。**命名纪律更新**：
- 只通过排序、交换、竞争测试（一级验收）：保持 `r_part`；
- 通过共同尺度不变性（二级验收核心指标）：可升格为**射影参与关系** `r_ρ`；
- 只有电路物理上确实使输出总量也近似恒定（额外性质，本报告未验证也不要求），才能称为"归一化占比坐标"。
- 测试脚本仍可计算 `ρ̂_i = y_i/Σ_j y_j` 用于观察/评分，但这个除法**只能是探针读数**，不得进入生产信号路径（同 §五 原则一致）。

---

## 七、遗留给正式实施阶段的具体待办

1. ~~修正 DivisiveNormalizationReceptor 的共享池调用方式~~ **已在 §五 修订版闭合**：正式实施时直接采用 §5.2 的"同步共享池"调用方式（`.normalize(i_a+i_b,dt)` 一次 + 读 `.pool_activity` 手算 `norm_factor`），不需要额外修正工作。
2. 用真实 ξ^occ 源（T0 冻结的 28、31 号点）替代本报告的标量合成输入，重新验证二级验收——需注意 T1 已发现的"真实 ξ^occ 源有 ~400 步启动延迟 + 半自主振荡"特性，可能同样需要 T1 式的合成脉冲优先验证策略。
3. 用真实 Neuron/Bundle 接线（`SynapticBundle` + `NeuronConfig`）复现本报告的标量 RC 模型，标定容值/权重/`sigma`（比照 T1 的"先定增益、反解容值"标定方法论）；`DivisiveNormalizationReceptor` 需要挂到一个真实"共享池神经元"上还是作为独立辅助对象直接调用，需要在正式实施时决定（本报告的标量验证不涉及 Neuron 封装）。
4. 通过升格验收后，把生成元正式重命名为 `r_ρ`（而非停留 `r_part`），并在正式工程报告中记录升格判定依据。
5. 二元验收通过后再扩展到 T2 冻结链的三元占比关系（ξ_31, ξ_28, ξ_23 三通道竞争）。
6. **关系层 step 调度协议**（纳入交叉比对第三份批判点7）：正式实施时须遵守统一的 8 步调度协议（详见 `cell-cell/claudecode方案/基础生成元双轨落地方案_v2_2026-07-16.md` 约束追加条款8）——"共享池必须先汇总全部通道电流再统一更新一次"本身就是该协议第3/4步的具体应用，本报告 §5.2 的实现已经是这个协议的正确示范。
