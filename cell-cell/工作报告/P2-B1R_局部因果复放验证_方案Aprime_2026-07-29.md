# P2-B1R：局部因果复放验证（方案A′）

日期：2026-07-29  
关联：`cell-cell/交叉比对/document - 2026-07-29T132244.981.md`  
状态：完成，T-RPP-R1′~R3′ 3/3 PASS（L2为非阻塞资格）

---

## 一、目标

评判要求执行方案A′：不做完整复放，改用**录制-回放**范式，在学习后冻结状态下输入相同关系脉冲，验证权重差异→局部电流差异（L1资格），以及DA池整体轨迹差异（L2非阻塞资格）。

## 二、实际改动

- 新增 `nexus_v1/tests/test_r_prec_replay_aprime.py`（274行）
- 三个测试：T-RPP-R1′（L1局部电流）、T-RPP-R2′（同状态阻断）、T-RPP-R3′（L2 DA池轨迹）

## 三、发现的真实问题

原方案A（训练期轨迹比较）混入三个变量（关系输入+DA门控+连续学习），无法隔离"已形成记忆改变未来"；原复放方案遇到collector自然激活不足（仅2%步数）。

## 四、测试结果

**L1资格测试：2/2 PASS**
| 测试 | 指标 | 结果 |
|------|------|------|
| T-RPP-R1′-L1 | learned/baseline电流ratio | 1.001169 (PASS, >1.001) |
| T-RPP-R2′ | enabled/blocked电流差 | 0.920 vs 0.000 (PASS) |

**L2测量测试：完成，限制得到确认**
| 测试 | 指标 | 结果 |
|------|------|------|
| T-RPP-R3′-L2 | DA膜电位平均差异 | 0.000000（单束在DA池中不可辨识） |

**回归测试：21/21 PASS**

## 五、冻结契约

1. **录制-回放范式**：训练期录制collector.pre_trace真实序列（554/50000活跃步），回放时直接设置pre_trace值，绕过自然激活不足问题。
2. **L1资格（必须）**：learned权重0.10980比baseline权重0.10875产生高0.117%的局部电流，证明学习后关系连接改变局部下游物理作用。
3. **L2资格（非阻塞）**：单束作用在多输入DA池中信噪比不足（D=0），建议登记 **LIM-RPREC-READOUT-001**（单关系束全局可辨识度有限），非DEG。
4. **同状态阻断**：从learned状态复制enabled/blocked分支，证明电流差异确实来自该bundle。

## 六、未完成项（非阻塞）

1. L2 DA池整体轨迹未达标——单束贡献被淹没，需事件核P2-B1K提供专用下游测量点。
2. 仅验证fast时间尺度（50步）关系，slow（600步）未测试。
3. 仅验证a_prec_b_fast方向，其余3个collector未扩展。

---

## 下一步

评判建议：P2-B1R已取得L1资格，P2-B1K不应再补做最小因果资格，而应证明"这段局部作用能否成为连续事件的元支撑"。等待下一轮P2-B1K或审查点3规格。
