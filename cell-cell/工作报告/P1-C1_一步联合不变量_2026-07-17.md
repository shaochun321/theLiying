# P1-C1：一步联合不变量（差分格式）

## 1. 目标
批判二十四裁定P1-C0补丁通过、C0正式冻结，直接进入P1-C1（V3已冻结的四项范围）：
夹具A/B真实运行/三级账本/完整联合图同构/退化和组合失稳验证。

## 2. 实际改动
- `joint_thermal_step_plan.py`：`JointThermalStepPlan`新增分解字段
  （`node_source_injection`/`node_contact_injection`/`node_transport_net`/
  `edge_contact_transfers`/`edge_transport_transfers`，折叠进`world_injections`
  前的分项，供账本核实"分别计算的物理通道之和"与"实际观测状态变化"是否闭合）；
  `JointThermalStepReceipt`新增`node_ledger_residuals`/`edge_ledger_residuals`。
- 新增 `test_joint_thermal_step_plan_c1.py`：夹具A（5世界节点+1热源+2扩散边+
  2条共享tail传输边+1接触边+1皮肤+环境泄漏）+ 夹具B（1世界节点+2皮肤+2接触边，
  测Σ_sH_is另一半：一节点连两皮肤）。

## 3. 发现的真实问题
无（本轮为设计范围内的正向实现，未发现V3已冻结公式/契约有误）。

## 4. 测试结果
T-C1-1~6全部PASS（夹具A真实运行/夹具B世界侧Σ_sH_is聚合/三级账本全部<1e-9闭合，
覆盖5世界节点+1皮肤+3条边/完整联合图同构φ下world_injections·stability·账本
残差·回执总量全部对应/退化一致性与`couple_world_skin_step()`数值精确一致/
组合失稳在多节点场景下正确拒绝且5节点+1皮肤+1热源全部状态不变）。与既有68项
相关测试+回归21/21全部PASS。

## 5. 冻结契约
`JointThermalStepPlan`的分解字段是节点级账本的唯一数据源（不重新计算物理量）；
边级账本验证的是plan自身逐边对称性（非从聚合观测状态反推单边贡献）；图同构
覆盖source/skin/contact/transport/回执，不止世界图。

## 6. 未完成项
- P1-C2（算子与长程资格：线性正域矩阵测试/过剩边界测试/步长收敛/冻结段驱动段
  浸泡）未开始，留待独立后续轮次。
- skin/source正式接入StructuralAddress——批判二十四裁定留到"C1结果显示无法
  追踪同构映射时再触发"，本轮C1图同构测试（T-C1-4）已能正确完成，未触发该
  条件，继续搁置。
