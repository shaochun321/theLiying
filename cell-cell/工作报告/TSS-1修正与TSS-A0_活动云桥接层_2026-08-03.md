# TSS-1修正 + TSS-A0：Σ_support资格降格与活动云桥接层

日期：2026-08-03
状态：完成
commit：6e5c811
评判依据：document - 2026-08-03T135526.068.md

---

## TSS-1修正（资格降格，不改代码）

原宣称：Σ_reach^(1)（生成元间物理可达性）→ 过强。

实际证明的是：外部局部物理作用激活部分锚定生成元，不激活远方生成元。
近邻站点31也被HeatSource**直接**作用（d=1.09 < radius=2.0），切断的是
内部神经链路（synapse_gain=0），不是皮肤物理传播边。

**冻结结论（修正版）**：局部外部物理发生能够通过锚定链路，产生具有空间
选择性的内部生成活动云（Σ_support^(1)）。

禁止宣称：已证明皮肤点间物理传播/生成元间可达性/内部空间距离。

3/3测试PASS，措辞修正，物理结果有效。

---

## TSS-A0（新文件）

`nexus_v1/relations/activation_cloud.py`，四个桥接类型：

- `PhysicalSupportRef`：外部发生的模态/地址/时间窗口/实例
- `GeneratorAnchor`：α_phys → G_α锚定（皮肤站点↔κ^10）
- `ActivationCloudEntry`：(α_phys, α_gen, y(t), I, parent_occurrence)
- `GeneratorActivationCloud`：ω[W] → C_ω[W]，汇总活动云，阈值过滤

主线位置：
```
皮肤外部发生 → 物理支撑锚定(TSS-A0) → 生成活动云 → Θ/Σ/Λ → 耦合生成元
```

---

## 冻结状态

```
TSS-0：类型契约，通过
TSS-1：Σ_support^(1)物理支撑局部性投影，通过（资格修正）
TSS-A0：物理支撑活动云桥接层，通过
下一步：等评判裁定（TSS-2尺度算子或其他）
```
