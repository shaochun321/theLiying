# EXT-2 全量复验收口工作报告

日期：2026-09-10
分支：`tin-malpais` / `feat/tss-theta`，收口 commit `01e94e5`（本报告提交后 main 同步至含本报告的最新 commit）
环境：参考机独占 CPU
台账出处：`tss/QUALIFICATION_LEDGER.md` EXT-2 节"全量复验（2026-09-10）"小节；`tss/EXPERIMENT_MANIFEST.md`

---

## 一、总览

EXT-2 C1 理论资格复审修改轮（`a888f5e`，报告见《C1理论资格复审修改轮工作报告_2026-09-10.md》）当时只做了**定向验收**（用户裁定：test_c1_coupling 12/12 + 3 diag + 收集数 + 母体回归）。本轮为其**全量复验收口**：跑完整 TSS pytest 套件、manifest 全部非 pytest 边界、母体 21 项回归，并对关键 diag 的结论输出与冻结记录做一致性抽查。

| 验收面 | 结果 |
|---|---|
| 完整 TSS pytest（`pytest tss/tests -q`，224 项） | **222 passed + 2 xfailed / 0 failed**（1:56:05） |
| 非 pytest 完整边界（manifest 一+五节） | **20/20 exit 0** |
| nexus_v1 母体 21 项回归 | **exit 0** |
| 结论完整性抽查（2 个冻结 diag） | 输出与冻结记录**逐项一致** |

**收口结论：EXT-2 轮全部改动（资格语义修正 + 测试拆分/新增 + 3 个缺口登记 diag + 文档同步）在全量边界下零回归；两个 LIM XFAIL 按裁定保持；r2_fork 的 integration→longrun marker 裁定被参考机实测追认。**

---

## 二、完整 TSS pytest 套件

- `pytest tss/tests -q` → **222 passed + 2 xfailed / 0 failed**（共 224 项，耗时 1:56:05）
- 两个 XFAIL 即 **LIM-RPREC-READOUT-001** 的两条 effect-size 项（`test_r_prec_replay_simple` 效应量层 / `test_r_prec_replay` R2 效应量层），确认保持——外部复评已裁定这两个 XFAIL 应保留、禁调阈值放行，本轮文件零触碰
- 收集数 224 与修改轮登记（223→224，+1 = T-C1-6b）一致，旧测试零消失

## 三、非 pytest 完整边界（20/20 exit 0）

manifest 第一节（资格套件中不被收集项）+ 第五节（诊断/探针/实验脚本）全部执行，全部 exit 0：

| 类别 | 脚本 |
|---|---|
| C1 适配器标定 | exp_C1_adapter_calibration |
| 本轮（EXT-2）新增 3 diag | _diag_c1_runtime_lineage_collision / _diag_c1_parent_amplitude_information_loss / _diag_c1_cross_occurrence_history_reconstruction |
| 哈希敏感项（PYTHONHASHSEED=0） | _diag_rprec_effect_compression / _diag_t3_c1r_readout_ab |
| DEG/LIM/T3-C0 系列 | _diag_deg018_dual_clock_equivalence / _diag_lim_population_readout_model / _diag_t3_c0_ratio_audit / _diag_t3_c0_multiseed / _probe_t3_c0_seed |
| P2-A 系列（8 个） | exp_P2A1a_*(2) / exp_P2A1b_*(3) / exp_P2A3_*(2) / exp_P2A_highinput_root_cause |
| 哈希扰动稳定性 | exp_memristor_stable_hash_verification |

注意 3 个新 diag 的 exit 0 语义（修改轮冻结）：= **缺口已正确复现并登记**（A9 runtime GAP / 幅度信息损失现状 / cross-occurrence 历史可由 H_τ 完全重构），不是资格 PASS。

## 四、结论完整性抽查

抽查两个承载冻结定量结论的 diag，输出与冻结记录一致：

- `_diag_rprec_effect_compression`：总压缩 **297× = 8.3× × 35.8×**、learned 权重局部电流上限 **0.117%**——与 LIM-RPREC-READOUT-001 冻结记录一致
- `_diag_lim_population_readout_model`：**N-不变**（多束方向定量否证判据Ⅰ）、**N\*≈132**（判据Ⅱ仅解一层）——与 LIM 多束读出标度律冻结记录一致
- 两者实测时长（20s / 49s）低于 manifest 参考值（~3min / ~8min）——负载因素（本轮独占 CPU），已核实为完整执行输出而**非提前退出**

## 五、母体回归

`nexus_v1` 母体 21 项回归 exit 0。EXT-2 轮物理路径零改动（仅资格语义/测试/文档），符合预期。

## 六、慢项实测（--durations 摘录）

| 测试 | 实测 |
|---|---|
| r_prec_t3 | 617s |
| c1_coupling（c1_1 共享测量） | 606s |
| c0_audit | 597s |
| r2_x2c | 423s |
| **r2_fork（r2f_3）** | **209s** |

r2_fork 参考机实测 209s（≈3.5min），远超 integration ≲60s 定义——修改轮基于外部两轮实测（182s/121.5s）做出的 **integration→longrun marker 裁定被参考机实测追认**。manifest 该行参考机时长已同步更新（`01e94e5`）。

## 七、main 同步记录与遗留

**main 同步（本报告轮执行）**：local `main` 自 `2f20aaf` fast-forward 至含本报告的最新 commit，纳入三个收口提交：

- `a888f5e` — EXT-2 C1 理论资格复审（A8 NOT_MET 机器化 T-C1-6b + A9 GAP 登记 + 资格语义修正）
- `736c749` — 修改轮工作报告
- `01e94e5` — 全量复验收口（台账+manifest）

不 push origin（origin/main 长期落后，历史惯例为本地同步）。

**遗留 / 下一步（与修改轮报告一致，无新增）**：

- 下一步为设计真正满足 A8/A9/K-06 的"时间方向组织候选"——未启动
- K-06 组织闭合仍阻塞于 R-E0-3 拓扑保护定义裁定
- A9 修复只走 sidecar 方向（物理路径原样），待理论裁定
- 项目 organism 侧整体仍处暂停状态（2026-09-06 用户决定），BACKLOG 旧条目不作为重启入口
