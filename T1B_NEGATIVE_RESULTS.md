# T1B_NEGATIVE_RESULTS — 负结果登记（§57，同等保存）

日期：2026-09-19

1. **legacy 在 World v2 合法域全域再现旧病**：cal 30 中 29 个
   DEGRADED/SEMANTIC_FAIL、held-out 20 中 20 个——floor/ceiling/动态
   范围塌缩不是 v1 特例，是结构性的（负控制职责完成）。
2. **naive 每步差分 RATE_PORT_DT_DEPENDENT_FAIL**：dt 每细化一档幅值
   精确 ×0.100——T1-A 原型的 [T/step] 形式被定量证伪，dt-aware 形式
   为唯一合法速率口（D2 债务定量实证）。
3. **A 喂现 L1 仍为语义错位**（T1-A 负结果延续）：smoke 未测 A（其
   消费端 L1 幅值语义属 G0_RECONNECT）；A 的合格是**端口级**，
   全链路资格待 L1 改造。
4. **occurrence smoke=0 次发生**：B 桥喂入下 L1 触钳（10.0）且
   collector 活跃（1.0017）但 closure 未发生——rearm=500 与
   θ_up/θ_down 标定源于 v1 尺度，**v2 输入下 closure 阈值需
   occurrence revalidation 重资格化**（只登记，未调参——§34 纪律）。
5. **LORO 退化**：canonical 零拟合使 leave-one-region-out 无"拟合域"
   可留出，退化为跨域状态检查——真正的 LORO 强测试将在未来任何
   需要拟合参数的层（如 L1 适应态参数）上才有效。
6. **强耗散角未触发 STRUCTURALLY_EXPECTED_ZERO**：r_leak<50 的
   held-out episodes 源燃烧期边界变化率仍高于近静判据（1%）——
   该类别在本轮数据中未被行使，其判据逻辑保留待 G0_RECONNECT 后
   更贫瘠 episode 域检验。
