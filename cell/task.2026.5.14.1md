# Morphosphere v38 Improvement — Task Tracker

## Phase 1: 诚实化 (~1 week)

- `[ ]` **1.0** 创建 DB 备份锚点 (pre-v38 anchor)
- `[ ]` **1.1** 独立测试集隔离
  - `[ ]` 创建 `run_v38_honest_baseline.py` — 用不同 seed 的 train/test 分离
  - `[ ]` 使用 `BayesianMotionRecognizer` 替代 `LegacyLookupRecognizer`
  - `[ ]` 报告：准确率 ± 标准差、混淆矩阵、95% 置信区间
- `[ ]` **1.2** 非平凡收敛
  - `[ ]` 修改 `pipeline_isolator.py` `run_convergence()` 加入反馈环路
  - `[ ]` 学习率衰减 + 收敛检测
  - `[ ]` 验证 drift > 0 然后趋向 0
- `[ ]` **1.3** Xin 数据驱动化
  - `[ ]` 修改 `pipeline_engine.py` `write_xi()` — Xin 质量基于预测残差

## Phase 2: 数学硬化 (~2 weeks)
- `[ ]` **2.1** 变分 GMM 替代 softmax PRX
- `[ ]` **2.2** 最优传输 (POT 库)
- `[ ]` **2.3** Oja 规则强化

## Phase 3: 数据扩展 (~1-2 weeks)
- `[ ]` **3.1** 扩展运动体制 (6 → 8+)
- `[ ]` **3.2** Allen Brain Observatory 接入

## Phase 4: 自验证 (~1 week)
- `[ ]` **4.1** 统计检验
- `[ ]` **4.2** 基线对比
- `[ ]` **4.3** 最终自评报告
