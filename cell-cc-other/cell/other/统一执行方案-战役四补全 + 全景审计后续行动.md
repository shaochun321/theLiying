### 一、全景扫描的确认结果

基于 `diag_bundle_audit.py` 对 27 份文档与代码库的逐项比对，识别出以下状态：

| 类别 | 数量 | 内容 |
|------|------|------|
| ✅ 已合入 | 多项 | thermal 差分束、AGC、Langevin、Memristor 修复等 |
| ❌ 未合入（需修正）| 1项 | **vestibular col→motor 束参数** |
| ⚠️ 待评估 | 2项 | aff→enc initial_weight、Phase 1 DA 门控 |
| 🚫 否决 | 4项 | V8.4 热编码参数、Klinotaxis、C_max 修改、cross-axis 增益提升 |

### 二、执行决策表

| 差距项 | 描述 | 决策 | 理由 |
|--------|------|------|------|
| **战役四 vestibular 束** | gain=5.0, lr=0.005 未与 thermal 束对齐 | ✅ **立即修改** | 两种模态共用 Motor，参数不一致导致模态失衡 |
| aff→enc initial_weight | 文档要求 2.5，代码实际 0.2 | ❌ **不修改** | 当前 0.2×gain=2.0 已能激活 Enc，且保留了 weight_max=1.0 下的 STDP 学习空间 |
| Phase 1 DA 门控 | 原计划用 DA 直接调制反射增益 | ❌ **废弃** | AGC 已通过慢速积分实现类似功能，无需额外硬接线 |
| V8.4 热编码参数 | 远端的非脉冲+CRI 参数 | ❌ **否决** | 与本地 Phase 1-3 已验证配置冲突，风险不可接受 |

### 三、执行步骤：战役四补全

**修改文件**：`hebbian.py`（L484-485 附近，vestibular 轴特异束定义区）

**修改内容**：

| 参数 | 当前值 | 新值 | 变动 |
|------|--------|------|------|
| `stdp_lr` | 0.005 | 0.05 | 10× |
| `synapse_gain` | 5.0 | 10.0 | 2× |

**涉及束**（3 条）：
- `col_yaw_to_move_x`
- `col_pitch_to_move_y`
- `col_roll_to_move_z`

**修改后状态**：vestibular 束与 thermal 束的增益和学习率完全对齐，两种模态在 Motor 输出端具有相同的驱动能力和学习速度。

### 四、验证协议

```bash
python -m pytest nexus_v1/tests/test_regression.py -x -v
```

**重点关注测试**：
- T4.1（轴/交叉权重比）：确认增益翻倍后轴特异束仍主导交叉束
- T4.2（交叉权重上限）：确认交叉束未突破 w_max=0.15
- T4.3（运动分化）：确认 x/y/z 三轴输出仍有区分度

### 五、后续规划（代谢壁问题）

战役四补全后，全景审计识别的唯一未解决问题是**代谢壁**（fill 在 100k 步内耗尽）。这是一个独立于上述所有差距的结构性问题，应在本次战役四补全验证通过后作为下一个专项处理。

### 六、执行确认清单

- [ ] 备份当前 `hebbian.py`
- [ ] 修改 L484-485：`stdp_lr=0.005` → `0.05`，`synapse_gain=5.0` → `10.0`
- [ ] 运行回归测试，确认 12/12 或 21/21 PASS
- [ ] 提交 commit，标注为“战役四补全：vestibular 束参数对齐”